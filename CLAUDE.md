# 專案慣例

## 狀態一定要記在 `docs/STATUS.md`

`docs/STATUS.md` 是待辦／進行中／完成的**唯一事實來源**（會同步到 Notion）。

**每次工作結束前必做**，不用等使用者提醒：

1. 新開工的事 → 寫進「🔵 進行中」，附動到的檔案。
2. commit 完 → 從「進行中」搬到「✅ 完成」，補上 commit hash 與日期。
3. 過程中發現但這輪沒做的事 → 寫進「🔴 待辦」，標 `TODO` / `BLOCKED` / `DECIDE`。
4. 更新檔頭的「最後更新」日期。

**狀態必須對得上 repo 實況，不要用印象填**：

- 標 `DOING` 之前確認 `git status` 真的看得到。
- 標 `DONE` 之前確認 `git log` 真的查得到。
- **線上有沒有生效不要猜**——程式進了 main 但沒驗證線上，一律標 `DEPLOY?`。

每個項目都要有一句「為什麼」。三個月後只有 why 有用。

## commit 訊息要寫「改了什麼、為什麼」

**禁止只寫 `update`、`fix`、`wip` 這種沒有資訊的訊息。**

一行講清楚改動的實質內容，看得懂比精簡重要：

- ✅ `時間軸：事件下界不再敞開到 epoch，錨定的會話要驗歸屬`
- ✅ `修正續扣單可能被誤作廢造成重複扣款`
- ❌ `update`
- ❌ `fix bug`

判斷標準：**三個月後有人用 `git log` 找「那個重複扣款的修正在哪」，光看訊息要找得到。**
找不到的話，之後任何人要重建「這週做了什麼」都只能一個個翻 diff，成本極高。

## 評估報告寫完就凍結

`docs/` 底下的 `*-EVAL-*.md`、`*-DESIGN-*.md` 是**決策紀錄，不是狀態看板**。

- 寫完就當歷史文件，**不要回頭改狀態**。
- 裡面不要寫「未 commit 未部署」這種會過期的話——狀態一律只寫在 `STATUS.md`。
- 報告只留：當時看到什麼、為什麼這樣決定、拍板結果。
- 結論要同步一份到 `STATUS.md`，報告本身當附錄。

## 同步到 Notion

`STATUS.md` 是上游，Notion 是展示層，**單向同步，不要反向手動改 Notion**。

- 節奏：**一週一次**就夠。Notion 給主管／PM／會計看，不需要即時。
- `STATUS.md` 每個項目給一個固定編號（`A-1`、`B-3`…），Notion 對應欄位存同一個編號，
  同步時照編號比對，不要靠語意猜。
- 同步前先確認 `STATUS.md` 本身沒過期（尤其行數、檔案數這類會漂的數字）。

## 其他

- 詳細規格放 `docs/` 專門文件，`STATUS.md` 只放指標與一句話說明。
- 樣式寫在 `app/assets/scss/` partials + `main.scss @use`，不要寫在 `.vue` 的 `<style>`。
- 多租戶：Firestore 結構變更（索引／TTL）通常要對**兩個**專案各跑一次，見 `docs/` 與記憶 `reference_firestore_index_deploy`。
