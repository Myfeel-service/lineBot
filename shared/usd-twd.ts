/**
 * 美金換台幣的匯率 —— **全站只有這一個值**。
 *
 * 2026-09-07 收斂前這個 32 在五個地方各寫死一次（`ai-cost-buckets.ts`、`host-costs.get.ts`、
 * `infra-costs.get.ts`、`costs.vue`、`super/workspaces.vue`），後端明明有回 `usdToTwd`、
 * 前端卻用自己那份＝兩個事實來源（`STATUS.md` 的 `B-44`⑥）。改一次匯率要改五個地方，
 * 漏一個就會出現「同一筆錢在兩張卡上金額不同」。
 *
 * ⚠️ **32 是刻意估高的保守值**，不是即時匯率（2026-08／09 兩次對帳，Google 帳單回推的
 * 實際匯率約 30.6）。理由是這幾張卡的用途是「不要被帳單嚇到」，寧可畫面比帳單高一點。
 * 代價是拿它跟主控台逐元對帳會固定高約 4%——真要對帳請看主控台，不要調這個值去湊。
 */
export const USD_TO_TWD = 32
