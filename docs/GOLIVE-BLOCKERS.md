# 上線缺口清單 — 缺什麼、去哪拿、為什麼要

> 目的:**讓「還缺什麼資料」不必再重問一次**。以前的規格都是靠截圖口耳相傳,沒落地成檔案,
> 所以同樣的問題會一直重複。從現在起:**任何從後台／官方文件截圖確認到的事實,都寫進這裡或
> `PAYUNI-RECURRING-DESIGN.md`,不要只留在對話裡。**
>
> 最後更新:2026-08-04。程式面 P1~P5 已完成(見 `PAYUNI-RECURRING-DESIGN.md`),以下都是「拿資料」而非寫程式。

---

## 🔴 A. 擋住「自動扣款實刷驗證」

### A1. 開通「約定信用卡幕後交易」 ← ✅✅ **沙盒已解決,不用送申請書(2026-08-06 實測)**

- **原症狀**:UPP 帶 `UseTokenType=1/3` → `UPP02087 商店未提供約定信用卡幕後交易`。

#### ✅✅ 2026-08-06 實測:`UPP02087` **已經消失**,沙盒建約定通了

對沙盒特店 `NPPA836109422` 打真 UPP,一組對照:

| 探針 | 回應 | 頁面回吐的 `UseTokenType` |
|---|---|---|
| ① 純單次付款（Version 1.0,不帶建約定欄位) | `success: true` /「進入支付頁」 | `0` |
| ② **建約定 `UseTokenType=3`（Version 2.0,我方正式要用的那組)** | **`success: true` /「進入支付頁」** | **`3`** |

→ **付款頁不但開得出來,還把 `UseTokenType: 3` 原樣回吐** = 建約定的參數真的被收下了,
   不是被靜默忽略。全文搜不到 `02087` 也搜不到「未提供」。

**結論:後台「串接設定 › 限定 API 設定」的開關真的有效,`UPP02087` 是純粹的開關問題。**
**→ 四份申請書(05/07 × 測試/正式)為了「開發與測試」完全不用送、不用用印。**
(正式店那兩份也大概率不用——正式店的 Token 開關 8/4 截圖就已經是啟用狀態。)

⚠️ 一個踩過的小坑:偵測錯誤碼別用 `/UPP\d{5}/` 去掃 HTML —— 我方訂單號叫 `PROBEUPP…`,
正則會抓到自己的訂單號,誤判成 `UPP69017` 這種不存在的錯誤碼。要看 `window.JS_INFO` 裡的
`success` / `message`。

#### ✅✅✅ 2026-08-06 **首刷真刷成功 + 整條鏈路第一次用真憑證走完**

實測台:`scratchpad/first-charge-run.ts`(本機小 server 收 `ReturnURL` + puppeteer driving 付款頁)。
`NotifyURL` 是 server→server(localhost 收不到,不影響);**`ReturnURL` 是瀏覽器導轉,localhost 收得到**
→ 首刷結果與 `CreditHash` 從這裡拿。付款頁畫面確認寫著「**一次付清信用卡 Token 授權**」+ 綁卡揭露紅字。

| 步驟 | 呼叫的我方函式 | 結果 |
|---|---|---|
| **① 首刷(UPP 建約定)** | `buildUppForm` + `buildTokenBindFields` | ✅ `SUCCESS 授權成功`、`TradeStatus=1`、台新 812、`ResCodeMsg=授權成功(模擬)`,**回傳 `CreditHash`(64 碼) + `CreditLife=1229` + `Card4No=0001`** |
| **② 續扣(IP 未列白名單時)** | `chargeCreditToken` | ⛔ `CREDIT03010 不提供此IP幕後交易，223.140.249.79` ← IP 白名單擋住(見 A3) |
| **②' 續扣(把該 IP 填進沙盒後台白名單後,同一支程式重跑)** | `chargeCreditToken` | ✅ **`授權成功`**、`TradeStatus=1`、`AuthCode=000000`、台新 812。**NT$399 與 NT$299 各刷一筆都成功 → 「每期金額可變」在金流層確認可行**(老闆拍板的折抵 #1 與降級期末生效 #2 的前提成立) |
| **③ Token 查詢** | (端點已知,程式尚無專用函式) | ✅ `查詢成功`:`CreditTokenStatus=1`(有效)、`CreditToken=probe-workspace-id`(**我方參照字串原樣回來**)、`CreditTokenType=2`、`CreditTokenExpired=1229`、`Card6No/Card4No` |
| **④ 解約** | `cancelCardBinding` | ✅ **`取消成功`** ← 過去只用假 Token 測到 `CANCEL03001`,**這是第一次真的成功**,證實 `BindVal`+`UseTokenType=1`+`CreditTokenType=2` 全對 |
| **⑤ 解約後再續扣** | `chargeCreditToken` | ✅ 回 `CREDIT02025 約定信用卡不存在` → **解約真的生效** |

**→ 程式面(加密/簽章/欄位/版本號/解約參數)全部用真資料驗證過,零未知。**
**唯一擋住續扣的是來源 IP,不是程式** —— 而 IP 一填進白名單,同一支程式立刻授權成功(②')。
**這同時證明中繼站方案必定可行**:PAYUNi 只認來源 IP,給它一個固定的就通。
③ 的查詢端點值得補一支正式函式(帳單頁可顯示卡片狀態)。

#### ✅✅✅ 2026-08-07 **端到端實測完成(我方後台 + PAYUNi + 光貿 一條龍)**

不是單元測試,是真的跑一次:臨時建租戶 → `/api/payment/create-order` → puppeteer 用測試卡真刷
→ `/api/payment/reconcile` 補開通 → 開發票 → 把本期結束日改成昨天 → 再 reconcile → **真的續扣**。
測完全部刪除(workspace/成員/訂單/發票/auth 使用者 + PAYUNi 解約),正式資料回到原本 5 個 workspace。

| 環節 | 結果 |
|---|---|
| 建單 | ✅ `kind=period_first`、`anchorDay` 已鎖、`termsVersion`/`termsAcceptedAt` 有記 |
| 真刷（測試卡 `4147631000000001`） | ✅ 導回我方 `ReturnURL` |
| **漏接 Notify 的補救**（`trade/query`) | ✅ `recovered:1` → 訂單 paid、訂閱開通 |
| 發票 | ✅ 首刷 399 → `ZA10031070`；799 → `ZA10031073` |
| **期末 roll → past_due → 續扣** | ✅ `recurring: { due:1, charged:1 }`、訂單 `RDY…` `kind=period_recurring` paid 799 |
| 續扣那期的發票 | ✅ `ZA10031074` |
| 訂閱最終狀態 | ✅ `starter/active`、`autoRenew:true`、有卡片憑證、末四碼 `0001`、`lastChargeDate` |

#### 🐛 **實測抓到一個會「客戶付了首期卻靜默降級」的真 bug（已修 + 有測試釘住）**

**症狀**:第一輪 e2e 跑完,訂閱開通了、發票也開了,但 `payuniCardToken` 是空的、`autoRenew=false`
→ 期末直接被降回免費層。dev log 有那行預埋的警告:`訂閱首期已付款但未取得約定 Token`。

**根因**:首刷的 `CreditHash` **只出現在 UPP 的 Notify／Return 回傳裡一次**。
Notify 沒送達時,補救路徑靠 `trade/query` 把訂單補成已付款,而**`trade/query` 的回傳結構裡
根本沒有 `CreditHash`**(實測列出全部 20 個欄位:MerTradeNo/TradeNo/TradeAmt/TradeFee/
TradeStatus/PaymentType/PaymentDay/CreateDay/Gateway/DataSource/Card6No/Card4No/CardExp/
CardInst/AuthCode/AuthType/CardBank/CloseStatus/CloseAmt/RemainAmt)→ 憑證就此遺失。
→ **「收了錢卻沒開通」有補救,「收了錢卻沒有續扣憑證」原本沒有。**

**修法**:新增 `queryCardBinding()`(payuni.ts),在補開通時用**我方首刷送出的參照字串**
(`CreditToken` = workspaceId)向 `credit_bind/query` 反查憑證,塞回 result 讓下游照原路吃
(`server/utils/payuni-reconcile.ts`)。查不到也照樣開通(錢已收),只是照舊記 `cardBindFailed`。

**⚠️ 反查的隱藏必填欄位(逐個變體實測出來的,官方文件沒寫)**:
| 送出的欄位 | 回應 |
|---|---|
| `{ CreditToken }` | `QUERY03001 查無符合綁定資料` |
| `{ CreditToken, CreditTokenType: 1 }`(會員級) | `QUERY03001` |
| **`{ CreditToken, CreditTokenType: 2 }`(商店級 = 我方首刷用的)** | **SUCCESS**,回 `CreditHash` / `Card4No` / `CreditTokenExpired` / `CreditTokenStatus` |

**修完重驗**:同一條路再跑 → `有卡片憑證: true`、`autoRenew: true`、期末續扣 `charged:1` ✅

#### ⚠️ 另一個沉默的洞:**每月扣款原本沒有任何排程在打(已補)**

`/api/payment/reconcile` 是唯一 `charge:true` 的路徑,而它**需要另外建一個 Cloud Scheduler 任務**;
middleware 的 tick 刻意不刷卡(Lambda 會凍結容器)。**那個任務沒建 = 自動扣款完全不會發生,
而且沒有任何錯誤**(客戶寬限 3 天後靜默降級)。
→ 已把 `billing:reconcile-and-charge` 掛進**現有的** `/api/cron/run-tasks`(Cloud Scheduler 每 10 分鐘
已在打),不必新建排程。安全性靠三層而非頻率:旗標未開回 0 / 每日 claim / 訂單號冪等鍵。

**兩個實測學到的行為(程式相關)**:
1. **同一張卡 + 同一個 `CreditToken` 參照 + 同一特店 → `CreditHash` 是同一組**(解約後再建約定,拿到的
   還是 `625566D4…`)。→ 換卡覆蓋舊 Token 的邏輯要注意「同卡重刷不會產生新 Token」;
   也代表解約後客戶用同一張卡回來,Token 會「復活」成同一個值。
2. **`credit_bind/cancel` 不吃來源 IP 限制**(IP 沒填時假 Token 就能得到 `CANCEL03001`,真 Token 也能
   成功取消)→ 只有**扣款類**(`/api/credit`)真的檢查 IP。中繼站其實只有續扣那一支是硬需求。

#### ✅ 找到開關了:**會員 › 商店清單 › 點該商店 › 「串接設定」› 限定 API 設定**

2026-08-04 截圖確認（**正式商店 NPPA836109423**）。那一頁有:

| 區塊 | 內容 |
|---|---|
| **API 串接金鑰** | 商店代號 / **Hash Key** / **IV Key**（正式金鑰就在這裡拿;旁有「更換金鑰」鈕） |
| **API URL** | Notify URL / Return URL（目前空白) |
| **限定 API 設定** | 一排開關:ATM 幕後取號 ✅ / 超商代碼幕後取號 ✅ / **續期收款幕後授權API ✅** / **信用卡Token API ✅ 啟用** / 非信用卡退款轉匯 ✅ / 優惠券全額折抵幕後 ❌ |
| **限定 API 之 IP 設定** | 「需鎖定限定API之來源IP,**您可設定最多 10 組**」;多筆以半形逗號分隔、按 enter 加入。**目前空白** |

**🔥 兩個關鍵事實:**

1. **正式商店的「信用卡Token API」開關已經是啟用狀態** → 窗口說的「正式環境應該不用再申請」得到證實。
2. **IP 是自己在後台填的,不是靠申請書**（申請書上那三格只是紙本備查;系統實際上吃 10 組）。

**→ 待確認的關鍵一步:去看「測試商店 S076820628」的同一頁。**
若它的「信用卡Token API」開關是**關**的 → 自己打開 → `UPP02087` 很可能就消失,
**四份申請書完全不用送、也不用用印。**（正式店已經是開的,所以正式那兩份大概率也不用送。）

#### 兩個容易搞混的錯誤訊息

| 錯誤 | 原因 |
|---|---|
| `UPP02087 商店未提供約定信用卡幕後交易` | **該商店的「信用卡Token API」開關沒開** |
| 「不提供**此IP**幕後交易」 | 開關開了,但**來源 IP 沒填進「限定 API 之 IP 設定」** |

→ 兩者都要處理:開關要開 **且** IP 要填。

#### ⚠️ 安全提醒

「串接設定」那一頁**同時顯示正式商店的 Hash Key 與 IV Key**。
- 那張截圖**不要再轉傳給任何人**（有金鑰 + 商店代號就能偽造請求）。
- 上線前建議按頁面上的「**更換金鑰**」轉一次,並同步更新 Amplify 的
  `PAYUNI_HASH_KEY` / `PAYUNI_HASH_IV`（換了沒同步更新 = 正式收款全部驗簽失敗)。

#### 已經找過、確定沒有的地方

- 「商業條件及支付工具設定」的信用卡區塊只有「一次付清(國內卡)」與 Apple/Google/Samsung Pay,**沒有 Token 項目** —— 開關不在那頁,在「串接設定」。

### A2. 申請書 ← **正式上線一定要;測試要不要「先問客服」**

**下載路徑**(2026-07-30 截圖確認):PAYUNi 文件中心 → 側邊選單最下方「**相關文件及申請書下載**」
→ 「**統一金流平台申請書 › 文件下載**」→ 共 16 張 xlsx（另有「金流平台紙本申請書 (全).zip」可一次下載）。

**與我們有關的只有兩張**（檔名已截圖確認）:

| 檔名 | 這是什麼 | 我方沙盒狀態 |
|---|---|---|
| `05.PAYUNi_幕後功能API申請書.xlsx` | 「幕後」= server 對 server 直接扣款,不經付款頁 | ✅ **看起來已開通**(`/api/credit` 回的是業務錯誤,不是權限錯誤) |
| `07.PAYUNi_信用卡Token API申請書.xlsx` | 把卡片存成 Token,之後憑 Token 扣款 = **訂閱的核心** | ❌ **缺這張**(`UPP02087`) |

> ⚠️ 正式特店可能**兩張都要送**(沙盒的 05 已開不代表正式也開)。

**申請書實際長相**(2026-07-30 截圖確認,Ver.20260126):

- 表頭:申請日期、**會員編號**、統一編號/身分證字號、企業名稱、連絡電話、電子郵件、申請人
- 「申請異動後設定」表格:**逐列填「商店代號 / 商店名稱」,再勾「啟用信用卡 Token」與「變更 IP位置」**
  → **沙盒商店 `S076820628` 本身就是一個商店代號,可以直接列在表上一起申請。**
  也可選「全部商店統一設定,共計 ___ 間」。
- **「幕後授權綁定IP」只有 3 個格子** → ⚠️ **是填「固定單一 IP」,不是網段(CIDR)**。
  這對 A3 是壞消息:Amplify 動態 IP 不可能填進 3 個格子 → **正式上線很可能真的需要固定出口 IP
  (NAT Gateway)**,務必在信裡問清楚。
- 異動原因:網站UI一致性 / 自有設計流程 / 其他
- 兩條紅字注意事項(**這兩條是合規義務,不是免責樣板**):
  1. 「為確保買方交易安全,如有卡號相關資料外洩風險,須由**收款會員承擔**相關責任。」
  2. 「因『第三方支付服務定型化契約應記載及不得記載事項』規定,會員應於**交易頁或網站揭露
     第三方支付業者 PAYUNi 平台服務條款**…若信用卡交易筆數或金額達國際組織規範,
     **須依規定配合進行 PCI DSS 資安認證**。」
     → 前半段我們已做到(結帳同意條款 + 法務頁);**後半段「量大要做 PCI DSS」要記在心裡**,
     不是現在的問題,但屬於規模化後的成本。

**送件規則**(申請書頁面的說明,原文照抄):
1. 申請表填妥後,請掃描後 mail 至 `service@payuni.com.tw`
2. 資料之填寫如有重複、錯誤或虛偽不實等,本公司可拒絕異動並且暫停提供服務
3. 已詳閱統一金流服務條款,資料如有異動其他權利義務依會員條款為主且不得抵觸
4. **商業用戶請蓋大小章** / 個人用戶請蓋章或簽名
5. **申請書須由本人親自簽名**,代簽名者須負法律責任

→ 白話:**要蓋公司大小章 + 負責人親簽 + 掃描寄信**。這是有實體流程成本的,所以見下方建議。

**建議順序（先便宜的）**:
1. **先截「串接設定 → 限定 API 設定」看有沒有開關**（見 A1）。
2. **再寄一封信問客服**（5 分鐘,不用蓋章）——問「沙盒能不能直接幫我們開」+ A3 的 IP 問題。信件範本見本檔最後。
3. **同時開始準備 07（和正式的 05）**——因為**正式上線一定要送**,這點沒有疑問,不要等問完才開始跑蓋章流程。

### ✅✅ **PAYUNi 窗口已親口確認（2026-07-31）**

我方問:「測試環境是不是也要先送 07 信用卡Token API申請書才能開通?」
窗口回:「**對，這個是需要填寫申請表單的。**」

→ **爭議結束:沒有測試豁免,申請書是必經之路。蓋章流程立刻開始跑,不要再等。**

以下是先前找到的官方文件佐證(與窗口說法一致):

✅ **證據已升級成官方明文（2026-07-31）**。爬完整份 API 文件後,「信用卡幕後Token交易(CREDIT)」
的串接前置作業寫著:

> 「須向PAYUNi提出申請,**審核開通且綁定幕後授權IP即可使用**。」
> 「下載申請表單向PAYUNi提出信用卡Token機制申請與綁定IP,**審核通過後即可串接**。」

「**即可串接**」不是「即可上線」→ **測試環境也要申請,沒有豁免**。所有「幕後」類 API
（Token、續期收款、超商代碼、街口、AFTEE、免跳轉元件）都是同一句話。

→ **所以 07 申請書是必經之路,蓋章流程現在就可以開始跑。** 客服信仍值得寄（問沙盒能否從速開通
+ IP 的問題),但不要因此延後送件。

### A2-1. 帳號與商店代號對照（2026-07-31 確認,填申請書要用）

| | **會員編號** | **商店代號** | 後台 |
|---|---|---|---|
| **測試（沙盒）** | `P236588346` | `S076820628`（MiniMeTest） | sandbox.payuni.com.tw |
| **正式** | `P2303845057` | `NPPA836109423` | www.payuni.com.tw |

→ **兩個會員編號不同,而申請書表頭只有一格「會員編號」→ 必須送兩份**（測試一份、正式一份）。

共用欄位:統編 `83610942`、企業名稱「麥菲爾股份有限公司」、Email `service@myfeel-tw.com`、
異動原因勾「**自有設計流程**」（我們是自建系統,不是套版購物車）。

### A2-2. ⚠️ 05 表第 3 條紅字:啟用幕後 = 放棄 3D 驗證,爭議款自負

05（幕後功能API）申請書比 07 多一條紅字（07 沒有這條）:

> 「3.全平台預設商店為 3D 交易,如會員申請**啟用信用卡幕後同等於不使用信用卡 3D 驗証機制**,
> **應自行承擔付款方因使用信用卡付款所生之所有爭議款項。**」

**白話**:一般刷卡有 3D 驗證（銀行發簡訊給持卡人),持卡人事後主張「不是我刷的」時,責任在銀行。
**幕後扣款沒有 3D**（持卡人不在場,沒人能收簡訊)→ **持卡人主張爭議時,錢由我方吸收。**

**這對訂閱制是不可避免的** —— 全世界的月租訂閱都是這樣（card-not-present recurring)。
但它是真的商業風險,要知道:

| 我方現況 | 效果 |
|---|---|
| **首刷走 UPP 且過 3D** | 第一筆有 3D 保護,只有**續扣**那幾期沒有 |
| 已存 `termsAcceptedAt` / `termsVersion` | **爭議時的舉證資料**（客戶當初明確同意每月扣款) |
| 付款頁 `ProdDesc` 帶品牌名 MiniMe | 客戶對帳單看得懂是什麼,降低「這什麼鬼扣款」型爭議 |
| ⚠️ **收據信尚未開通（SES 未設）** | **這是目前最大的缺口** —— 收得到收據的客戶爭議率明顯較低。上線前應該開 |

→ **結論:不是不能做,是要接受這個風險並把舉證與通知做好。**
   **SES 通知信從「選配」升級成「上線前該做」。**

### A3. 綁定授權 IP 怎麼處理(**已有實證,是真的**)

- 幕後交易要綁定授權 IP,但我方跑在 **AWS Amplify(動態 IP)**。
- **實證(2026-07-30)**:打 `/api/period` 回
  `PRD02043 商店未提供續期收款幕後交易，223.143.244.121` — **PAYUNi 把我方對外 IP 一起回傳**,
  證實它對幕後 API 會檢查來源 IP。
  (注意:`/api/credit` 從**同一個 IP** 打過去是正常回業務錯誤的,所以那支沒被 IP 擋。)
- **申請書上「幕後授權綁定IP」只有 3 個格子**(2026-07-30 截圖) → **固定單一 IP ×3**,沒有網段欄位。
- **官方文件全篇沒有任何「網段 / CIDR」字樣**（2026-07-31 全文檢索確認）→ 幾乎可以確定只吃單一 IP。
- ⚠️ **連「開發機」的 IP 都是浮動的**（2026-07-31 實測）:
  同一台機器,上午 PAYUNi 回報我方 IP 為 `223.143.244.121`，數小時後查是 `118.232.253.83`。
  → **不只正式環境（Amplify）有這問題,連沙盒測試那一份申請書也填不出一個穩定 IP。**
  這是跟窗口談 IP 網段時最有力的具體證據。
- **要問 PAYUNi**:能不能填 IP 區段／能不能不綁。

#### ⛔ 2026-08-06 **結案:IP 檢查是真的在擋,「留空 = 不限制」錯誤** ← 用真 Token 才現形

**先講結論:`CREDIT03010 不提供此IP幕後交易，223.140.249.79`** —— 用**首刷真的拿到的 `CreditHash`**
打沙盒 `/api/credit` 續扣,IP 立刻被擋。**固定出口 IP 是必要條件,中繼站(下方方案②)是必做不是選配。**

**為什麼之前的假 Token 探針會得到相反的結論(重要教訓)**:

> **`CREDIT02025 約定信用卡不存在` 的檢查在 `CREDIT03010` IP 之前。**
> 用不存在的 `CreditHash` 探測,**永遠在 IP 那關之前就被短路**,看起來像「IP 沒擋」。
> → **假資料探針可以證明「路徑存在／欄位對」,不能證明「權限通過」。** 要驗權限必須用真憑證。
> (同理更正 A4 表:`/api/credit` + 假 Token 回 `CREDIT02025` **不代表 IP 通**。)

**完整檢查順序(2026-08-06 全部逐關撞出來)**:
`DEF01005 商店存在` → `DEF01007 Hash 比對` → `DEF01002 解密` → `DEF01006 商店狀態`
→ `CREDIT02025 約定卡存在` → **`CREDIT03010 來源 IP`** → 授權。

**實測台已自動化**(`scratchpad/first-charge-run.ts`,一行指令跑完):本機起小 server 收
`ReturnURL`(瀏覽器導轉,localhost 收得到)→ puppeteer 開沙盒付款頁 → 填測試卡
`4147631000000001` / `12/29` / `123` → 拿回 `CreditHash`。**要重測隨時可再生一個 Token。**

| 探針 | 回應 | 讀法 |
|---|---|---|
| **沙盒 `/api/credit`(真 Token,首刷拿的)** | **`CREDIT03010 不提供此IP幕後交易，223.140.249.79`** | **⛔ IP 白名單真的在擋** |
| 沙盒 `/api/credit`（假 Token） | `CREDIT02025 約定信用卡不存在` | **不能讀成「IP 通過」——在 IP 之前就短路了** |
| 沙盒 `/api/credit_bind/cancel`（假 Token） | `CANCEL03001 查無符合約定資料` | 同上,而且**取消類 API 不吃 IP 限制**(真 Token 解約成功,見 A1 下方鏈路實測) |
| 正式 `NPPA836109423` `/api/credit`（拿沙盒金鑰） | `DEF01007 Hash比對不符合` | 特店**存在**,但卡在驗簽 |
| 正式 `NPPA836109421/422/424` | `DEF01005 商店不存在` | 正式特店代號只有 `…423` |
| **正式 `NPPA836109423`（拿正式金鑰,`.env_main`)** | **`DEF01006 商店狀態不符合`**（回應可用我方金鑰驗簽+解密,`Message:"商店狀態不符合"`) | **① 正式金鑰是對的(驗簽通過) ② 正式特店尚未開通／狀態不符 → 幕後 API 全部卡在這關,比 IP 檢查更前面** |

**🚧 另一個獨立的上線阻礙:正式特店 `NPPA836109423` 目前「商店狀態不符合」**(`DEF01006`)
——不是金鑰問題(正式金鑰已驗簽通過)、不是 IP 問題(還沒走到那關)、也不是 05/07 申請書的問題
(那兩張管的是「幕後/Token 權限」,這裡是**特店本身審核中**,2026-08-06 老闆確認「目前在審核中」)。
**影響範圍比自動扣款大:正式站連一筆單次付款也會被擋在同一關。**
→ 正式站的 IP 行為要等特店開通後,重跑 `scratchpad/probe-decrypt.ts` 才驗得到(但沙盒已證會擋,
   不必再賭)。

**要做的兩件事(順序不能顛倒)**:
1. **開發／測試**:把當下浮動 IP 填進**沙盒**後台「限定 API 之 IP 設定」(半形逗號分隔、最多 10 組)
   → 續扣才測得動。**IP 一變就要重填**,這就是為什麼正式環境不能靠人工填。
2. **正式**:架中繼站取得固定出口 IP(方案②),把那**一個** IP 填進正式後台。

#### ⚠️ 「去 Amplify 後台查我們的 IP」是行不通的（2026-08-04 查證）

1. **Amplify 的 SSR 是 AWS 託管的 Lambda,對外 IP 來自 AWS 的共用位址池,會變,而且
   Console 裡沒有任何地方顯示「你的對外 IP」。** 不是我們沒找到,是它不存在一個穩定值。
2. **Amplify Hosting 的 SSR compute 不支援接 VPC**（GitHub `aws-amplify/amplify-hosting`
   #3302、#3362 至今仍是待實作的功能請求）→ 業界標準解法「Lambda 接 VPC + NAT Gateway
   取固定出口 IP」**在 Amplify 上直接用不了**。

#### 🎯 找到同架構前例:ShopStore 也在雲端,解法就是「平台級固定出口 IP」(2026-08-04)

ShopStore（台灣開店平台,幾百家商店在上面收 PAYUNi）的官方教學直接叫商家在
PAYUNi 後台填 **`35.221.226.156`** —— 我用 whois 查證,**那是 Google Cloud 的 IP**。

意思是:**ShopStore 自己就跑在雲端（GCP）,一樣沒有天生的固定 IP**。
他們的解法 = 幫整個平台配一個固定出口 IP,所有商店的幕後呼叫都從那個 IP 出去,
每個商家在自己的 PAYUNi 後台填的都是**平台那一個 IP**。

→ **這正是我們方案 A 的架構**（我們 = MiniMe 平台,各租戶各有 PAYUNi 特店,
   全部幕後呼叫走我方唯一的固定出口）。**不是治標,就是業界的治本** ——
   PAYUNi 的設計要求來源 IP,所有雲端平台的答案都是「收斂到一個固定出口」,
   沒有人找到繞過的魔法。

其他佐證:各家教學（wpsite.pro、ShopStore）教商家找 IP 都是「查網站 A 紀錄」——
**全部預設固定 IP 主機**,沒有任何一篇處理「outbound IP 會變」的情境;
QDM／1shop 這類平台商家則直接填平台提供的 IP。

#### 若 PAYUNi 堅持單一固定 IP,可行方案

**好消息:只有「幕後」那幾支需要固定 IP** —— `/api/credit`（續扣）、`/api/credit_bind/*`
（查詢/解約）。客戶刷卡的 UPP 付款頁是瀏覽器導轉,不受 IP 限制;`trade/query` 目前從
Amplify 打是通的,也沒被擋。所以要處理的只有 2~3 支呼叫。

**業界怎麼解「serverless 打需要 IP 白名單的金流」**（2026-08-04/05 查證,這是常見到有整類 SaaS 在賣的問題）:

| 方案 | 做法 | 粗估成本（待核實） | 工 | 適用性 |
|---|---|---|---|---|
| ① 請金流放寬成網段 | ~~多數金流可談~~ | 0 | 0 | ❌ **已自我否決(2026-08-05):我方根本提不出可用的網段**,見下方 |
| **② 自架小中繼站**（**主方案,2026-08-06 起確定必做**,程式端已做好） | Lightsail/EC2 最小機 + 固定 IP(Elastic IP),跑一個 HTTPS 反向代理;設 `PAYUNI_RELAY_BASE` 讓**只有續扣/解約那 2~3 支**走它 | 約 USD 3.5~6/月 | 半天（機器）+ 0（程式已完成） | ✅ 不需 VPC,Amplify 可用 |
| ③ 靜態 IP 代理 SaaS | QuotaGuard / Fixie 這類「固定 IP 出口即服務」 | 約 USD 19/月起 | 1 小時 | ✅ 最省事但比自架貴;金流流量走第三方,資安要評估 |
| ④ **Cloudflare** | 見下方專節 | Enterprise 年約 | — | ❌ **這個規模用不到** |
| ⑤ VPC + NAT Gateway | AWS 官方解 | USD 32/月起+流量 | 中 | ❌ **Amplify SSR 不支援 VPC,用不了** |
| ⑥ fck-nat 便宜 NAT 機 | 社群省錢版 NAT | 約 USD 3/月 | 中 | ❌ 同樣要 VPC,用不了 |

#### ❌ 「問 PAYUNi 放寬成網段」已自我否決 —— 我方提不出網段（2026-08-05 查證）

抓 AWS 官方位址清單 `https://ip-ranges.amazonaws.com/ip-ranges.json` 實際驗過:

- 27 種服務標籤裡**沒有 `AMPLIFY`,連 `LAMBDA` 都沒有** → Amplify SSR 的出口 IP
  是從**該區域整個 EC2 共用池**隨機拿的。
- 池子規模:東京區 = **85 個 CIDR ≈ 430 萬個 IP**;美東 = 294 個 CIDR ≈ 2,100 萬個。
- 池子**會長大**(AWS 為此提供 `AmazonIpSpaceChanged` 變更通知訂閱),且**全世界任何
  AWS 客戶都共用** —— 攻擊者租一台同區 EC2 就在「我們的網段」裡。

→ 就算 PAYUNi 願意收網段:塞不進 10 格上限、等於白名單失效、還會漂。
  **死因在我方提不出可用的網段,與 PAYUNi 無關。** 所以不要在信裡問這題。
  唯一能給出的「確定 IP」= 自架中繼站那台的靜態 IP,中繼站因此是主方案而非備案。

#### ❌ Cloudflare 為什麼解不了這題（2026-08-05 查證）

一句話:**Cloudflare 免費/付費方案只管「進來的流量」,不管「出去的 IP」;要固定出去的 IP 得買 Enterprise。**

| 想用的 Cloudflare 功能 | 實際情況 |
|---|---|
| **Dedicated Egress IP**（正是我們要的「固定出口 IP」） | **只有 Enterprise 合約客戶能加購**,官方文件明寫 “available as an add-on for any Cloudflare Zero Trust **enterprise-contracted** customer”,要價年約等級 → 為了每月幾十筆扣款不合理 |
| Cloudflare Tunnel（cloudflared） | **方向相反** —— 那是「把外面的請求安全帶進我方內網」,不是「讓我方的請求從固定 IP 出去」 |
| Workers 當跳板 | Workers 的對外 IP 也是 Cloudflare 的**共用池**,同樣會變 → 換一個共用池而已,沒解決問題 |
| Cloudflare 代管 DNS/CDN（我們已在用） | 只影響**別人打進來**的路徑,對我方主動打出去的 IP 完全沒有影響 |

來源:[Dedicated egress IPs（Cloudflare One 文件）](https://developers.cloudflare.com/cloudflare-one/traffic-policies/egress-policies/dedicated-egress-ips/)、
[Egress policies](https://developers.cloudflare.com/cloudflare-one/traffic-policies/egress-policies/)、
[Cloudflare 部落格:Gateway dedicated egress policies](https://blog.cloudflare.com/gateway-dedicated-egress-policies/)

→ 諷刺的是,**Cloudflare 賣的就是方案②那台機器的豪華版**。我們自己架一台 USD 3.5/月的
   就得到同一件事(單一固定出口 IP),差別只在他們有全球備援與 SLA。

#### ✅ 方案② 程式端已完成並實測（2026-08-05）

環境變數 **`PAYUNI_RELAY_BASE`**:留白 = 直連 PAYUNi(**現行行為一行不變**);
設了 = 幕後那幾支的網址換成中繼站,**路徑原樣保留**。
`resolveBackendUrl()`（server/utils/payuni.ts）,4 個呼叫端都接好了。

**實測**（真沙盒 `credit_bind/cancel`,同一個已簽章請求分別走兩條路）:

```
① 網址改寫  直連: https://sandbox-api.payuni.com.tw/api/credit_bind/cancel
           經中繼: http://127.0.0.1:54585/api/credit_bind/cancel
② [直連] {"ok":false,"notFound":true,"outerStatus":"CANCEL03001","message":"取消失敗，查無符合約定資料"}
  [中繼] {"ok":false,"notFound":true,"outerStatus":"CANCEL03001","message":"取消失敗，查無符合約定資料"}
  中繼站實際看到: {"method":"POST","path":"/api/credit_bind/cancel","ct":"application/x-www-form-urlencoded","bytes":444}
```

→ 兩條路徑**結果完全一致**、路徑保留、回覆通過我方金鑰驗簽。

**⚠️ 刻意不用系統層 proxy(`HTTPS_PROXY` / `NODE_USE_ENV_PROXY`)** ——
Node 22 確實支援 `NODE_USE_ENV_PROXY=1`(實測過),但它是**全域**的:會把 Gemini、LINE、
光貿發票的流量一起繞進那台小機器 → 那台機器就變成**整個系統的單點故障**。
換網址只影響幕後那幾支,其餘流量一行都不碰。也因此不必新增 `undici` 依賴。

**中繼站掛掉的後果很小:** 客人刷卡的 UPP 付款頁永遠直連 PAYUNi;續扣是排程,
今天沒扣到明天會再試(寬限期 3 天)。所以那台機器不需要高可用。

**中繼站不需要被信任:** 請求在我方就已用特店金鑰 AES-GCM 加密 + SHA256 簽章,
回覆也要用同一組金鑰驗簽才會被採信 → 一台被入侵的中繼站**簽不出「扣款成功」**。
(它仍能看到加密後的封包、能擋掉請求 —— 所以照樣只開放必要的來源與路徑。)

#### 中繼站怎麼架（半天,Caddy 版）

> 📘 **逐步操作教學(可直接照做,含驗收/回滾/常見錯誤)→ `docs/PAYUNI-RELAY-SETUP.md`。**
> 下面是摘要,細節以那份為準。

1. **開機器 + 綁固定 IP**:AWS Lightsail 最小方案(USD 3.5/月) → Networking 頁按
   **Attach static IP**(Lightsail 的靜態 IP 附掛在執行中的機器上不收費)。
   同 AWS 帳號、同區域,帳單與權責都跟現有基礎設施在一起。
2. **裝 Caddy**(自動申請並續期 Let's Encrypt 憑證,零手動)。`/etc/caddy/Caddyfile`:

```caddyfile
relay.yourdomain.com {
    # 只開放 PAYUNi 幕後那幾支,其餘一律 404 —— 別讓它變成公開的開放代理
    @payuni path /api/credit /api/credit_bind/*
    handle @payuni {
        reverse_proxy https://api.payuni.com.tw {
            header_up Host api.payuni.com.tw   # TLS SNI 與 Host 都要對,否則上游拒絕
        }
    }
    respond 404
}
```

3. **DNS**:`relay.yourdomain.com` A 記錄指向那個靜態 IP。
   建議 Cloudflare 設**灰雲(DNS only)**。
   ⚠️ **更正(2026-08-07)**:本檔原本寫「開橘雲會讓出口 IP 不是我們那台,白名單白填」——**那是錯的**。
   橘雲代理的是「**別人打進中繼站**」的方向;中繼站**打出去給 PAYUNi** 用的仍然是它自己的 IP,
   白名單不會失效。建議灰雲的真正理由是:**扣款路徑上少一個依賴、少一個故障點**
   (要用橘雲把機器藏起來也可以,只是憑證與逾時要自理)。
4. **PAYUNi 後台**:「限定 API 之 IP 設定」只填**那一個靜態 IP**(把現在為了測試塞的
   浮動 IP 全部清掉)。
5. **Amplify 環境變數**:`PAYUNI_RELAY_BASE=https://relay.yourdomain.com` → 重新部署。
6. **驗收**:超管後台跑一次續扣排程,看 `charged` 有沒有進;或在機器上
   `curl -s https://api.ipify.org` 確認它自己的出口 IP == 白名單那個。

**測試環境不用架** —— 沙盒的 IP 白名單直接填當下的浮動 IP 就能測(已經這樣測完 A1~A21)。
中繼站只為正式環境存在。若要在沙盒也走中繼,記得把 Caddyfile 的上游改成
`https://sandbox-api.payuni.com.tw`(中繼站必須與 `PAYUNI_ENV` 同環境)。

#### 申請書上的 IP 三格怎麼填

**先空著,或註記「待確認」。** 填一個當下的 IP 進去,過幾小時就失效、幕後扣款照樣被擋。
申請書另有「**變更IP位置**」欄位 → IP 是可以事後異動的,所以值得問窗口:
**能不能先送件讓審核開始跑,IP 之後再用「變更IP位置」補?** 那可能省掉好幾天。

### A4. 已經確定的事(不用再問)

| 探針 | 回應 | 結論 |
|---|---|---|
| `/api/credit` + 假 Token | `CREDIT02025 約定信用卡不存在` | 幕後授權**有權限**、簽章/加密通 —— ⚠️ **2026-08-06 更正:不代表 IP 通**(IP 檢查在這關之後,用真 Token 才會現形成 `CREDIT03010`,見 A3) |
| `/api/credit` 不帶卡 | `CREDIT02011 未有信用卡號` | 同上,只是參數錯 |
| UPP 不帶建約定欄位 | 正常開出付款頁 | Version 2.0 本身沒問題 |
| UPP `UseTokenType=2`(記憶卡號) | 正常開出付款頁 | 記憶卡號不需該權限,**但不可拿來做訂閱**(要客戶本人在付款頁完成,不是我方可主動發動的扣款) |
| UPP `UseTokenType=1/3`(約定) | ~~`UPP02087`~~ → **2026-08-06 已通**(`success: true`,頁面回吐 `UseTokenType: 3`) | **開關打開後權限就有了,見 A1** |

### A5. 沙盒測試資料 — ✅ **已取得並實測(2026-07-30)**

來源:PAYUNi 文件中心「交易測試資料說明」。**只在測試區有效。**
（成功卡與失敗卡皆已用瀏覽器自動化真刷驗證過,見本檔末「實測紀錄」。）

**信用卡測試卡號**（到期日與背面末三碼可任意填）:

| 用途 | 卡號 |
|---|---|
| **一次付清（成功）** | `4147631000000001`、`3560511000000001` |
| **一次付清（失敗）**<br>模擬 3D 交易 ECI 值不符 → 主動取消授權 | `4147631000000002`、`3560511000000002` |
| 分期付款 | `4147632000000001`（不支援 9 期）、`4741502000000001` |
| 銀聯卡 | `6200000000000001` |
| Apple / Google / Samsung Pay | 不限卡號,測試區皆模擬成功 |

> 🎯 **失敗卡號（…0002）很有用**:它是我方唯一能真實觸發「授權失敗」的方法,
> 用來驗帳單頁的 `failReason` 顯示與「重新付款」按鈕,不必靠 mock。

**其他付款方式**（我方目前只開信用卡,列著備查）:
- ATM 轉帳 / 超商代碼:登入測試區後台 → 交易動態明細 → 點「**模擬繳費**」按鈕完成付款。
- LINE Pay:申請時的 Channel ID / Secret 可填任意數字;於 LINE Pay App 綁卡後即可付款,測試區皆模擬成功。
- AFTEE:僅接受 `0909999981`（非會員）、`0909999991`（會員,密碼 `Password1234`）。

⚠️ **有卡號 ≠ 能測訂閱**。`UPP02087` 是在「開出付款頁之前」就被擋掉的權限問題,
根本走不到輸入卡號那一步。卡號能測的是**單次付款**（見下方測試計畫）。

### A6. 解除約定 — ✅ **已解決,不用再問**

2026-07-30 從官方文件「信用卡Token取消(約定/記憶卡號)(CREDIT) Ver 1.0」截圖拿到答案:
那個「綁定唯一值」的欄位名是 **`BindVal`**，放什麼由 `UseTokenType` 決定
（**約定(1) → 放 `CreditHash`**;記憶卡號(2) → 放 `CreditToken`）。

**已實作 + 沙盒實測通過**:用真實作打沙盒 + 假 Token → `CANCEL03001 取消失敗，查無符合約定資料`
= 欄位與格式全對。詳見 `PAYUNI-RECURRING-DESIGN.md` §2 與 §8.5。

→ **P5 完成,PAYUNi 這邊程式面已無任何未知。**

---

## 🔴 B. 擋住「發票真的開出去」

### B0. ✅ 光貿三件套已實測（2026-08-07,公開測試統編 12345678）

| 動作 | 結果 |
|---|---|
| B2C 開立（399 含稅,無統編 → 買方統編填十個 0） | ✅ `ZA10031054` + 隨機碼 |
| **作廢**（f0501) | ✅ 成功 ← **第一次驗證** |
| B2B 開立（799 含稅,帶統編 83610942 + 抬頭) | ✅ `ZA10031055` |
| **折讓 100 ×2 次**（g0401) | ✅ 兩次都成功,**可累加** ← **第一次驗證** |
| 折讓超額（再折 700 > 剩餘) | ✅ 光貿自己擋:`4040173 此折讓單加上其他同發票折讓單的折讓金額已大於原發票的開立金額` |

**⚠️ 踩到的時區陷阱(值得記,因為它只在半夜爆)**:折讓/作廢要帶「**原發票日期**」,
第一次用 `toISOString()`(UTC)算 → 台灣時間 00:00–08:00 之間會算成前一天 →
`4040151 原發票日期錯誤`。**正式程式用的是 `taipeiDate()`(void-invoice.post.ts:49 / allowance.post.ts:52),
是對的** —— 這條記下來是為了防止日後有人「簡化」成 `toISOString()`。

### B1. 光貿正式金鑰

- **要三個值**:`GUANGMAO_INVOICE_SELLER_UBN`(賣方統編)、`GUANGMAO_INVOICE_APP_KEY`、`GUANGMAO_INVOICE_API_URL`(正式 = `https://invoice-api.amego.tw`)。
- **哪裡拿**:公司已在用光貿 → 跟內部要現成的統編 + App Key(已決定共用同一帳號,同字軌由光貿統一配號不會撞號);要新開才找光貿客服 `lin.ee/qKyeysc`。
- **現況（2026-07-30）**:本機 `.env` 已填**光貿公開測試帳號**（統編 `12345678`）→ 本機可以完整測發票。
  ⚠️ **生產（Amplify）仍未設** → 真客戶每筆訂單的發票狀態都是 `skipped`,**等於零開票**。
  ⚠️⚠️ **公開測試帳號絕對不能上生產** —— 真客戶的發票會開到測試平台,那是稅務問題。
- ⚠️ `API_URL` 刻意沒有預設值:預設成正式站的話,沙盒測試會把測試發票開到正式平台。
- **不急於今天**:沙盒公開測試帳號可以繼續測作廢/折讓。但**只要開始對真客戶收錢,這個就必須先設好**——收了錢沒開發票是稅務問題。

### B2. 方案價含稅還是未稅(**要會計一句話**)

- 我目前**按含稅實作**:399 = 銷售額 380 + 稅額 19。
- 你的回答是「應該是含稅」→ 已與實作一致,但**請會計正式確認一次**,因為這會影響已經開出去的發票。

### B3. 全額折抵那一期不開發票,會計認不認

**用一個具體例子講**:

| 時間 | 發生什麼 | 發票 |
|---|---|---|
| 7 月 | 客戶付 799 買「入門」方案 | ✅ 我們開了一張 **799** 的發票 |
| 8 月 | 因為某個原因(例如服務中斷賠償),我們決定補他 **400 元**。老闆拍板:**不退現金,改成折抵下期** | — |
| 9 月 | 該扣 399(他降到「輕量」)。有 400 可折 → **399 全部被折光,這個月他不用付錢** | ❓ 要不要開發票? |

**我的做法:9 月不開發票。**

**為什麼**:那 400 元是「7 月那筆 799 裡面的錢」,7 月已經開過發票、也已經報過稅了。
9 月只是把那筆已收的錢拿來抵,**沒有任何新的錢進到公司**。
如果 9 月還為它開一張 399 的發票,等於**同一筆錢開了兩次發票、銷售額被報了兩次**。

**所以要問會計的就是**:「這樣不開,對嗎?」

若會計說不行,替代做法有兩條,但都更麻煩:
1. 開一張 0 元發票(不確定財政部認不認)
2. 改成真的退現金 400 給客戶 + 對 7 月那張發票開折讓(要動退款 API,而且跨月只能走折讓)

### B4. 已經確定的事(不用再問)

- ✅ **光貿 OrderId 長度不是問題**:2026-07-30 用公開測試帳號實測 17／19／20／21／24／30 碼,**全部開立成功**。20 碼那個限制是 ezPay 時代的,已不適用。目前真正的瓶頸只剩 PAYUNi `MerTradeNo` ≤25 碼。

---

## 🟠 C. 通知信（SES）— **已升級成「上線前該做」**

**為什麼升級**:05 申請書第 3 條紅字寫明「啟用信用卡幕後 = 不使用 3D 驗證 → **爭議款自負**」
（見 A2-2）。收得到收據的客戶爭議率明顯較低 —— 每月看到一封「已收款 NT$399」,
就不會在對帳單上看到陌生扣款而去打電話給銀行。**這是直接省錢的事,不是體驗加分項。**

### 程式面:✅ 全部完成（2026-08-04 覆核）

| 信 | 何時寄 | 冪等機制 |
|---|---|---|
| **付款成功收據** | 單次付款 / 訂閱首刷 / 每期續扣成功 / **折抵全額支付** | 只在「首次結算成功」時呼叫 |
| **扣款失敗提醒** | 續扣**第 1 次**失敗（之後每日重試不再寄,避免騷擾） | `chargeAttempts === 1` |
| **續扣前提醒** | 本期到期前 3 天 | `renewalReminderSentFor` 記期別 |
| **額度快用完 / 已用完** | 達 80% / 100% | `quotaEmailSentFor` 記「期別:門檻」 |

- 寄信層 `server/utils/email.ts`:**未設定就 no-op（只 log）、永不 throw** → 沒開 SES 不會壞任何流程。
- 金額一律走 `resolveRecurringCharge`（含期末降級與折抵）→ 信、帳單頁、實際扣款三者同源。
- 收據會把折抵攤成「方案月費 − 折抵 = 本次實收」;折抵蓋滿整期時標題改「**本期已續訂（由折抵支付）**」,
  不會寫成誤導的「付款成功 NT$0」。

### 你要做的四件事（順序照做）

#### ① 驗證寄件網域

AWS Console → **Amazon SES** → Verified identities → Create identity → 選 **Domain** → 填 `myfeel-tw.com`
→ AWS 給你幾筆 **CNAME**（DKIM）→ 加到網域的 DNS → 等它自動驗證通過（通常幾分鐘到數小時）。

> 這一步是在證明「這個網域真的是你的」,否則任何人都能冒用你的網域寄信。

#### ② ⚠️ 建 SSR Compute Role 並給 SES 權限 ← **最容易漏掉的一步**

**Amplify 的 SSR 執行環境預設「沒有」任何 AWS 服務權限。** 只設環境變數的話,
程式跑起來會拿不到憑證 → SES 回 `AccessDenied` → 信永遠寄不出去,而且**因為我方設計成
「寄信失敗不影響主流程」,你只會在 log 看到錯誤,金流一切正常** → 很容易以為已經在寄了。

做法（AWS 於 2025-02 才開放這功能):
1. IAM → 建 Role,**自訂信任政策**:
   ```json
   { "Version": "2012-10-17", "Statement": [{
       "Effect": "Allow",
       "Principal": { "Service": ["amplify.amazonaws.com"] },
       "Action": "sts:AssumeRole" }] }
   ```
2. 附一個只給寄信的最小權限政策:
   ```json
   { "Version": "2012-10-17", "Statement": [{
       "Effect": "Allow",
       "Action": ["ses:SendEmail"],
       "Resource": "*" }] }
   ```
3. Amplify Console → App settings → **IAM roles** → 指派這個 **SSR Compute role**。

#### ③ 申請「出 sandbox」

SES → Account dashboard → **Request production access**。
沙盒模式只准寄給「你自己已驗證過的信箱」,要寄給客戶必須離開沙盒。通常 1 個工作日內核准。

**表單怎麼填（AWS 會問三件事）**:

| 它問什麼 | 照這樣寫 |
|---|---|
| Mail type | **Transactional** |
| Use case description | 只寄交易性通知給已付費客戶:付款收據、自動扣款失敗提醒、續扣前提醒、用量額度通知。**不寄行銷信、不購買名單**,收件人一律是客戶自己在後台填寫的帳務信箱。 |
| 如何處理退信與客訴 | 我方於 SES Console 監控 Bounce / Complaint 比率;客戶信箱由本人於後台自行填寫並可隨時修改;所有信件皆為交易通知,不含訂閱式行銷內容。 |

> ⚠️ 別寫成「行銷/推廣」——那會被要求補一堆退訂機制與名單來源證明。我們確實只寄交易信。

#### ④ 填兩個環境變數（Amplify → Environment variables）

| 變數 | 值 | 說明 |
|---|---|---|
| `EMAIL_FROM` | `MiniMe <noreply@myfeel-tw.com>` | **網域必須是 ① 驗證過的那個**,否則 SES 直接拒寄 |
| `AWS_SES_REGION` | 例 `ap-northeast-1` | SES 是分區域的,**填錯區域會找不到你驗證過的網域** |
| `EMAIL_REPLY_TO`（選填） | `service@myfeel-tw.com` | 客戶按回覆時的收件信箱 |

**帳號密碼不用填** —— 憑證由 ② 的 role 提供。

#### 驗收方式

設完重新部署,然後在後台做一筆測試付款 → 應該收到「付款成功通知」。
沒收到就看 Amplify 的 log:
- `[email] 未設定 SES（EMAIL_FROM / AWS_SES_REGION）` → ④ 沒設好
- `[email] 寄信失敗：… AccessDenied` → **② 的 role 沒建或沒指派**
- `[email] 寄信失敗：… not verified` → ① 沒過,或 `EMAIL_FROM` 網域不符
- `Message rejected: Email address is not verified`（只有收件人被擋）→ ③ 還在沙盒

## ⚪ D. 我看不到、需要你確認的

### D1. Amplify 上的 PAYUNi 正式金鑰

- 我只能看到本機 `.env`(裡面是**沙盒**金鑰,**特店代號 `NPPA836109422`** —— 2026-08-06 探針更正:
  不是 `S076820628`。沙盒兩個特店都存在,但 `S076820628` 用我方金鑰簽會回 `DEF01007 Hash比對不符合`,
  只有 `NPPA836109422` 回業務錯誤 → 我方 `.env` 的金鑰配對的是 `NPPA836109422`)。
  生產環境的環境變數在 Amplify Console,我看不到。
- 要確認:`PAYUNI_MERCHANT_ID` / `PAYUNI_HASH_KEY` / `PAYUNI_HASH_IV` 是**正式特店**的、且 `PAYUNI_ENV=prod`。
- 你的回覆是「可能還沒」→ 那正式收款是**不會成功**的(會打到沙盒或報金鑰錯)。上線前務必核對。
- **✅ 2026-08-06:正式金鑰本身已驗證有效**(本機 `.env_main`,`NPPA836109423` + 32/16 碼)——
  打正式 `/api/credit` 拿回的回應能用這組金鑰**驗簽+解密成功**。剩下的問題不是金鑰,是特店狀態(見 A3)。
- ⚠️ **`.env_main` 目前是 `PAYUNI_ENV=test` 卻配正式店號+正式金鑰 = 最糟的組合**:
  請求會被送到**沙盒網址**,而沙盒不認識 `…423`(實測回 `DEF01005 商店不存在`)→ **正式收款全滅,
  且錯誤看起來像金鑰壞掉,極難查**。正式環境必須 `PAYUNI_ENV=prod`。
  `resolvePayuniEnv`(server/utils/payuni.ts)只認 `prod|production|live|core`,**認不出來的值會安靜退回沙盒**
  (只印一行 warning)→ 拼錯不會噴錯,會靜默打到沙盒。

### D2. 殘留藍新 `periodNo` — ✅ **已查,而且真的有一筆(我原本的推論是錯的)**

2026-07-30 用服務帳號對生產 Firestore(`linebot-e8dda`)做**唯讀**掃描,`workspaces` 共 5 筆:

```
6e379812-881f-4da2-b32d-f960617fda81
  planId: lite · status: active · autoRenew: true
  periodNo: "SELFPTN0001"  periodOrderNo: "NPSELFPERIOD01"  payuniCardToken: 無
```

看單號命名(`SELF…`)是**自助開通流程的手動測試資料**,不是真實的藍新委託。方案分佈:lite ×2、internal ×3。

**它會造成什麼**:`autoRenew=true` 但**沒有任何扣款憑證** → 到期時原本的邏輯會把它推進
past_due 寬限期「等一筆永遠不會來的扣款」→ 白給 3 天 + 帳單頁顯示誤導的紅色
「這期的自動扣款尚未成功」→ 3 天後照樣降級。

**✅ 已修(程式面,不需要動資料)**:`rollSubscriptionToCurrentPeriod` 的寬限期前提改成
「**autoRenew 且真的有 `payuniCardToken`**」。沒有憑證就直接降級,不進寬限期。
這同時也是通則防呆(與 payment.ts「沒拿到 Token 就不開 autoRenew」同一個道理),已加 3 個測試。

**⬜ 還可以做的資料清理(需要你同意才動生產)**:把那筆的 `periodNo` / `periodOrderNo` / `autoRenew`
清掉,讓資料不再誤導。**不做也不會壞**(程式已經不看那兩個欄位了),只是資料庫裡留著假訊息。

---

## 附:文件中心怎麼找(圖二那個側邊選單)

| 選單項目 | 我需要裡面的什麼 |
|---|---|
| **相關文件及申請書下載** | A2 的兩張申請書 |
| **交易測試資料說明** | A5 測試卡號 |
| **API › Notify › 整合式支付 (UNiPaypage)Notify** | 首刷回傳欄位是否真叫 `CreditHash`/`CreditLife`/`Card4No`（P2 最後一個「只靠文件記憶」的地方） |
| **API › 交易建立** | UPP 建約定的 `UseTokenType`/`CreditToken`/`CreditTokenType` 與版本號 |
| **錯誤代碼** | `UPP02087` 的官方說明(能佐證是不是真的要申請) |
| ~~API › 信用卡Token取消~~ | ✅ 已拿到（`BindVal`,見 A6) |
| 資料加解密 | 已對實,不用再看 |

---

## 附二:給 PAYUNi 窗口的 LINE 訊息（2026-08-04,**用印前先確認版**）

### 為什麼是「先確認再用印」

用印要調公司大小章 + 負責人親簽,**蓋完才發現填錯就得重跑一次**。
先把填好的值列給窗口核對(讓他掃一眼就好,不必開附件),確認無誤再蓋章。
順帶好處:「還沒用印、IP 還空著」是一致的狀態,不會顯得漏填。

### 先搞懂送件路徑（申請書自己揭露的）

申請書下半部是 **PAYUNi 內部簽核流程**:
`PAYUNi 送件經辦簽名` → `單位主管` → `部門主管` → `設定人員` → `驗收人員`

→ **PAYUNi 一定要有人簽「送件經辦」,案子才進得了內部流程。**
寄公用信箱 = 等不特定的人撿起來當經辦;給認識案子的窗口 = 直接指定經辦,卡住有人可以追。
**最穩:主收件人給窗口、cc `service@payuni.com.tw`。**

### 第 1 則:請對方先確認 + 問誰送（檔案直接附上,不列欄位）

```
兩份申請書填好了，測試和正式各一份，共四份，檔案附上：
・05 幕後功能API申請書
・07 信用卡Token API申請書

還沒用印，想先請您幫看一下填得對不對，
確認沒問題我們再蓋章，避免蓋完才發現要改。

（05 是因為我們測試商店打幕後扣款 API 是通的，
  但不確定正式商店有沒有開，所以一起送比較保險。）

想問一下流程：用印完是直接寄 service@payuni.com.tw 就好，
還是先給您這邊會比較順？

另外表單最下面「PAYUNi 送件經辦簽名」那一欄，
是由貴司這邊的人簽對嗎？我們就不動那一區。
```

### 第 2 則:IP 與時程

```
另外兩件事想一起確認：

1. 「幕後授權綁定IP」那三格我們目前空著——
   我們系統跑在 AWS 雲端服務上，對外 IP 是動態的、
   平台本身不提供固定 IP。

   請問可以綁「網段」嗎？
   如果一定要單一固定 IP，我們會另外架一個固定 IP 的出口配合，
   但需要幾天架設。這種情況下能先送件讓審核開始跑、
   IP 架好後再提「變更IP位置」異動補上嗎？

2. 正式商店（NPPA836109423）的特店審核目前還在進行中，
   05／07 這兩份可以並行處理嗎？
```

### 對方回什麼 = 權責在哪（自用,不用傳）

| 回答 | 代表 | 接著做 |
|---|---|---|
| 「給我就好,我幫你送」 | 他是經辦或能指定經辦 | 最佳;之後進度都問他 |
| 「我轉給業務/專員」 | 他是客服,不碰送件 | 請他介紹那位業務,以後直接對接 |
| 「寄 service@ 就好」 | 他不碰送件流程 | 照做,把他留作技術諮詢窗口 |
| 「要等特店審核過」 | 有前後順序 | **立刻追排程**,會影響上線日 |

### ⏸ 用印前先別做的事

- **IP 三格留空**,等第 2 則的答案
- **表單最下面「PAYUNi 送件經辦簽名」與四個主管欄不要動** —— 那是 PAYUNi 內部簽的

## 附三:官方 API 文件在哪（2026-07-31）

整份 `docs.payuni.com.tw` 已被爬下來（226 個 markdown）。**規格有疑問先 grep 這裡,不要猜、也不要再要截圖。**

- 位置:`~/Desktop/payuni/out/md/`（爬蟲腳本 `~/Desktop/payuni/fetch_showdoc.py`）
- 對照表（檔名編號 → 文件）:

| 檔案 | 內容 |
|---|---|
| `012_Ver 2.0.md` | **整合式支付頁 UPP**（首刷建約定的請求/回傳參數、測試卡號） |
| `021_V1.3.md` | **信用卡幕後Token交易 CREDIT**（續扣;含 TradeStatus 定義與申請要求） |
| `026_Ver 1.0.md` | 信用卡Token查詢（約定） |
| `027_Ver 1.0.md` | **信用卡Token取消（約定/記憶卡號）**（`BindVal` 出處） |
| `053_Ver1.0.md` | 續期收款幕後（另一套模型,**同樣要申請+綁 IP**） |
| `097~101` | 續期收款 建立/狀態修改/訂單內容修改/**卡號修改**/訂單查詢 |
| `074_所有API通用錯誤代碼.md` | 通用錯誤碼 |
| `075_交易建立 (整合式支付頁_UPP).md` | UPP 錯誤碼（**`UPP02087` 在第 123 行**） |
| `002_修改記錄.md` | 官方 API 異動紀錄 |

常用查法:
```bash
cd ~/Desktop/payuni/out/md
grep -rn "UPP02087" .            # 查錯誤碼
grep -rln "CreditHash" .          # 查欄位出現在哪些 API
grep -n "^| Version" "012_Ver 2.0.md"   # 查某支 API 的版本號
```

---

## 附四:給 PAYUNi 客服的詢問信（2026-08-05 修訂版,**可直接寄出**）

**寄到**:`service@payuni.com.tw`（申請書頁面指定的送件/詢問信箱）
**也可以**:把「短版」貼給窗口(王建民)先問一輪,他答不了再寄正式信。

> **修訂記錄**:初版有問「IP 可否填網段」。後來抓 AWS 官方位址清單驗證後**整題拿掉**——
> Amplify 的出口 IP 來自整個區域的 EC2 共用池(東京區 85 個 CIDR ≈ 430 萬 IP、會長大、
> 全 AWS 客戶共用),我方根本提不出一個可用的網段,問了也沒用(見 A3)。
> 中繼站因此从備案升級為**主方案**,而**問題 1(先送件、IP 後補)成為時程關鍵**。

### 為什麼把三題綁成一封

三題都是一來一往就能定案的封閉問題,且共同決定送件時程與日後開新租戶的流程成本。
寫法上刻意先講我方已完成什麼、方案是什麼,再問問題——避免拿到樣板回覆。

### 完整版（Email）

> **主旨:幕後 Token 扣款申請書送件與綁定 IP 時程請教(會員編號 P2303845057)**
>
> PAYUNi 客服團隊您好:
>
> 我們是麥菲爾股份有限公司(統編 83610942),目前正在串接「信用卡幕後 Token 交易(CREDIT)」,
> 用於訂閱制月租服務的每月自動扣款。
>
> **帳號資訊**
> - 正式:會員編號 `P2303845057` / 商店代號 `NPPA836109423`
> - 測試:會員編號 `P236588346` / 商店代號 `S076820628`
>
> **目前進度**:沙盒環境已完成 UPP 首刷建立約定(取得 `CreditHash`)、`/api/credit` 幕後扣款、
> `/api/credit_bind/cancel` 解除約定的完整測試,回應均正常。
> 幕後 API 的來源 IP,我方將以一台**固定 IP 的中繼主機**統一送出(主機建置中)。
>
> 有三個問題想請教:
>
> **問題 1|可否先送件審核,IP 之後再以「變更IP位置」補登?**
>
> 申請書上有「變更IP位置」的異動選項。我方固定 IP 主機尚在建置,想確認是否可以
> **先送件讓審核流程開始,IP 待主機建置完成後再申請變更**?若可行,審核與建置就能並行,
> 不會互相等待。
>
> **問題 2|同一個固定 IP 可否套用到會員底下所有商店?**
>
> 我方為 SaaS 平台,未來每一個客戶會各自擁有一個 PAYUNi 特店,但**全部幕後 API 呼叫
> 都會從我方同一個固定 IP 送出**。想確認:
> - 是否可用申請書的「全部商店統一設定」一次涵蓋?
> - 未來新增商店時,是否每一間都需重新送一次紙本申請書,或有較簡便的流程?
>
> **問題 3|05 與 07 申請書的送件範圍與審核時間**
>
> - 正式商店(`NPPA836109423`)是否 05「幕後功能 API」與 07「信用卡 Token API」兩張都需要送?
> - 測試環境我方已在後台「串接設定 › 限定 API 設定」啟用「信用卡 Token API」,目前可正常串接。
>   此情況下測試環境是否仍需送件?
> - 送件後一般審核作業時間約幾個工作日?
>
> 以上麻煩協助確認,謝謝。
>
> 麥菲爾股份有限公司
> 江政諭
> service@myfeel-tw.com

### 短版（LINE 給窗口）

> 建民您好,幕後 Token 扣款想再確認幾件事(正式 `NPPA836109423` / 測試 `S076820628`):
>
> 1. 幕後 API 的來源 IP 我們會用一台**固定 IP 的中繼主機**統一送出,主機還在建置 →
>    可以**先送申請書讓審核跑,IP 之後用「變更IP位置」補**嗎?這樣審核跟建置可以並行。
> 2. 我們是 SaaS,未來每個客戶各一個特店,但幕後呼叫都從我方同一個固定 IP 出去 →
>    可以用「全部商店統一設定」一次涵蓋嗎?以後新增商店還要再送紙本嗎?
> 3. 正式商店 05 和 07 **兩張都要送**嗎?測試環境我們已經在後台自己開了「信用卡Token API」
>    也能串,這樣測試還需要送件嗎?審核大概幾個工作日?

### ⚠️ 寄出前你可能想拿掉的一句

問題 3 第二點主動說了「**測試環境我方已在後台自行啟用**」。

- **留著的好處**:這是他們自己後台的開關,講清楚才問得到「那到底要不要送件」這個真正的答案;
  對金流業者透明是對的。
- **拿掉的理由**:理論上對方可能因此把沙盒的開關關掉、要我們補件才恢復。
  (機率低 —— 那是後台給商店自助的功能。)

→ 建議**留著**。但這是你的判斷,先講給你知道。

### 這封信會決定什麼

| 對方的回答 | 我方接下來 |
|---|---|
| **可以先送件、IP 後補** | 蓋章流程今天就開始跑,同時架中繼站 → 兩邊並行,省好幾天 |
| **要 IP 才收件** | 先花半天把中繼站架好拿到 IP,再送件(A3 有完整 runbook,程式端已完成) |
| **全部商店統一設定可行** | 每接一個新客戶不用再送紙本 → 這題影響的是**日後開通新租戶的流程成本**,不只上線 |
| **測試也要補件** | 照送(表已填好在 ~/Desktop/PAYUNi申請書/);沙盒現在能測就繼續測 |
