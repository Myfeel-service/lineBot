# 專案狀態總表（待辦／進行中／完成）

> **這份是唯一狀態事實來源。** 每次有實質變動就更新這份，不要只寫在對話或記憶裡。
> 最後更新：2026-08-14
> 規則與欄位定義見本檔最後一節「怎麼維護這份」。

**狀態代碼**

| 代碼 | 意思 |
|---|---|
| `TODO` | 還沒開工 |
| `DOING` | 已動工，程式在 working tree，**尚未 commit** |
| `DONE` | 已 commit 並推上 `main` |
| `DEPLOY?` | 程式已進 main，但**線上是否生效／外部設定是否做完未驗證** |
| `BLOCKED` | 卡在外部（申請、憑證、老闆拍板） |
| `DECIDE` | 等老闆拍板才動 |

---

## 🔵 進行中（DOING — 未 commit）

| 編號 | 項目 | 狀態 | 說明 |
|---|---|---|---|
| `B-27` | **`.env.example` 與程式對齊（含兩個「照著填不會有效果」的錯名）** | `DOING` | 起因是盤點本機 `.env` 缺什麼。動到 `.env.example` 一支。①**刪掉 `NEWEBPAY_*` 四個**（金流已全面走 PAYUNi；設了會讓 `/newebpay/notify` 這條還活著的舊路由重新變成可開通訂閱的入口，程式殘留由 `B-23` 清）②**`AWS_COST_*` → `COST_EXPLORER_*`**——原本寫的名字**程式從來沒讀過**，照著填完全沒效果（正確名字見 `A-9`）③SES 區域補註「Amplify 上不能叫 `AWS_SES_REGION`，線上不設、靠執行角色的 `AWS_REGION`」④光貿那段修正：測試／正式**共用同一個網址**，分辨的是統編＋APP_KEY，換金鑰要兩個一起換，並補上「允許 IP 留空」（見 `B-7`）⑤補 `PUBLIC_FAQ_TEMPLATE_SHEET_URL`⑥`LINE_CHANNEL_*` 保留但註明只剩 seed-from-env 一次性工具在用、正本在 Firestore。**雙向對過**：`.env` 有的變數本檔都提到、本檔提到的變數程式都讀得到。⚠️未 commit |
| `G-15` | **首頁改版：換敘事，不換設計語言** | `DOING` | 老闆給 artifact 參考稿要求照**架構**調一版，五個拍板點當場回覆（主 CTA 換免費註冊／幕僚模式掛即將推出／399 加「起」／示意名單照用／痛點主軸換掉）。**只換敘事**：原本一區一區介紹功能，改成「你已經有的好友名單很值錢，只是沒人經營」這條線走完。新順序＝Hero 好友名單卡（1,806 位超過 60 天沒互動，取代原本的 LINE 對話 mockup）→ 卡在哪（店主三句原話：沒時間／後台太複雜／工具又貴）→ 怎麼變簡單（設定時／設定完成後兩張面板）→ 做什麼（AI 客服／自動化行銷／客戶貼標三張面板）→ 定價（先比半個客服 20,000＋半個行銷 22,500 vs 399，再接方案卡）→ 註冊三步 → 綠色 CTA 帶（預約 Demo，降為次要出口）。⛔**設計語言維持原樣**：第一版照抄參考稿的深松綠深色段、奶油黃、大綠對話泡泡、底部黏性條，老闆說「不要被那個風格帶走」→ **已全數回退**成白底＋品牌綠、eyebrow＋標題＋說明的既有節奏、細線白卡、原本那條綠色 CTA 帶；`_landing.scss` 檔頭已寫明別再引進第二組主題色。**刻意沒照抄參考稿三件事**：①商品資訊卡留著（風控要在同一處看到商品名稱／說明／售價／付款發票，散掉會被退件）②五張方案卡留著（售價要與向金流申報的三階段一致，不能只剩 399）③參考稿最後那個聊天式註冊是假流程，不做——「免費註冊」一律接真的 `/login`。399 起、方案卡、商品資訊售價全讀 `plans.ts`，調價不用改門面。動到 `app/pages/index.vue`（整支重寫）、`_landing.scss`（重寫，共用外殼 `.lp-nav`/`.lp-btn`/`.lp-foot` 原樣保留免得連坐三個法務頁）、`SiteLegalPage.vue`＋`SiteFooter.vue`（CTA 與標語跟著換）。刪掉的 class 逐一反向 grep 過沒有別頁在用。實機驗過：typecheck 0 錯、1210 測試綠；975～1680px 六種寬度＋手機零橫向溢出、零 JS 錯誤；28 個進場區塊快捲慢捲都會出現；`/privacy` 開過確認沒被連坐。**抓到並修掉自己造的四個對齊缺陷**——並排兩張面板標題差 72px（示意圖沒鎖高）、「即將推出」膠囊讓標頭高 0.5px 使兩卡差 1px、三張功能卡標題差 45px（`min-height` 設 178 但最高那張自然高 223，只墊矮的沒用）、手機上「起」與「只是」被斷行拆開。⚠️**未 commit、未部署**；等老闆看過畫面再動。⛔已知取捨待確認：原本的「一般行銷工具 vs MiniMe」比較表與 Roadmap 三模式區塊在新架構中被移除（功能由定價對比與「即將推出」承接），要留得說一聲 |

---

## 🔴 待辦（TODO / BLOCKED / DECIDE）

### A. 上線與部署

| 編號 | 項目 | 狀態 | 說明 |
|---|---|---|---|
| `A-1` | ~~AWS Cost Explorer 成功路徑未實測~~ | `DONE` | **2026-08-11 實機驗收通過，結案**（連同 `A-9`）。老闆照教學走完全流程，成本總覽正確顯示原價 NT$254＋「原價−折抵＝實付 NT$0」。8 月費用拆解稽核＝合理（大宗是 SSR 運算時間與部署建置，重度開發期的量；全由折抵金吸收） |
| `A-2` | 自動回應合一：部署前實機驗七件事 | `TODO` | 動了送訊息與 webhook 編排，不驗不能上 |
| `A-3` | 異常中心索引部署兩租戶 | `DEPLOY?` | `knowledgeSources (workspaceId ASC, outdatedAt ASC)` **8/11 已隨 E-8 的索引一起部署兩租戶**（沒加 `--force`，4 個 field override 完好）。索引建置需要時間，建好後小幫手 `knowledgeOutdated` 探針自動走快路徑（`FAILED_PRECONDITION` 整天的噪音應消失），待驗 |
| `A-4` | myfeel LINE Webhook URL 仍填舊網域 | `TODO` | LINE Developers 後台要換成正式網址 |
| `A-5` | LIFF Endpoint URL 遷移 | `TODO` | ⛔順序：先改 LINE console endpoint → 部署 → 才跑 `resync --apply`；舊網域不可下架 |
| `A-6` | 對話 180 天清理沒掛排程 | `DECIDE` | 兩租戶都沒掛，訊息無限累積（成本問題非資料遺失）。掛上就真刪且無 TTL 可挽回 → 等老闆決定 |
| `A-7` | `aiTurns` TTL policy | `TODO` | 新子集合，要兩租戶各手動設一次。**08-12 用 fields API 驗過確實還沒設**（兩租戶現有 TTL 只有 aiFeedbackEvents／aiHandoffEvents／knowledgeSuggestions／webhookEventLocks 四個） |
| `A-8` | ~~`aiHandoffEvents` TTL policy~~ | `DONE` | **2026-08-12 查證結案：policy 早就設好了**。用 fields API 列兩租戶 TTL，`aiHandoffEvents.expireAt` 兩專案皆 `ACTIVE`——`AI-KB-TODO.md` 08-01 部署紀錄「TTL 兩專案已設」是對的，本表 08-10 寫「沒設」是誤記。docs 盤點抓到兩處說法相反才驗的 |
| `A-9` | ~~AWS 成本金鑰環境變數~~ | `DONE` | **2026-08-11 已設好並部署生效，結案**。Amplify 上是 `COST_EXPLORER_ACCESS_KEY_ID`／`COST_EXPLORER_SECRET_ACCESS_KEY`（本機 `.env` 同名）。⛔**名字不能用 AWS 開頭**：Amplify 主控台整個擋 "AWS" 保留前綴（第一版 `AWS_COST_*` 存不進去才改名 `bb4f56e`），Lambda 又會蓋掉 `AWS_ACCESS_KEY_ID` 三個保留名 |
| `A-11` | `auditLogs` 索引部署兩租戶 | `TODO` | `firestore.indexes.json` 已加 `(workspaceId ASC, createdAt DESC)`（`8e9cc88`）。Phase 2 之前要 `npx firebase-tools deploy --only firestore:indexes` ×兩租戶（⛔別加 `--force`）；Phase 0/1 只寫不讀，晚部署不擋事，可跟下次索引部署一起跑 |
| `A-10` | ~~排程推播會隨機卡死在「發送中」~~ | `DEPLOY?` | **2026-08-13 修完（`ef6fcd2`＋複審即修 `4ec6bb2`），詳見完成區**。線上未驗：部署後要看①兩筆卡死單（AROMIC預熱／MATELSAER開賣）有沒有在 10 分鐘內被看門狗自動轉成「失敗」＋小幫手亮紅 ②失敗單詳情有失敗原因與「重設為草稿再發一次」按鈕（兩筆的原因句應該都是「還沒送到 LINE 就停了——沒有任何人收到，可以放心重發」，若出現「無法確認」那句就別直接重發，先抽問一兩位客人） |

### B. 金流與發票

| 編號 | 項目 | 狀態 | 說明 |
|---|---|---|---|
| `B-1` | ~~PAYUNi「07 信用卡 Token API」申請書~~ | `DONE` | **不用送件，2026-08-06 結案**。`UPP02087` 是 PAYUNi 後台「串接設定 › 限定 API 設定」的開關沒開，不是缺申請書；開關打開後沙盒建約定 `UseTokenType=3` 直接通。證據見 `docs/GOLIVE-BLOCKERS.md` A1 |
| `B-2` | PAYUNi P3 每期續扣排程 | `DONE` | 程式 P1~P5 全完成，2026-08-07 端到端實測綠（續扣、折抵、降級、取消、解約、防重複扣款）。⛔續扣必用 `confirmRenewal`，否則一次扣款拿兩個月 |
| `B-3` | PAYUNi P4 降級／折抵、P5 換卡 | `DONE` | 同上一輪 e2e 驗過。設計見 `docs/PAYUNI-RECURRING-DESIGN.md`。⚠️ 2026-08-13 更正：原標題含「清藍新死碼」是誤記，那半件**沒有做**，已拆出 `B-23`——本行只認 P4／P5 的後端能力 |
| `B-4` | **PAYUNi 固定 IP 中繼站上正式** | `DEPLOY?` | **2026-08-14 實測：正式中繼站早就架好了，本行原本寫「正式站要架」是過期的。** 唯讀探針結果：`pr-8f3a.lineminime.com` 與 `pr-8f3a-test.lineminime.com` 都解析到 **`54.249.132.4`**（AWS 東京）；用沙盒特店打**無 `-test`** 那台回 `DEF01005 商店不存在`、正式特店打回 `DEF01006`（與直連正式站逐字相同）→ **無 `-test` = 正式站、有 `-test` = 沙盒站**，兩台都活著且轉發正確。代理路徑實測：`/api/credit`、`/api/credit_bind/cancel`、`/api/trade/query` 皆 200，`/api/upp`、`/admin`、`/` 皆 404（**不是開放代理**，把關正確）。憑證 Let's Encrypt 有效至 2026-11-05。**剩下兩件只有老闆能確認**：①`54.249.132.4` 有沒有填進 PAYUNi 後台 IP 白名單 ②Amplify 有沒有設 `PAYUNI_RELAY_BASE=https://pr-8f3a.lineminime.com`（本機 `.env` 指的是 `-test` 那台）。⚠️另：這台機器的存在與網域**在 repo 文件裡完全沒有紀錄**，`PAYUNI-RELAY-SETUP.md` 的範例名還是 `relay.lineminime.com`（該網域無 DNS 紀錄）——教學要補上實際主機名與靜態 IP，否則下次沒人知道它在哪。原始背景：`CREDIT03010 不提供此IP幕後交易` 用真 Token 撞出來＝IP 白名單真的在擋，而 Amplify 出口 IP 每天換。⛔中繼站要代理 `/api/trade/query`，否則查單退回直連（`4a1167f`，已實測有代理） |
| `B-5` | **PAYUNi 正式特店 `NPPA836109423` 審核中** | `BLOCKED` | 打正式 `/api/credit` 回 `DEF01006 商店狀態不符合`（正式金鑰已驗簽通過＝不是金鑰問題，也還沒走到 IP 那關）。**影響比自動扣款大：正式站連一筆單次付款都會被擋在這關**。**2026-08-14 用唯讀探針（`trade/query` 查一筆不存在的單，不建單不動錢）重驗：仍是 `DEF01006`，還沒過**。同一支探針打沙盒特店回 `QUERY03001 查無交易`＝特店正常時該長的樣子，兩相對照確認差別出在特店狀態而非我方請求。要再確認一次直接重跑探針即可 |
| `B-6` | PAYUNi 灰度旗標 `PAYUNI_PERIOD_ENABLED` | `TODO` | 預設關。要等中繼站＋正式特店都就緒才開 |
| `B-7` | 光貿發票正式金鑰 | `BLOCKED` | `GUANGMAO_INVOICE_*` 沒填 = 每筆訂單發票 skipped = 零開票。**2026-08-14 查證：光貿後台「API 資訊」的「允許 IP」要留空、不要填**——實打正式 API（本機未登記 IP `211.21.19.45`，故意帶錯簽章）回的是 `code 16 簽名驗證錯誤` 而非 `code 14 IP 錯誤`＝空白等於不限制。Amplify 出口 IP 每天換，填了會變成「收款成功但發票全 skipped」的靜默故障。⛔**不要**把發票流量繞進 `B-4` 的 PAYUNi 中繼站：那台無備援，扣款晚一天可補、發票沒開是稅務問題 |
| `B-8` | ~~光貿作廢／折讓沙盒實測~~ | `DONE` | **2026-08-12 沙盒重測通過，結案**：公開測試帳號開立→作廢（`ZA10035769`）、開立→折讓 NT$100（`ZA10035770`）皆 code 0——`GOLIVE-BLOCKERS.md` B0 說 08-07 已測過是對的，本表沒跟上。殘留：**作廢折讓（g0501）路徑未實測且無呼叫端**（程式 `TODO(光貿文件)` 註記還在），要用到折讓沖銷時再補 |
| `B-9` | ~~含稅／未稅口徑~~ | `DONE` | **2026-08-14 老闆拍板：含稅。與現行實作一致，零程式改動**（`shared/billing/tax.ts` 的 `priceMonthly` 就是含稅價＝實際刷卡金額＝發票 TotalAmount，未稅銷售額與稅額由它反推且保證相加相等；399 = 380 + 19）。對外文案也已標「含稅」：定價頁、升級對話框、條款、帳單頁金額欄。⚠️ 這條是老闆決定，若會計日後有異議，已開出去的發票只能走折讓沖銷重開 |
| `B-10` | 升級按比例退費／折抵 | `DECIDE` | `payment.ts` 刻意註記的待辦。建議折抵不退現金，等拍板 |
| `B-11` | 退款流程（`trade/close`） | `TODO` | 會動錢，建議連真實金流一起測。**2026-08-14 初步測試釐清了兩件事**：①**程式面根本還沒有**——`server/utils/payuni.ts` 只有 upp／trade\_query／credit／credit\_bind 四組端點，`trade/close` 沒有任何實作、也沒有呼叫端，所以「要測退款」現階段沒有東西可測 ②**串接可行性沒問題**：拿沙盒特店直接打 `sandbox-api.payuni.com.tw/api/trade/close`（帶 `CloseType`／`CloseAmt`，訂單編號是不存在的探針號）回 HTTP 200 `CLOSE02016`——是 close 專屬的業務錯誤，**不是驗簽錯誤**，代表我方的加密與檢查碼這支照樣吃、路徑存在。（`CLOSE02016` 的確切字義官方錯誤表沒查證，推測是查無此筆交易或狀態不可退，真的要做時再對表。）→ 這條的工作內容是**寫實作**，不是驗證可行性 |
| `B-12` | 收據／通知信（SES）：填寄信環境變數＋四種信開通 | `TODO` | 已寫未開。相依 `B-24`（網域驗證）與 `B-25`（離開沙盒）都完成後才輪到這步：填 `EMAIL_FROM`＋開通收據／續訂提醒／扣款失敗／降級四種信。⛔SES 區域變數不能叫 `AWS_SES_REGION`（Amplify 擋 "AWS" 前綴，同 `A-9` 的坑），程式有 `AWS_REGION` 退路。2026-08-13 依 Notion 細分拆出 `B-24`／`B-25` |

| `B-13` | 續訂前 7 天／1 天通知 | `TODO` | Email＋後台橫幅兩處。減少「怎麼又扣我錢」的客訴。相依 `B-12`（SES）。原 `TODO.md` #20 |
| `B-14` | 扣款失敗通知（含換卡引導） | `TODO` | 沒有它＝卡過期的客戶直接靜默流失，我方也不知道。相依 `B-12`、`B-19`。原 `TODO.md` #21 |
| `B-15` | 扣款失敗自動重試（dunning） | `TODO` | 寬限期 `GRACE_DAYS=3` 骨架已在 `shared/billing/period.ts`，重試就落在這幾天。原 `TODO.md` #23 |
| `B-16` | 即將降級通知（寬限期倒數） | `TODO` | 原 `TODO.md` #22 |
| `B-17` | 期中降級 | `TODO` | ⛔**必須先做 `B-10`（proration）**，否則客戶會損失已付的剩餘天數。原 `TODO.md` #26 |
| `B-18` | 年繳方案 | `DECIDE` | 8 折換現金流與留存。`plans.ts` 目前只有 `priceMonthly`。要不要做是商業決策。原 `TODO.md` #27 |
| `B-19` | 換卡必須連帶收一期全額月費 | `TODO` | **入口其實存在**（2026-08-12 查證改寫——原「後台沒有換卡入口」描述錯誤）：帳單頁「更新付款方式」＋扣款失敗警示「換一張卡付款」兩處都有，past_due 補繳／active 提前付下期的文案也分好了（`billing.vue` P5）。真正缺口＝PAYUNi 約定 Token 綁在一次真實付款上，**純換卡也得刷一期月費**；待問 PAYUNi 有無 0 元／1 元純綁卡（官方文件查無，可跟 `B-4`/`B-5` 聯絡窗口時一併問）。另：免費／客製方案刻意不顯示按鈕（按了只會 400） |
| `B-20` | 多個官方帳號能否共用同一組發票設定 | `DOING` | 盤點中。牽涉統編在組織層或工作區層的既有設計 |

| `B-21` | 全額折抵那一期不開發票，會計認不認 | `BLOCKED` | 影響折讓／作廢流程設計。與 `B-9` 同屬稅務問題，只有會計能拍板 |
| `B-22` | 確認 Amplify 上填的 PAYUNi 正式金鑰 | `TODO` | 正式站到底填了哪一組，只有老闆看得到。填錯或沒填＝上線第一筆就失敗。要確認 `PAYUNI_MERCHANT_ID`／`HASH_KEY`／`HASH_IV` 是正式特店的且 `PAYUNI_ENV=prod`。⚠️ 金鑰本身 2026-08-06 已驗證有效，缺的是「正式站填的是不是這組」 |
| `B-23` | 清掉藍新（newebpay）舊實作 | `TODO` | 2026-08-13 從 `B-3` 拆出（原誤記為已完成）。實況：`server/utils/newebpay.ts`＋`.test.ts` 還在，而且**不是死碼**——`server/routes/newebpay/notify.post.ts` 與 `return.ts` 是活著的路由，`server/middleware/payment-reconcile-tick.ts:38` 還在判 `newebpayMerchantId`。⚠️ 意思是藍新的幕後 Notify 端點目前仍可被打進來開通，金鑰沒設時只會驗簽失敗擋掉，但這條路存在就是風險。動它之前先確認沒有任何舊客戶還走藍新 |
| `B-24` | SES 寄信網域驗證（DNS CNAME） | `TODO` | 2026-08-13 依 Notion 細分從 `B-12` 拆出。要在網域 DNS 加 SES 給的 CNAME 記錄完成 DKIM 驗證。⚠️ 卡點在「誰有 DNS 權限」，不是工程。`B-25` 相依本項 |
| `B-26` | 沒填發票金鑰期間收到的訂單，事後不會自動補開 | `TODO` | 2026-08-14 查證缺口：`invoice.ts:52` 沒金鑰時只把訂單標 `invoiceStatus: 'skipped'`、**不寫 `invoices` 文件**，而每晚補開的 `reissueFailedInvoices` 只掃 `invoices` 裡 `ok == false` 的 → skipped 那批永遠掃不到，要人工補。**最省事的解法是排順序：`B-7` 金鑰必須在第一筆真實收款之前填好**（現在 `B-5` 正式特店還在審，正式站收不到錢，還來得及）。若真的來不及，才需要補一支「把 skipped 訂單撈回來開」的補救路徑 |
| `B-25` | SES 離開沙盒（申請正式寄信額度） | `TODO` | 2026-08-13 依 Notion 細分從 `B-12` 拆出。沙盒只能寄給已驗證的信箱＝真客人一封都收不到。⚠️ 要向 AWS 申請且**有審核前置時間**（非當日），相依 `B-24` 先完成。應該早點送，不要等到要寄信才發現在排隊 |

### C. AI 與腳本

| 編號 | 項目 | 狀態 | 說明 |
|---|---|---|---|
| `C-1` | 腳本 13 模塊 ③⑨ | `TODO` | A+B+C 階段其餘全完成 |
| `C-2` | 「查詢訂單」改用腳本收四格再轉真人 | `TODO` | 老闆選了「先關規則，之後再做」 |
| `C-3` | AI 正式上線前 6 步 | `TODO` | ①停 anyText 規則 ②設 handoffNotify 收件人+SLA ③設 serviceHours 勿擾 ④設 shopUrl ⑤replyMode 先 draft 跑 1-2 週 ⑥confidence 上線初期回 0.75 |
| `C-4` | AI history 沒時間上限 | `TODO` | 會撈到 2.5 個月前舊訊息（已報未修） |
| `C-5` | AI 表現頁「先問清楚」無下鑽 | `TODO` | 偏高警示行說「去把卡片標題改清楚」但沒有路能看到是哪幾張卡在打架；要嘛列出反問最多的主題，要嘛連去 playground 重演 |
| `C-6` | AI 表現頁：補卡成效回饋 | `TODO` | 使用者在乎排行第 4 名的缺口——「上週補的 5 張卡，這週救回 12 題」。讓店家看得到補知識有用，才會一直餵。建議收件匣已有試答驗證的資料基礎可接 |
| `C-7` | 超管成本頁：呼叫/答出比例異常警示 | `TODO` | 計費收在「答出」不收在呼叫（2026-08-10 拍板，完整決策與虧錢分析見 `docs/BILLING-UNIT-POLICY-20260810.md`），防極端「呼叫很多答出很少」的高成本帳號不靠改收費、靠內部警示（例：invocations/answered > 5 標黃）。資料現成（costs.get.ts 已回兩個數） |
| `C-8` | 呼叫硬上限要不要做 | `DECIDE` | 額度只數「答出」→ 答不出來的呼叫**永遠燒不完額度**、成本理論上無上限（試算：月呼叫 2 萬次×答出率 5% ≈ NT$680 成本 > 輕量月費；免費層尤其）。選項＝月呼叫超過額度 N 倍（如 10×）自動轉真人。見 `docs/BILLING-UNIT-POLICY-20260810.md` §4 |
| `C-9` | `resync-preview` 504 風險 | `TODO` | 同吃 `chunkTextWithLlm`，可套 job 機制 |
| `C-10` | `upload.post.ts` 6MB 雷 | `TODO` | 同 Lambda payload 上限，未改直傳 signed URL |
| `C-11` | 產品名注入：splash 租戶 | `TODO` | 若要做，需同樣種 productName + reindex |
| `C-12` | 🔴 知識庫重傳同一份檔會重跑重收費 | `TODO` | 手動上傳**沒有內容去重**（`contentHash` 只有 url／Google Sheet 自動同步在用），重傳沒改過的大檔＝OCR＋切卡＋embedding 整套重跑重收，且不受則數／token 額度擋。最省力修法＝把現成的 contentHash 比對接到手動上傳，未變更就跳過 |

| `C-13` | 語意自動貼標（沒有關鍵字也能判客訴） | `TODO` | 現在只能靠關鍵字，客人沒講到那幾個詞就抓不到，**真正在生氣的客訴往往漏接**。差異化賣點 |
| `C-14` | 腳本節點：串接外部 API | `TODO` | 獨立大工程，底座已備好 |
| `C-15` | 腳本節點：標籤觸發 | `DECIDE` | 2026-08 決定暫時不執行，可視需求重啟 |
| `C-19` | ~~兩份 Google 試算表分享給服務帳號~~ | `DONE` | **2026-08-12 查證結案：早就分享好了**。直查 Firestore：四個 gsheet 來源（Test＋正式 `p_` 各兩份）全部 `status=ready`、無 syncError——「failed」是 7/31 稽核當下的舊況，`AI-KB-TODO` 沒更新。無程式改動 |
| `C-20` | 過期行銷卡逐張停用或設有效期限 | `TODO` | 募資倒數／折扣碼 NEXT300／感恩回饋價／5 月出貨進度等，後台圈選（啟用開關＋有效期限功能已上線）。與 `C-17` 互補：prompt 治本管新卡，舊髒卡要人圈或走重新同步重切。原 `AI-KB-TODO` A3 |
| `C-21` | 知識庫補缺的內容 | `TODO` | 價格查詢方式、除濕機水箱容量／噪音分貝等規格數字、森呼吸 NEXT（清淨機）產品卡、貨到付款、實體店面、國際運送。建議收件匣上線後可照榜補。店家方動作。原 `AI-KB-TODO` 批次 1 |
| `C-22` | PDF 表格改走 Gemini 原生文件理解 | `DECIDE` | unpdf 純文字抽取會把規格表拆爛（規格題／比較題答不好的上游原因）。7/31 評估「先看比較題輕量修法夠不夠再定」→ 相依 `C-23` 的重驗結果。原 `AI-KB-TODO` G2 |
| `C-23` | AI 檢索修復的回歸驗證一直沒跑 | `TODO` | 08-01 修完比較題／一句兩問時說好「部署後用 7/31 的 4 道比較測題重驗」＋新匯入流程實抓一份說明書＋一個網址走一遍＋反問選項 label 品質實測——至今沒人跑過，`C-22` 等它的結果。原 `AI-KB-TODO` |
| `C-24` | ~~🔴 AI 生成腳本會捏造事實（營業時間/價格/折扣碼）~~ | `DONE` | **2026-08-12 修完**：prompt 硬規則「沒給的事實一律【請填入：…】占位、描述有給的照用」＋編輯器 flowWarnings 對占位符標黃（`findPlaceholderTexts`，流程圖黃點同步亮）。16 案重測：營業時間/價目表都出占位符、使用者給的 499 照用 |
| `C-25` | ~~AI 生成的觸發詞會劫持正常訊息~~ | `DONE` | **2026-08-12 修完**：prompt 教（≥2 字、禁高頻詞）＋確定性後檢剔除（單字/黑名單/含租戶敏感詞的死關鍵字；端點載 `sensitiveTopics` 傳入）。剔光也安全——語意路由吃範例句不吃 keywords。重測：「退款」被剔留「退錢」、「你好/在嗎/哈囉」整批剔除 |
| `C-26` | ~~AI 生成不會拒絕無關/做不到的描述~~ | `DONE` | **2026-08-12 修完**：prompt 拒答出口 `{"error":"原因＋建議"}` → 422 原樣給使用者；建議**只准指向真的存在的功能**（圖文選單/歡迎訊息/勿擾時段/知識庫/收資料轉真人——第一版模型會推薦已下架的「關鍵字自動回覆」，實測抓到才加這條）。查詢類（先收資料再轉真人）明講可做不准拒。重測：天氣/寫詩/抽獎/貼標/時間條件全部拒答且理由可用 |
| `C-27` | 腳本範本：補「訂單查詢」第五張？ | `DECIDE` | ①②③ **2026-08-12 修完**（出貨查詢改收編號+轉真人並去掉「我的訂單」租戶假設；「人工客服」搬到找真人範本；刪「要退」誤觸詞）。剩④：要不要補第五張範本，牽涉範本牆 2×2 版面要不要一起調，等拍板 |
| `C-31` | 小幫手對話式引導常態化＋語意化操作 | `TODO` | 評估 `bb437ff`（報告 `docs/ASSISTANT-SEMANTIC-OPS-EVAL-20260813.md`＋老闆版 artifact）。**2026-08-14 老闆三題全拍板**：①分期照 0→1→2 ②🔴高風險紅線（群發/對客人說話/刪除/憑證/成員/錢，最後一顆按鈕永遠留人）＝**永久原則，工程不自行放寬** ③Phase 2 中風險首批含 AI 設定。**Phase 0 地基（`8e9cc88`）與 Phase 1 引導常態化（`44ca5f0`）都已做完，見完成區**；⚠️Phase 1 的三條劇本與開通回歸**未實機走**（要一個掛著對應異常的帳號實測）。下一步＝Phase 2 代辦 MVP：操作模組註冊表＋pending-op 確認流＋低風險首批＋`scripts/preview-impact`，估 5–8 天 |
| `C-33` | ~~所屬產品打錯字／變體寫法的防呆~~ | `DONE` | **2026-08-14 老闆拍板「全做」，①〜④ 當日做完（`807d759`）**：①④欄位打字當下就地攔（新名字講「存檔後會新增一個產品」；跟現成名幾乎一樣問「是同一台嗎」二選一；只差空白符號建議統一寫法；匯入預覽與資料頁同元件同步吃到；⛔已合併掉的舊叫法比對 `known` 不比收斂清單，否則對處理過的名字喊「新增」是假話）②整理視窗補「自己挑兩個合併」手動入口＋確認框明講型號誤併風險③偵測補訊號 3「字幾乎一樣＝可能打錯字」（⛔數字不同＝12L vs 16L 視為不同型號刻意不列；包含關係留給訊號 2 才有型號風險標）。判定收單一來源 `shared/product-name-similarity`（欄位端與偵測端同一把尺）。typecheck 0 錯、1210 測試綠（新增 18） |
| `C-32` | 知識庫匯入視窗再簡化 | `TODO` | ①〜⑦ **2026-08-13 做完（`494f5ee`，老闆拍板照建議順序全做）**：預覽頁不教人勾「看不到的勾選框」＋條數只講一次／FAQ 範本指示照實際路徑講／「多樣商品」整塊偵測到內容才出現＋官網警告只在貼網址時顯示＋問句改敘述句／禁用主按鈕講原因／標題統一「加入知識」／勾了總表沒做出來補交代／導覽第 3 步改置中卡。**剩⑧：檔案不收 CSV**——購物平台匯出商品清單常是 .csv，拖進來被「只支援 PDF 與 Excel」擋掉，使用者得自己會轉檔；需後端解析配合 |

### D. 等老闆拍板

| 編號 | 項目 | 說明 |
|---|---|---|
| `D-1` | ~~草稿模式要不要扣則~~ | **2026-08-10 老闆拍板：收，結案**。草稿是送給客服的成品（收件匣建議回覆），價值有交付、只是對象是店家不是客人——計費行為不動，修的是頁面定義文字（「1 則＝AI 產出一則回答：自動發送或草稿擬稿」）。反問／playground 維持不收：收在價值不收在成本（整月全 AI 成本 NT$15 vs 方案月費，回收的是信任不是錢）。保險絲另列：呼叫/答出比例異常的內部警示 |
| `D-2` | ~~「自己搞定」要不要含「問完答成功」~~ | **2026-08-10 被「場」制取代，結案**：主指標改成場級「全程搞定率」後，反問後答成的場自然算搞定（那場沒轉真人），題級低估問題不再影響畫面。`followupAnswered` 計數器保留照記，若日後要做題級診斷還用得到 |
| `D-3` | 「系統」發送者標籤要不要留 | 老闆只講真人/AI/機器人三類，系統是我加的 |
| `D-4` | 貼圖算不算「待處理」 | |
| `D-5` | 昨日摘要「沒人回 N」可否點進名單、要不要補 SLA 那條 | |
| `D-6` | 超量加購：要做成可收錢，還是從方案表拿掉 | `plans.ts` 每個付費方案都寫 `overagePerReply: 0.8`、前端也顯示「超量加購 NT$0.8/則」，但實際行為是**則數用完一律轉真人**——既不讓用也不收錢。**張開了兌現不了的支票，而且撞頂的正是最賺錢的客戶**。原 `TODO.md` #24 |
| `D-7` | `owner` 角色跟 `admin` 權限完全一樣 | `shared/permissions.ts` 的 `CAPABILITIES` 沒有一項要求 owner，它只是等級 4。兩條路選一：給它專屬權限（刪除 OA、轉移擁有權），或承認它只是個標籤。原 `TODO.md` #30 |

| `D-8` | 小幫手異常提醒：三項待拍板 | 呈現方式與嚴重度分級未定，工程不自行決定 |
| `D-10` | 熱機池大小 vs 冷啟動速度（每月約 NT$450 的取捨） | 7/31 暖機改版（`fc591f4`）把保溫從每次 1 個請求變 4 個（母＋3 子實例）＋暖 2 條頁面渲染，換來「第一次點特別久」從 2.3s 降到 0.14s、~1/4 請求不再慢 0.3~1.4s。代價＝主機基準費用從 ~NT$200/月 → ~NT$650/月（2026-08-11 逐日拆帳查證）。目前全由 AWS 折抵金吸收＝實付 0，**折抵金用完前不急**。要省的話旋鈕現成：`WARM_CHILDREN` 3→1 約省一半、或離峰時段降頻。工程建議：留著，客人體感值這個錢 |
| `D-9` | 方案目錄要寫「每月則數」還是「每期則數」 | `I-1` 收尾時留下的唯一判斷題。用量計數器全部統一成「本期」（錨定日），但升級對話框與落地頁的方案表寫「每月則數 1,000 則」「1,000 則/月」——**兩邊都不算錯**（付費方案就是月繳、一期恰好一個月），可是同一個流程裡按「升級方案」會先看到「每月」再看到「本期」。選項：①方案表照樣講「每月」（賣點好懂），只在額度欄補一顆定義 ⓘ ②全面改「每期」，跟計數器一致但賣相變差。工程傾向①，但這是文案／賣相決策 |
| `D-12` | 卡死的兩則推播要不要補發 | 兩則都停在「發送中」但 **LINE 一則都沒送出**（受眾快照、deliveries、LINE 統計全空）＝客人什麼都沒收到，補發**不會**重複轟炸：①「AROMIC預熱」8/12 21:00 檔，標籤「問卷 - AROMIC睡眠香氛機」206 人——預熱有檔期時效，要快決定 ②「MATELSAER開賣」6/18 檔，標籤「問卷 - MateRaser」164 人——兩個月前的舊案，大概率直接作廢。**`A-10` 修好部署後不用進資料庫**：看門狗會自動把兩筆轉成「失敗」，後台失敗單就有「重設為草稿再發一次」按鈕（老闆可自行操作，重設不發訊息、要再走一次驗證並發送）；作廢就放著不管或留失敗狀態 |
| `D-11` | 回覆字數上限 220→300＋截斷提示 | 查證過**不用工程**：程式預設本來就是 300，MYFEEL 的 220 是 Firestore 直接寫入值（後台滑桿 step=50 產不出 220）→ 後台滑桿拉到 300 即可。另查明截斷會**無聲丟掉列表尾端項目、不留省略提示**（「有賣什麼」清單被截尾），要不要加省略提示＝拍板。原 `AI-KB-TODO` A4，08-12 盤點撈回 |
| `D-13` | ~~首頁改版的三個取捨~~（報告 `docs/LANDING-UX-EVAL-20260814.md`） | **2026-08-14 老闆逐項拍板，結案**：①商品資訊卡搬到頁尾上方 → **不做**（維持在定價區內，風控前科不冒險）②補社會證明 → **不做**（沒有可具名的試用店家素材；⛔日後要補請給真材料，全示意數字的頁面加假見證會從「示意」變「造假」）③比較表與 Roadmap → **比較表直接做一個**（已完成，見 `G-16`⑪，區塊 id `#diff`；Roadmap 三模式不另外加回來，「即將推出」那張卡已承接）。同時否決評估中的「砍註冊三步」與「刪痛點總結條」——那兩區留著。**2026-08-14 追加拍板**：④「預約 Demo」整區移除（已有免費試用，表單失去唯一必要用途＝企業報價；想找人談改走頁尾客服電話／信箱＋定價區下方那行來信洽詢）⑤方案卡只主打 399（⛔商品資訊仍須列全部三階段，見 `G-16`⑧） |
| `D-14` | ~~「售價統一 399」與系統實況不符~~ | **2026-08-14 老闆拍板「先隱藏就好，目前只 show 399」，本列結案**，做法＝**只動露出、不動計費**。已完成：①首頁方案卡只留免費＋399（老闆同時拍板免費卡**留著**）②行銷文案「另有 NT$799／NT$1,499 方案與企業報價」整行移除③商品資訊售價改吃 `featuredPlans`，顯示「月租 NT$399（新臺幣含稅價…）」，「三階段」字樣拿掉④**修掉一個既有漏洞**：`AdminPlanUpgradeDialog` 原本只濾 `internal`、沒濾 `landingHidden`，導致**專業版 4,990 一直對客戶露出而且真的能按下去結帳**——那正是當初標 `landingHidden`（未向 PAYUNi 申報）要擋的價格，等於從後台這個介面漏出去。已補上過濾（要恢復販售請先完成申報再拿掉旗標）。實測三種寬度：頁面已無 799／1,499／4,990 字樣。⚠️**殘留的不一致（刻意保留，非漏做）**：`starter`(799)／`growth`(1,499) 在 `plans.ts` 仍是可自助結帳的方案，也就是「系統收得到、官網沒寫」。風控紅線是「門面出現未申報的價格」，列得比申報少不踩線，所以現況安全；但若日後要**真正停售**，得改 `plans.ts` 標停售＋處理既有 799／1,499 訂閱戶＋確認是否向 PAYUNi 重新申報——那時再開新項 |

### E. 技術債

| 編號 | 項目 | 說明 |
|---|---|---|
| `E-1` | ESLint 未導入 | 刻意延後（初次導入會產生大量風格 noise），typecheck 已進 CI |
| `E-2` | 巨型檔案待拆 | `server/utils/handler.ts`(**3940 行**，2026-08-10 實測；8/4 時 3256 行，兩週長 ~700 行)、`flow.vue`、`AdminPanel.vue` — 應獨立成無行為變更的重構 PR |
| `E-3` | 重複實作收斂 | `tsToMs` 全 repo 9 份、worker-pool 3 份、`deliverOrDraft` 統一出口 |

| `E-4` | README 仍是 Nuxt 預設樣板 | 新人接手沒有入口文件。`memory/project_script_13modules.md` 寫得很好，可當技術文件起點 |
| `E-6` | ~~`.ar-link` 全 repo 沒有定義~~ | **2026-08-13 結案**（`f6a9645`）：`.ar-link` 已與 `.link` 一起補進 `app/assets/scss/admin/_shared.scss:498`——是自動回覆下架時連坐刪掉共用樣式，還原那批時一併補回。本行原留在技術債區與完成區說法相反，2026-08-13 同步時查證後結案 |
| `E-5` | `OrgMemberDoc` 型別與實作對不上 | `shared/types/organization.ts:58-72` 宣告 doc ID 是 `${uid}_${orgId}` 且 `uid` 必填，與實際寫法不符 → 隱性 bug 來源。原 `TODO.md` #29 |
| `E-10` | 殭屍 open session 治本：查「活動／加好友為什麼幫沒開口的客人開場」 | E-8 治標（查詢下推）已擋住讀取費，但 myfeel 有 **1,584 筆** `status='open'` 且客人從未開口的場**永不關閉、逐月增生**（count 聚合成本與資料量跟著漲）。要嘛來源不開場、要嘛排程把超過 N 天沒開口的 pre-inbound 場自動關掉。動手前先確認統計口徑（`isPreInboundFollowSession` 一帶）不被影響 |
| `E-11` | ~~`UIUX-TODO.md` 未封存、未完成項不在本表~~ | **2026-08-12 結案**（`8c4785b`）：逐條重驗後封存——「Landing 假 Demo 表單」其實 7/20 自助導購批已修（真 POST `/api/leads`＋honeypot，該檔比修復早一天定稿沒記到）、`.btn-danger`／無障礙也早解；還活著的三塊拿到編號 `E-13`／`E-14`／`E-15`（老闆 08-12 拍板：全部記錄待辦） |
| `E-13` | 後台手機版 RWD | 全站沒有手機版：`.layout-wrapper` 固定 240px 側欄＋`overflow:hidden`、無漢堡無抽屜，41 個 SCSS 只有 10 個有手機規則；flow/richmenu 拖曳只綁 mouse 事件（`useAreaEditor.ts:272`），觸控完全不能用也沒有「請用電腦」提示。**客服在外面用手機回不了訊息是最痛場景**。範圍未拍板（關鍵頁 vs 全站 vs 不做），工程建議先做對話頁＋工作台（側欄改抽屜）。原 `UIUX-TODO` R4，08-12 老闆拍板記錄待辦 |
| `E-14` | UIUX 打磨批次（編輯器易用性＋樣式技術債） | 一個編號收整批，動到該區時順手清：flow 存檔錯誤欄位級定位（輪播「第 M 欄」／自動捲到出錯卡／`AdminAreaActionEditor.errorMessage` 死碼）、輪播卡固定 380px 雙軸捲動（`_flow.scss:223`）、richmenu 超界要按兩次送出（改即時 clamp）、按鈕裝飾 emoji（➕🗑️📥）、統計卡三套重複實作（el-card/.bc-stat-box/.cmp-stat-box）、`AiContextBanner` 樣式寫在元件內、`_auto-reply.scss` 舊 layout 類死碼（自動回覆 8/9 下架後前提已變，可整批確認刪除）、`areaColors` 色盤兩份、匯入對話框（虛線框沒綁 drop／長輪詢無取消——部分可能已被 08-01 批次解掉，動工前重驗）、landing select 無空白預設。原 `UIUX-TODO` 打磨項，08-12 拍板記錄待辦 |
| `E-15` | 門面手機版找不到「登入」 | 漢堡選單有了（產品／差異／定價／常見問題）但「登入」鈕在手機寬度被 `display:none` 藏掉（`_landing.scss:393`）又沒補進選單＝手機訪客完全找不到入口。修法＝選單裡加一條「登入」連結，小改動。08-12 重驗仍在，老闆拍板先列待辦 |
| `E-16` | ~~圖文選單「設為預設」只帶 LINE 側 ID 時不驗歸屬~~ | **2026-08-14 修完結案（`8e9cc88`）**：`setDefault` 一律反查歸屬才准（有 `firestoreId` 驗文件、只有 `richMenuId` 就用它反查同工作區的選單文件，查不到 404），順帶修掉只帶 LINE 側 ID 時 `isDefault` 旗標不寫的資料缺口。線上生效與否同批 `DEPLOY?` |
| `E-17` | ~~小幫手 `get_ai_usage` 把 token 細目回給 viewer~~ | **2026-08-14 修完結案（`8e9cc88`）**：工具回傳拿掉 inputTokens/outputTokens/buildEmbeddingTokens（`F-5`：token 只給超管，聊天不能繞過），附測試釘住（假資料塞 99999 驗證不進 prompt）。線上生效與否同批 `DEPLOY?` |
| `E-12` | docs 校期批次（08-12 盤點餘項） | ①`GOLIVE-BLOCKERS.md`：A2 申請書整段／附二用印信／附四客服信與 `B-1` 結案衝突要標作廢、A3 的 Caddyfile 摘要缺 `/api/trade/query` 要指向 `PAYUNI-RELAY-SETUP.md`、SES 教學要人填 `AWS_SES_REGION` 但 Amplify 擋 "AWS" 前綴存不進去（程式有 `AWS_REGION` 退路、教學要改）——⛔**老闆要先上傳這份文件，動它之前先問**（2026-08-12 交代） ②`PAYUNI-RECURRING-DESIGN.md` §10/§12「credit 這支沒被 IP 擋」已被 08-06 推翻（假 Token 在 IP 檢查前短路），是唯一沒補更正註的文件 ③`STATS-SIMPLIFICATION-20260807.md`（檔頭還寫「回填狀態」）與 `AI-PERFORMANCE-PAGE-REDESIGN-20260810.md` 補凍結聲明 ④`.env.example:86-87` 還是舊名 `AWS_COST_*`，照抄會讀不到憑證（`bb4f56e` 唯一漏網） ⑤`CONVERSATION-STATS-DEFINITIONS.md` 檔頭日期停在 08-06、`bot_flow` 說明還講已下架的「自動回覆」 |

### F. 已知限制（不打算修，但別忘了）

| 編號 | 項目 | 說明 |
|---|---|---|
| `F-1` | 改桶只影響未來資料 | 小幫手／生成腳本的 token 改記「後台自用」後，**八月以前的舊資料仍留在「回答客人」桶**，且月結桶是加總值、拆不出來，無法回填 → 跨月比較時九月起口徑會略有不同 |
| `F-2` | 成本無法分攤到各官方帳號 | 資料庫（Google）與主機（AWS）都不依 workspace 分記用量，只有 AI 能逐帳號拆。頁面已寫明 |
| `F-3` | 跨雲流量是唯一估算項 | 資料庫在 Google、主機在 AWS，每次讀取都是對外流量但**沒有任何用量指標可查**。用「讀取次數 × 每筆 1 KB × US$0.12/GiB」估，已對帳單校準（誤差 5% 內）。常數 `ASSUMED_BYTES_PER_READ`，調高＝更保守 |
| `F-4` | 估算單價無條件進位 | 「一次抓 NT$0.04」是實測平均進位到分位。⚠️若哪天實測平均掉到 NT$0.005 以下，進位到分位會變成高估兩倍，那時要改進位到千分位 |
| `F-5` | 金額一律只給 super admin | 五支會回金額的端點全部有守衛；`ai/usage/summary` 對非超管**連 key 都不回**。理由：計費賣「則數」，token 是平台進貨價，租戶拿到金額就能反推毛利 |
| `F-6` | 金額只在「成本總覽」一頁 | 2026-08-10 拍板：用量監控頁不放任何金額與 token（那頁只講則數與品質）。API 仍回成本欄位給超管，因為 `ai-settings` 的 token 數還在用。⛔新增金額顯示前先想清楚要不要開第二個地方 |
| `F-7` | 「30 天內被問 N 次」會低估 | 事件掃描上限 400 是**先撈再篩 reason**，事件多的帳號會低估；已改誠實標示「至少 N 次」。治本要加 `(workspaceId, reason, createdAt)` 索引，08-04 評估仍不做（AI 未接真客人、低估不會發生，且現在動索引最安全）。**重啟條件＝AI 上線時程確定**。原 `AI-KB-TODO`，08-12 盤點撈回 |

### G. 組織、權限與成長

| 編號 | 項目 | 狀態 | 說明 |
|---|---|---|---|
| `G-1` | ~~找出沒掛方案的官方帳號並補上~~ | `DONE` | **2026-08-14 體檢完成，正式租戶零缺口**。直查兩個專案的 `workspaces`：myfeel（`linebot-e8dda`）5 個帳號**全部有方案**（MYFEEL／鼴究室／Myfeel Test = `internal`，Kevin小店／小胖商店 = `lite`，狀態皆 `active`）；splash（`linebot-b137e`）只有 1 個 `Splash Test` 沒掛，那是測試租戶的測試帳號，影響為零。**順帶查清楚實際行為**（本行原本寫的「從吃到飽變成被攔在 200 則」語意不夠精確）：`billing.ts:129` 的 `raw ?? defaultFreeSubscription()` 會把「沒有訂閱欄位」的帳號**在記憶體裡當成免費方案**（200 則、不寫回資料庫），所以既不是吃到飽也不是完全不能用，就是免費層；只有**整個工作區文件讀不到**（Firestore 故障）才會回 `quota: null` 而不擋則數。要重跑體檢：用 `firebase-admin` 掃 `workspaces` 看 `subscription.planId` 有沒有值即可。原 `TODO.md` #7 |
| `G-2` | `plan.seats` 席次上限沒有真的生效 | `TODO` | 方案表寫了席次數字，但邀請成員的端點從來不檢查，客戶可無限邀請。**開放自助註冊前必須先補**。原 `TODO.md` #16 |
| `G-3` | 成員邀請信 | `TODO` | 現在完全沒有——被邀請的人不會收到任何通知，等於隱形白名單。相依 `B-12`（SES）。原 `TODO.md` #19 |
| `G-4` | 自助註冊流程 | `TODO` | 首次登入自動建組織＋一個免費 OA → 落地在開通引導 → 用完 200 則才出現方案頁。⛔前置：`G-2`。原 `TODO.md` #17 |
| `G-5` | 自助試用流程 | `TODO` | `trialing` 狀態早就存在，但沒有任何流程用到它。原 `TODO.md` #28 |
| `G-6` | 客戶屬性逐人檢視頁 | `TODO` | 腳本能把客人資料寫進客戶屬性，但後台沒有逐人檢視／依屬性篩選匯出的頁面。**資料進得去、人工看不到＝等於白收** |
| `G-13` | G-11「落對話頁」要不要擴大到所有新帳號入口 | `DECIDE` | 08-12 拍板只改了開通精靈的出口；帳號選擇 `enter()`、`/admin/{wid}` 預設轉址、TutorialAgent 等約 8 處入口仍落全零統計頁——新帳號從那些門進來還是接冷場。要不要把拍板範圍從「開通出口」擴大到「新帳號所有入口」等老闆決定（08-12 複審發現） |
| `G-14` | ~~精靈頁離開後劇本鏈不會中止~~ | `DONE` | **2026-08-14 修完結案（`44ca5f0`）**：劇本引擎抽成共用 runner 時把取消做成一等公民——dispose 後任何劇本原語直接丟取消例外，async 鏈就地停下（有測試釘住），不再跑到下一個提問才停 |
| `G-16` | **首頁 UI/UX 收斂＋只主打一個方案** | `DOING` | 出自 `docs/LANDING-UX-EVAL-20260814.md`；老闆逐項拍板（不做：砍註冊三步、刪痛點總結條、商品資訊搬家、補社會證明）。**已完成：**①`#value` 三張功能卡補「示意」標示（卡上有 142 位／NT$1,280／23:41，Hero 與成本對比都標了只有這區漏，是 `G-15` 改回原風格時連 figcap 一起弄丟的）②CTA 用詞統一「免費註冊」③**社群分享預覽圖**：新增 `scripts/build-og-image.mjs`＋`npm run build:og` 產 `public/og-cover.png`（1200×630，讀 `logotype.svg` 不手抄）；⛔網址用 `useRequestURL().origin` **不寫死網域**（多租戶＋本專案搬過網域）；⛔圖上刻意不寫價格（調價沒人會記得重跑），價格放 ogDescription 由 `plans.ts` 動態組④FAQ 獨立成 `<section id="faq">`（原本巢狀在 `#pricing` 內，導覽列點下去會捲進價目表中間）⑤「即將推出」卡降權重（建議示意兩張減成一張）⑥重做比較表 `#diff`（沿用原本浮起勝方卡樣式，五列改成貼合新敘事）⑦**移除「預約 Demo」整區**（老闆問「已經有免費試用還需要嗎」）——表單、`lp-form/lp-field/lp-input/lp-hp` 樣式、`submitLead`／honeypot／`interestedPlan` 全部清掉，換成置中的綠色收尾 CTA；⛔`/api/leads` 端點與超管名單頁 `admin/super/leads.vue` **保留不動**（歷史名單還要看），但首頁已無呼叫端；頁尾的 `/#demo` 一併移除改成「功能比較」`/#diff`，法務頁註解同步——不留死錨點（實測全頁 0 個死錨點）⑧**方案卡只主打免費＋399**（老闆拍板）——⛔用頁面層級的 `FEATURED_PLAN_IDS` 篩，**不可以借 `plans.ts` 的 `landingHidden`**：那個旗標的意思是「沒向 PAYUNi 申報、不可對外露出」，借來當展示開關會讓商品資訊的售價列跟著少掉，變成頁面宣稱售價與申報不符；**商品資訊仍列全部三階段 399／799／1,499**（實測確認），卡片下方另加一行老實交代「另有 799／1,499 與企業報價，來信洽詢」。**視覺**：⑫定價區排版重整。**試了三種才對，SCSS 已寫註解叫人別再改回去**：(1) 說明靠左、卡片置中收 720px → 整區其他元素都靠左 164px、只有卡片從 360px 開始，中間一座孤島；(2) 說明擺左欄、卡片擺右欄並排 → 邊界是齊了，但**左欄 78% 是空白（304/388px）**，比孤島更難看；(3) **最終＝計費說明置中壓在兩張卡正上方、整組置中**——說明與卡片變成同一個單位，群組內空白從 304px 降到 30px，卡片置中偏差 0。只有兩張卡時整組置中才對，群組窄於區塊其他元素是刻意的。順帶把成本比較卡的列距 20→16px、商品資訊標題降一級（1rem 粗黑→0.8rem 灰）——法遵揭露要找得到，但不該比方案卡還搶眼。⑬方案卡功能列改成**每張都列同一組項目、沒有的打灰色叉**——原本「只列有的」會讓行數不一（免費卡按鈕上方空 60px、輕量卡只空 22px），統一後空隙 24/22 且一眼看得出升級多拿到什麼；兩張都沒有的項目（例如腳本自動化）不列，免得整排叉徒增行數。卡片不再置中收 720px、卡片高度層級統一（靜置 `--sh-soft`、hover 才升 `--sh-card`，原本八張並排都用重陰影整片發灰）、收尾四項保證改置中單列。**過程中抓到並修掉的缺陷**：⛔原比較表 `border-top: 1px solid var(--line-soft)` 那個變數**從未定義**→整條宣告無效、分隔線根本沒畫出來（改 `--hair`）；`ogDescription` 寫成立即求值的函式卻擺在 `lowestPaidPrice` 宣告之前→踩 TDZ 讓 app 初始化失敗；手機方案卡橫捲後**捲出畫面的卡片永遠不與 viewport 交會、`.lp-reveal` 恆透明**（進場效果改掛外層）；比較表漏補手機版樣式；`--demo-h` 設 212 只墊高右卡、左卡自然高 213.3px 照樣凸出 1.3px；⛔方案卡從 5 張收成 2 張後，之前為 5 張寫的「手機橫向捲動」沒跟著拿掉→**第二張卡在手機被裁掉一半**（已改直排，SCSS 留了註解叫人別加回來）。**⑨兩處間距／對齊修正（老闆看畫面抓到）**：（a）成本卡的標頭與內容左右都沒對齊——標頭 `padding: 14px 20px`、內容 `24px 26px 26px`、名單列又是 `20px`，三個值各寫各的，結果標頭文字比下面的列標籤右移 **12px**、右緣差 **6px**；收成單一 token `--panel-pad-x: 24px`，標頭／內容／名單列／名單頁尾全部吃同一個值（實測全站面板左緣差 0）。（b）定價區「標題貼著表格」——每個區塊的標題到內容間距是各寫各的（why/easy/value/diff 44、faq 36、signup 50），而定價區那張成本卡**根本沒設 margin-top，間距是 0**；收成 `--section-head-gap: 44px`，並補一條 `.lp-section > .lp-wrap > .lp-panel` 讓直接掛在 wrap 下的面板也吃到（用 `>` 限定直接子層，避免重複套到 .lp-two/.lp-demos 內的面板）。⛔手機版的 `.lp-cmp` 另外寫死 `margin: 24px` 會蓋掉 token，已一併改用變數。**⑩好友名單頭像改吃真人照片**（老闆拍板用真人照片）——這張卡是標了「示意畫面」的 UI 示意圖、訊息也只是普通客人問句、不是掛名推薦，用授權過的人像素材沒問題。**照片本身要老闆提供**（我不去抓圖：版權與肖像權要有授權來源），程式端把機制接好：照片放 `public/avatars/1~6.jpg`＋把 `AVATAR_PHOTOS_READY` 改 `true` 就生效，規格與**授權要求**寫在 `public/avatars/README.md`。⛔**照片與退路都做成 CSS 背景兩圖層**（上層照片、下層人形剪影），不要改回 `<img>`＋`@error`：圖片是 SSR 出去的，404 早於 Vue 掛載、監聽器還沒接上（實測完全不生效），而且 Chrome 會在 `alt=""` 的破圖上再畫一個破圖圖示；背景圖層載不到則是直接不繪製，剪影乾淨透出。⛔旗標預設 `false`：不加這道的話，照片還沒放時每次載入都會噴 6 個 404 一路帶上正式站。⛔照片是佔位人像，旁邊不可加「某某店家實際使用」這類說法（會變成沒有本人同意的掛名推薦）。**⑪精簡文字**（老闆問「文字太多的地方能否再優化」）——先量每個區塊字數再砍，不憑印象：最重的是定價前言 69 字，而且**與 FAQ 第三題「按用量計價會不會爆帳單」幾乎逐字重複**（同一頁講兩次）→ 砍到 26 字只留「額度用完會先轉真人接手，不會自動扣款」；比較表的 lede 40 字整句在複述上面的 h2 → 整句刪；其餘四處（其他方案那行、痛點總結條、兩則圖說）逐句收緊。⛔示意標示與「即將推出」說明只縮短不刪除——那是誠實揭露不是贅字。老闆回「文字要讓人容易理解但不要過度」後，把砍過頭的三處補回可讀版本（定價前言丟掉了「有免費額度」這個重點、「用量更大？」問句太跳、比較表整段 lede 被刪掉導致與其他區塊節奏不一致，補一句不重複標題的短 lede）。全頁 2,507→2,414 字。**另外統一全頁中文標點**（原本全形 58／半形 71 混用、同畫面兩種都看得到）→ 全形，實測渲染後半形逗號/分號/問號皆為 0。驗過：typecheck 0 錯、1210 測試綠、sass 編譯過；390／768／1024／1440 四寬度零溢出、零裁切、零 JS 錯誤、進場區塊全數顯示；桌機並排元素對齊 spread 全 0；`/privacy` 未被連坐。頁長桌機 9.1／手機 13.8 螢幕。⚠️**未 commit、未部署** |

### H. 對話與接手

| 編號 | 項目 | 狀態 | 說明 |
|---|---|---|---|
| `H-1` | 不同分類點開都要看得到完整對話 | `DEPLOY?` | 已修，線上未逐項驗證 |
| `H-2` | 對話區會有重複的產生 | `TODO` | 已回報未修，需先重現 |
| `H-3` | 各別帳號已讀功能分開 | `TODO` | 目前已讀是共用的，多客服同時看會互相影響 |
| `H-4` | 對話結束後分類專案／案件類別 | `TODO` | 需求未細化，先確認要解決什麼問題再排 |
| `H-6` | 未讀紅點消不掉修正要實機驗 | `DEPLOY?` | 程式已進 main（`8fde8e0`，見完成區），線上未驗。四件事要在後台各走一次：①開著某位客人的對話、請他傳一句 → 泡泡長出來後**那顆紅點要自己消**（不用點走再點回來）②在會話分頁按「接手」→ 紅點要消 ③開一個**這次部署前**就有訊息的舊對話放著不動，看 Network 不再每 30 秒重抓一次 `messages`（舊資料的時間差還在，靠前端擋）④「全部已讀」按一次要清乾淨，最上面那列不會留著 |

---

## ✅ 完成（DONE — 已 commit 並推上 main）

> ⚠️ 這批全部已進 `main`，但**線上部署狀態未逐項驗證**（`DEPLOY?`）。要確認得看 Amplify／Firebase console。

| 日期 | 項目 | 代表 commit |
|---|---|---|
| 2026-08-14 | `C-33` **所屬產品防呆①〜④全做**（老闆問「打錯按 Enter／不小心新增類似產品怎麼辦」→ 評估四項當日拍板全做）：①④**欄位打字當下就地攔**——清單裡沒有的名字老實講「存檔後會新增一個產品」；跟現成名幾乎一樣（GPLSU vs GPLUS）琥珀框問「是同一台嗎」二選一〔是，改用現成的／不是，這是新產品〕；只差空白符號的建議統一寫法；匯入預覽與資料頁同一個元件同步吃到；⛔「是不是新產品」比對 `known`（含已合併掉的舊叫法）不比收斂清單——對使用者處理過的舊叫法喊「會新增產品」是假話。②整理視窗補「**自己挑兩個合併**」手動入口（偵測三訊號總有漏網＝完全不同的講法，之前只能繞路逐份資料改名）＋合併前確認框明講型號誤併風險，合併錯可解除；合併／解除後「所屬產品」下拉快取即時失效。③偵測補**訊號 3「字幾乎一樣＝可能打錯字」**——整站匯入是 AI 自己標名字不經欄位，事後也要抓得到；⛔數字不同（12L vs 16L）視為同系列不同型號刻意不列、包含關係留給訊號 2（才有型號風險標）；被較多資料使用的當正式名。判定收單一來源 `shared/product-name-similarity`（欄位端與偵測端同一把尺，兩個畫面不能兩種說法）。typecheck 0 錯、1210 測試綠（新增 18） | `807d759` |
| 2026-08-14 | `H-6` **未讀紅點消不掉＋每 30 秒空轉修根因**（老闆回報「紅點有些根本跳不出來」→ 通盤稽核前後端未讀鏈路）：08-11 的 `H-5` 把紅點的「時間」與「方向」收成同源，但**蓋章那一邊還是另一本帳**——對話文件的 `lastMessageAt` 另外蓋 `serverTimestamp`，訊息自己存的是 LINE 事件時間，同一則訊息兩個時間差幾百毫秒（網路＋冷啟動）；而紅點問「列上這則有沒有晚於我看過的」，**差 1 毫秒就算沒看過** → 前端拿時間軸的時間蓋章永遠追不上列上那個。三條災情：①開著的對話紅點永遠消不掉（正在處理的客人最常開著＝最有感），按「接手」後也一樣（`reloadTimeline` 整條路都沒帶列上的時間）②`maybeRefreshOpenTimeline` 判「畫面讀到最新沒」比的也是這兩個永遠差一步的時間，恆為否 → **每 30 秒白重抓一整段時間軸（40 則）**，`E-7`/`E-8`/`E-9` 那輪漏掉的同款讀取費③「全部已讀」偶爾按不掉最上面那列（基準只取 `lastActivityAt`，它與 `unreadAt` 是兩份文件各自蓋的、會話那次還是背景補寫，誰先落地隨機）。修法＝**後端治本**：`lastMessageAt` 直接用訊息自己的時間、不另外蓋一次（口徑同 `cleanup.post.ts` 回填；送出的訊息兩邊都是 serverTimestamp，行為不變）；**前端**抽 `selectedRowUnreadMs()`＝紅點比對的那個欄位，`reloadTimeline` 一律拿它當已讀基準（接手／交還／結案／自己回一句／自己收新訊息／跳回最新全涵蓋）＋`openTimelineRowMs` 記住上次為哪一則讀過（**舊資料那個時間差還在，沒有這道就繼續每 30 秒掃**）＋全部已讀多取一份 `unreadAt` 最大值。新增 `handler.unread-timestamp.test.ts` 5 條釘住「兩邊同一個時間」（實地驗過改回 serverTimestamp 會紅 2 條）。typecheck 0 錯、1190 測試綠。⚠️**未實機驗，四件事見待辦 `H-6`** | `8fde8e0` |
| 2026-08-13 | `C-32`①〜⑦ **知識庫匯入視窗再簡化**（老闆看截圖問「能不能更好操作」→ 走查 1910 行全流程評 8 項、拍板照建議順序做）：**P0 畫面跟事實對不上**＝預覽頁開頭教人「勾選要匯入的」但清單預設收起、畫面上根本沒有勾選框（改成條數只在摘要區講一次＋只留截斷警告）；FAQ 範本沒設母本網址時走「下載 Excel」、說明卻寫「填完貼回來」照字面做會卡住（改成照實際路徑講）。**P1 空白狀態太重**＝「很多樣商品嗎」整塊（含只跟網址有關的官網首頁警告）在沒貼東西時就佔近半視窗 → 偵測到內容才出現＋警告只在貼網址時顯示＋問句改敘述句；禁用主按鈕補「先貼上內容或選個檔案」；標題「上傳 / 匯入」→「加入知識」與入口同名。**P2**＝勾了總表沒做出來補一句交代。導覽「總表」步驟目標元素空白狀態不再存在 → 改置中說明卡（引擎對缺目標會標「找不到」，不能放著）。typecheck 0 錯、1162 測試綠。剩⑧ CSV 見待辦 | `494f5ee` |
| 2026-08-14 | `C-31` **Phase 1 引導常態化**（Phase 0 同日稍早，見下row）＋`G-14` 結案：①開通精靈的對話原語（say/ask/apiRetry/換代輪詢）抽成 `useAgentScriptRunner` 共用引擎＋宣告式步驟格式＋10 測試——原本 1100 行含 race 的狀態機**零測試**；取消變一等公民（dispose 後劇本鏈就地停下＝`G-14` 修掉）；開通主線順序收成單一 `MAIN_FLOW` 表（原 `start()` 重複兩份，加一步要改兩處）②三條「帶你修好」劇本掛小幫手面板（LIFF 登記錯／轉真人通知沒設／知識同步失敗），異常卡多一顆「用聊天帶我修」：解釋後果→給正確網址或選人→**真的驗證有沒有生效**；教學口徑取後端同一支檢查端點（G-7「教一套驗一套」的教訓變機制）③問答回覆會帶路：模型回 goto 目的地 id（白名單 15 頁＋最多 2 張＋去重，**編不出網址最多挑錯頁**），回覆下長站內連結卡，與開通精靈共用渲染層——08-06 明列未做的「深連結」補齊。為什麼：引導是代辦（Phase 2）的信任地基。typecheck 0 錯、1185 測試綠（+12）、sass 編譯過；⚠️**三條劇本與開通回歸未實機走**（驗法：找掛著對應異常的帳號點「用聊天帶我修」走完＋開通精靈全流程再走一次） | `44ca5f0` |
| 2026-08-14 | `C-31` **Phase 0 地基＋`E-16`/`E-17` 結案**（拍板同日動工）：①操作稽核 `auditLogs`——`writeAuditLog`＋憑證欄位遮罩（token/secret/…一律 `••••`）＋`diffChangedFields` 只記有變欄位，先接 `ai/settings.put` 與 `richmenu/setDefault` 兩個門檻最高的寫入點（沒變更不寫＝不留噪音），索引入 `firestore.indexes.json`（部署＝`A-11`）②工具註冊表 key 綁 `shared/types/admin-agent.ts` 的 `ADMIN_AGENT_TOOL_LABELS` 單一來源——前端手寫第二份對照表退役（它已實際漏了 08-06 新工具的標籤，UI 一直裸奔英文工具名）；`ToolDef` 加 `requires`/`mutates`，agent 迴圈收 `role` 逐工具比對權限（「五個直讀工具剛好都 viewer 級」從巧合變機制），`mutates=true` 在 Phase 2 確認流上場前一律擋下（防手滑掛寫入工具）③`E-17` token 細目不再回 viewer ④`E-16` setDefault 一律反查歸屬。為什麼：AI 代辦開放寫入前，系統必須先答得出「這筆是誰改的」。typecheck 0 錯、1173 測試綠（新增 11）。**評估報告補 08-14 拍板註＋更正**（`ai/settings` 其實是先讀後深合併，「整份覆寫」是評估誤判） | `8e9cc88` |
| 2026-08-13 | **小幫手「從查到做」評估＋分階段規劃**（老闆提問：對話式引導常態化＋語意化操作可行性；老闆拍板前提＝只做後台側、依風險分級）：四路程式碼盤點（開通引導／小幫手本體／腳本架構／207 支端點風險分級）＋三份歷史文件對讀。結論＝**可行、收斂三個半成品不蓋第四套**——訊息渲染層只有開通精靈在用、劇本引擎已會代打寫入但寫死在開通、LLM agent 7 工具全唯讀回純文字；「從查到做」是 08-06 S2／08-07 Phase 3 兩度立案的舊帳。做法＝操作模組註冊表抄腳本形狀（收斂＋驗證分離、驗證器與功能頁同一支、模型不生 ID、寫入先驗證後執行不重生）；分期 0 地基（稽核 log＋工具權限欄位）→ 1 引導常態化（chat 回結構化訊息＋劇本 runner 抽出＋「帶你修好」掛 alerts）→ 2 代辦 MVP（pending-op 確認流）。順帶抓到三缺陷（`E-16` setDefault 歸屬、`E-17` token 洩漏、腳本上下架無確認收進 Phase 2 預覽端點）。報告 `docs/ASSISTANT-SEMANTIC-OPS-EVAL-20260813.md`＋老闆版 artifact；拍板三題在 `C-31` | `bb437ff` |
| 2026-08-13 | `C-28`／`C-29`／`C-30` **知識庫評估九項全做**（含匯入全流程）：①**所屬產品改成挑現成的**——手打是「同一台被當成兩台」的源頭（三月「GPLUS 智慧除濕機 12L」／六月「GPLUS除濕機12公升」→ 客人問保固被反問「您指的是哪一台」還列出同一台兩種寫法）；新 `KnowledgeProductNameField` 三處共用，**刻意不用 `el-select allow-create`**（要按 Enter 才算數，直接按儲存會安靜丟掉打的字）。附帶四缺口：手寫知識終於有產品欄（寫進自動建的 manual 資料；掛既有資料的卡刻意忽略避免只進全域清單卻沒存）、資料列表看得出誰沒設、下拉排除已合併舊叫法、整站匯入結束列出這批的產品名與「新」標記（**讀不到清單時不標新**）。判定抽 shared `needsProductName` 與體檢同一把尺＋測試。②**匯入等待的承諾原本是假的**：文案寫「可以先關掉視窗去做別的事」，但工作編號只活在記憶體，換頁／重整就撿不回來，伺服器那份還活著也拿不到 → 大檔整套重跑重收費（疊 `C-12` 沒去重）。改成編號落 localStorage（按租戶分鍵、55 分）＋進頁面自動接續，文案改成做得到的說法（接著跑、不會從頭重來；離開期間暫停——伺服器端是輪詢兼推進），側欄補「整理中／整理好了」入口。③學習成本：對外「卡／張」統一「條」、知識清單狀態篩選、總表勾選改問「這份資料裡有很多樣商品嗎」＋整理完 ≥8 條主動建議（明講再花一次用量）、「其中 N 條含金額」可點只展開那幾條。**順手修既有缺陷**：`cancelPreview` 先 `cancel()` 又 `reset()` 把取消旗標清掉＝按了取消照樣跑完並蓋掉畫面。typecheck 0 錯、1162 測試綠（新增 6）。三輪 code-review 修掉元件註冊名不符（照原樣上線整個欄位不會顯示）、讀不到清單卻全標「新」、背景接續搶走整站匯入畫面 | `bf9d22e` |
| 2026-08-13 | **還原自動回覆下架時連坐刪掉的共用樣式**（老闆回報活動／推播頁 padding 不見）：`023b36d` 刪 `_auto-reply.scss` 時，`ar-*` 這批 class 早被活動貼標／推播／客服預存／流程編輯器／組織設定借用——`.ar-editor-body` 的 padding 消失＝三頁編輯區整頁貼邊，提示字、空狀態、開關色、對話快速插入面板的分類膠囊（`.conv-picker-tab`）一起裸奔。修法＝`ar-*` 收容到 `admin/_shared.scss` 共用層保留原名，conv-picker 分頁鈕還原回 `_conversations.scss`，順帶補從未定義過的 `.link`/`.ar-link`（「前往標籤管理」「查看用量」一直長得像純文字）。為什麼會漏：下架時只掃了功能引用沒掃「別頁借用的 class」——**刪共用 partial 前要反向 grep 每個 class 在 .vue 的引用**。headless Chrome 驗過 computed style | `f6a9645` |
| 2026-08-13 | **推播卡死修正複審即修（code review `ef6fcd2`）**：10 路搜索＋對抗驗證抓到 14 項，全部成立並修完。最危險的是我自己寫的預設值方向錯——「LINE 有沒有被呼叫」用 `updatedAt?.toMillis?.() ?? 0`，欄位缺失或 `{_seconds}` 形狀就變 0，剛好落在「沒有任何人收到，可以放心重發」那側＝**會害人把整份名單重複轟炸**（改 `parseFirestoreDate`，只有兩個時間都讀得懂且相等才敢說安全，其餘一律「無法確認」）。其餘：停滯時鐘改用最後一次寫入（受眾大時解析名單要幾分鐘，原會誤殺活單）／時間讀不出來的單照樣收殮（原 `continue` 會永遠卡住又占滿 `limit(20)` 名額）／看門狗查詢補 `.select()`（原每分鐘搬整份文件含上千 `resolvedUserIds`）／`failureReason` 補齊另兩條寫 failed 的路徑並把內部英文換成人話、措辭收進 `shared/broadcast-failure.ts`（小幫手警示已承諾「進去看失敗原因」，只有看門狗有寫＝承諾兌現不了）／retry 改「先清舊帳再改狀態」（原先翻 draft，清理中途掛掉就再也進不來，舊名單混進下一次報表）＋清 `broadcastClickLogs`＋重發換 LINE 彙總單位（否則開封／點擊疊加、點擊率可超過 100%）／列表頁輪詢條件補 `processing`（原只看 scheduled＝「列表頁備援」名不符實）／前端拿掉多餘 `selectedDetail`（詳情失敗時 A 的「可放心重發」會掛在 B 的標題下）／同款 fire-and-forget 雷 `payment-reconcile-tick` 在 Lambda 停用（對帳走 run-tasks，會等它跑完），判斷抽 `server/utils/lambda-runtime.ts`。typecheck 0 錯、1156 測試綠 | `4ec6bb2` |
| 2026-08-13 | `A-10` **排程推播隨機卡死「發送中」修根因**（8/12 AROMIC預熱 206 人整批沒送出、6/18 MATELSAER開賣卡兩個月，LINE 都沒被呼叫）：①刪 `broadcast-scheduler-tick` middleware＋croner 插件偵測到 Lambda 停用——兩者都是 fire-and-forget，Lambda 回應一結束執行就凍結，發送死在 claim 之後、受眾快照之前；排程觸發只留等得到結果的兩條路（Cloud Scheduler→trigger-scheduled、列表頁輪詢→process-due）②看門狗：processing 超過 10 分鐘→標 failed＋寫 failureReason（用「認領後有沒有第二筆寫入」判定 LINE 是否被呼叫、能否安全補發），小幫手既有 broadcastFailed 警示自動亮（原本兩顆推播警示都看不到 processing 卡單）③失敗單出口：retry 端點重設回草稿＋後台失敗原因卡與「重設為草稿再發一次」按鈕。為什麼：processing 原是死路（排程器不撿、不能取消、不能重發、無看門狗），卡死＝永遠無聲無息。typecheck 0 錯、1137 測試綠；補發拍板在 `D-12` | `ef6fcd2` |
| 2026-08-12 | **開通引導六修複審即修（code review b73e668）**：5 路搜索＋5 路對抗驗證，確認 15 項、修 12 項（3 項駁回：連點誤觸／NaN 時間／STATUS 慣例，查證後不成立）。重點：①排障選單開著時輪詢全停但轉圈卡照轉——訊息真的來了還喊「還沒等到」，按跳過就錯過魔法時刻 → 改成整段等待輪詢不斷＋等待 45→90 秒（45 秒對第一次加好友的人太短，排障變常態）②驗 Webhook 原本每按一次寫 workspace doc＋清**全租戶**憑證快取 → 改走唯讀 `line-webhook-verify` ③排障重驗掉了 401→重貼 Token 出口、自家 API 掛掉被說成「就是卡在這裡」→ 共用 `verifyAndAdvise` 分診 ④`/liff/lead` 七處字面量收 `shared/liff-lead-path` 單一來源＋測試釘住，`publicBaseUrl` 空時先補查不再默默兜瀏覽器網址 ⑤`skips().firstMsg` 只寫不讀 → 補守門 ⑥慶祝卡不再把點選單／影音說成「貼圖／圖片」＋AI 跳過補 note 與話術。typecheck 0 錯、1131 測試綠；**未實機走 LINE 憑證流程**（同 Phase 1 限制）；沒修的兩件入 `G-13`/`G-14` | `f7b694a` |
| 2026-08-12 | `G-7`〜`G-12` **開通引導六修**（同日評估即修，報告 `docs/ONBOARDING-CHAT-EVAL-20260812.md`）：LIFF 步驟不再教「填你的官網」改給 `{base}/liff/lead` 複製卡（口徑同 `liff-endpoint-check`，教錯＝活動連結死＋剛開通就被健康檢查亮紅）、教「先在 OAM 啟用 Messaging API」（不然 Developers 看不到帳號）、測試前教關 LINE 內建自動回應（系統測不到只能教，不關客人每句收兩套回覆）、等第一則訊息 45 秒沒動靜主動排障＋當場重驗 Webhook、完成／之後再說落地改「對話」頁（`G-11` 老闆 08-12 拍板照建議：新帳號統計全 0 接冷場，對話頁有剛傳的那句）、小項四件（AI 開啟失敗補跳過出口／草稿＋跳過通知交互提醒／進度條 dead code／開場文案）。typecheck 0 錯、1128 測試綠；**未實機走 LINE 憑證流程**（同 Phase 1 限制） | `b73e668` |
| 2026-08-12 | `C-24`/`C-25`/`C-26`＋`C-27`①②③ **AI 生成腳本三防線＋範本微調**：沒給的事實出【請填入：…】占位符（編輯器標黃）、觸發詞後檢剔除（單字/高頻詞/含租戶敏感詞的死關鍵字）、做不到的描述拒答 422 帶原因（建議只准指向真的存在的功能——第一版模型推薦已下架的自動回覆，實測抓到才加）；範本＝出貨查詢半死路改收編號轉真人、「人工客服」歸位找真人範本、刪「要退」。為什麼：編出很像真的營業時間/價格人審抓不到；「問題」當關鍵字會攔走「訂單有問題」蓋掉 AI。16 案 live 重測全過 | `0c84510` |
| 2026-08-12 | **AI 生成腳本 16 案實測評估**：直呼 `generateScriptDraft` 跑 16 種語意（模糊/無關/injection/簡英文/敏感詞/九連問…），結構層全綠（0 接線錯、0 卡死步驟、卡死防護迴路實測有效），內容層抓到三大缺陷入表 `C-24~C-26`＋範本微調 `C-27`。為什麼：生成只出草稿不落庫的設計守住了下限，但「編出很像真的營業時間/價格」這類錯誤人審抓不到，得在生成端擋。報告 `docs/SCRIPTS-AI-GEN-EVAL-20260812.md` | `4ea3096` |
| 2026-08-12 | **UIUX-TODO 封存＋C-19 查證結案**：`C-19` 試算表其實早分享好（直查 Firestore 四個 gsheet 來源全 `ready`）；`UIUX-TODO` 逐條重驗——假 Demo 表單 7/20 已修（原盤點誤判為最重孤兒）、手機登入重驗仍壞；活項拿編號 `E-13`（手機版 RWD）/`E-14`（打磨批）/`E-15`（手機登入），老闆拍板三題全部記錄待辦。為什麼：封存前不重驗就會把已修的當待辦搬進來（跟 `A-8`/`B-8` 同一種假帳，這次反方向） | `8c4785b` |
| 2026-08-12 | **docs 全面盤點＋三疑案定案**：`A-8` 結案（TTL 兩租戶早設好，fields API 驗證；本表 08-10 誤記）、`B-8` 結案（沙盒重測作廢／折讓皆 code 0）、`B-19` 改寫（換卡入口存在，真缺口＝必伴隨一期付款）；`AI-KB-TODO.md` 封存（8 條孤兒待辦搬進本表＝`C-19~23`/`D-11`/`F-7`，4 條已完成補勾，別名一檔兩說法補註）。為什麼：8/10 收斂三上游時漏了 `UIUX-TODO`/`AI-KB-TODO` 兩份，孤兒待辦與「文件 vs 本表說法相反」×3 靠全面盤點才浮出；`UIUX-TODO` 與文件校期留 `E-11`/`E-12` | `9da018c` |
| 2026-08-11 | `E-7`/`E-8`/`E-9` **資料庫讀取費四洞修補**：offset 翻頁改 startAfter 游標（Firestore 對跳過的每筆收讀取費，實測 offset(3000).limit(1) 回 0 筆計 3,008 次）；「待處理」佇列條件 hasInbound 下推 where（原記憶體過濾對 1,584 筆殭屍場過濾率 0%，停在分頁上＝19 萬次讀取/小時，平日 20-30 萬/天的主力）；輪詢守門（搜尋字留框暫停＋背景分頁停跑回前景補刷）；深連結 userId 直達＝1 次讀取。為什麼：8/11 單日 219 萬次讀取（NT$49）。新複合索引 (workspaceId,status,hasInbound,lastActivityAt DESC) **已部署兩租戶且建置完成**；程式待部署（`DEPLOY?`），上線前實機驗：搜尋/載入更多/待處理分頁/監控頁開對話。治本（殭屍場源頭）＝待辦 E-10 | `f384e50` |
| 2026-08-11 | `H-5` 未讀紅點亂跳＝兩本帳在比：後端給 unreadAt（＝對話 lastMessageAt，非進行中的場 null 不亮）與 lastMessage/lastDirection 同源；已讀基準去掉 Date.now()；開著的對話自己收新訊息；背景分頁延後蓋已讀。為什麼：時間吃 lastActivityAt（按鈕/接手/排程都推進）、方向吃最後一則訊息，看完的列反覆變紅。**尚未實機驗**：五種現象要在後台各走一次 | `f384e50` |
| 2026-08-11 | `C-17`/`C-18` 切卡與答題 prompt 去行業字眼：時效改判準式（「只寫下個月再看還是對的內容；是拿掉那幾句不是丟掉整張卡」——後半句漏講 LLM 會整批丟掉集資產品卡，實測撞過）；「募資金額不是售價」通用化；免責剝除「僅供參考」須綁圖像主詞。為什麼：34 張卡寫死當下集資狀態；老闆拍板不做關鍵字過濾（跨行業必誤刪）。既有髒卡走「重新同步」重切 | `7b9c0f1` |
| 2026-08-11 | `C-16` 知識來源變動偵測改雙指紋（逐字＋抹數字）＋學習網址數字會不會自己動；學不會標 detectStalledAt 明講「自動偵測對這個網址無效」。為什麼：官網數字每天變，「連兩輪同值才算真變」永遠湊不齊＝假綠燈；差異門檻量過不可行（數字自變 18.4% > 真改版 5.6%）。正式站要等三個檢查輪（三天）才脫困 | `b487393` |
| 2026-08-11 | `I-1` 用量量詞（則/次/期）收進 shared/billing/usage-units.ts 單一來源，七頁＋額度信＋小幫手引用不自造句；「本月」→「本期」。為什麼：invocations 被掛上收費單位「則」（真值差 2.5 倍）且欄位緊鄰「計費方案」 | `e83faa3` |
| 2026-08-11 | `A-1`／`A-9` 結案：AWS 成本接通**實機驗收通過**——成本總覽顯示原價 NT$254（Amplify 238／Route 53 16）＋折抵行「原價 − 折抵 ＝ 實付 NT$0」。逐日拆帳排查費用來源（修正稍早「開發降溫會回落」的說法——**主因是結構性的，不會自己回落**）：①7/31 暖機改版（`fc591f4` 併發子保溫＝每 ping 4 個請求＋暖 2 條頁面）→基準線 ~3,700→8,000 請求/天，**用錢買速度的取捨**（修掉冷啟動 2.3s→0.14s），約 NT$450/月 ②7/23 排程接上（warmup＋run-tasks）→基準線從近 0→~3,700/天 ③開發活動只是尖峰疊加（工作日多 3~4 千/天，會回落）。要不要縮熱機池見 `D-10` | （外部設定＋驗收，無 commit） |
| 2026-08-11 | AWS 成本金鑰環境變數改名 `COST_EXPLORER_*`。為什麼：老闆實際在 Amplify 設 `AWS_COST_*` 被主控台拒絕——**Amplify 把整個 "AWS" 前綴列為保留字**，當初只防了 Lambda 的三個保留名。nuxt.config／教學／頁內說明同步改 | `bb4f56e` |
| 2026-08-11 | 主機花費改顯示**原價**：折抵金（Credit/Refund）用 RECORD_TYPE 維度拆出來另計（同一次查詢不加錢），卡片另起一行「原價 − 折抵 ＝ 實付」。為什麼：帳號還有 AWS 折抵金時帳單淨額是 0，只看淨額會誤讀成「主機不用錢」，折抵用完數字才突然冒出來。順帶：解析抽純函式補 3 測試、開通說明改「打開頁面即自動啟用」。老闆當日正在照教學設金鑰（A-1／A-9），設完即可驗成功路徑 | `d8e6813` |
| 2026-08-10 | 狀態列加額度即時數藥丸（A 案：跟錢有關第一眼看到；near 琥珀/over 紅、點了捲到方案卡；無限方案不顯示）＋超管成本頁「答出・後台」副行改真拆解（原排版把不在 207 裡的後台 125 排得像拆解，實測資料剛好 82+125=207 更騙人）＋「則」定義補與 LINE 訊息則數無關 | `6a17838` |
| 2026-08-10 | 拆對不上帳的「91 次」原因行（事件帳 vs 場帳三組打架）＋無限方案用量改對齊報表月份（不再繼承續約日窗口）＋方案卡上移到趨勢前（跟錢有關，但仍在成績/待辦後）＋上月不足 5 場不做比較（▲6300% 是垃圾數字）。為什麼：老闆拿實際資料抓到 91 vs 43 vs 4、94 則 vs 8 月對不起來 | `93947ba` |
| 2026-08-10 | 拆「答完客人又找真人」常駐數字（場制已吸收該訊號，>25% 才進 verdict 但書）＋方案卡「則」旁補定義 ⓘ；全頁單位收成兩種——表現區只講「場」、帳務卡只講「則」。為什麼：老闆問「這是什麼、有必要嗎、到底是則還是場」——三種單位並存就是學習成本 | `0709b0b` |
| 2026-08-10 | 無限方案也顯示本期已答則數（「本期 AI 已回答 N 則・不限則數」，標題改「方案用量」；無錨定週期則退回純文字不印 0）。為什麼：無限是計費條件不是隱藏數字的理由，客製戶要看得到 AI 做了多少工 | `52949c6` |
| 2026-08-10 | **AI 表現主指標改「場」制**：全程搞定率＝AI 首接且沒轉真人的場÷AI 首接的場，資料吃對話統計 session 月桶（trend 桶新增 aiEscalated）、兩頁天生一致；「先問清楚」退出主畫面只在 >30% 時現身琥珀診斷行；畫面全面去「出手/題」；舊 /api/ai/usage/trend 下架。為什麼：老闆連三輪看不懂工程單位——「場」是使用者在對話列表自己驗證得了的東西 | `472731f` |
| 2026-08-10 | 趨勢圖三根並排改**堆疊**（一根柱子＝分母，率＝綠段佔整根）＋hero 副標念出分子分母＋「自己搞定」算式 tooltip；折線改中性炭灰、圖例線圖示、裁開頭空月份、標題跟實際柱數。為什麼：老闆看不懂 39% 怎麼算出來、也不懂「先問清楚」為何獨立一段——根因是並排柱把分母藏起來。順帶修轉真人原因聚合漏濾 `isFollowup`（分項會加超過總數） | `115833b` |
| 2026-08-10 | AI 表現頁 UIUX 評估後修 6 項：三個載入點失敗態＋重試（斷線不再顯示「沒客人來問／都處理完了」假綠燈）、verdict 待補數只認「答不出來」白名單且不隨篩選漂、案例篩選收成 hero 同語言分組（預設補得動的那組）、清單載入更多＋清空回填、「答完又找真人」>25% 進 verdict 但書、補往建議收件匣的橋。為什麼：儀表板給判斷，判語數字不對＝叫人白做工 | `3b82e68` |
| 2026-08-10 | 趨勢圖加「自己搞定率」折線＋tooltip 結論先行（柱子看量、線看好壞；八月柱子變高但率其實從 43% 掉到 39%） | `b150bb2` |
| 2026-08-10 | AI 表現：「轉給真人」圖例下補原因拆解行（答不出來 62 有救／刻意要人接 15／沒留原因 13；三數據修正全部完成） | `f025a03` |
| 2026-08-10 | AI 表現：成績分母改「AI 出手過」（directHandoffs/followupAnswered 子計數器、反問成果可見、39%→42% 不再冤枉 AI） | `4fdb4bc` |
| 2026-08-10 | AI 表現頁優化 7 項：hero 下方補「這樣算好還是不好」的判語、額度卡從第二張搬到最後（先講價值再講錢）、側欄移到 AI 組第一個、修掉「本月 vs 本期」自相矛盾與循環定義 | `cdebc94` |
| 2026-08-10 | 用量監控改名「AI 表現」並從 usage.read 降到 ai.read（計費改逐欄位擋，第一線客服看得到 AI 表現）；hero 主角換成自己搞定率 | `9972fc1` |
| 2026-08-10 | 用量監控頁 UIUX 修繕 6 項（硬寫「這個月」會說謊、hero 補「較上月」基準、趨勢補第三段、待補知識清單上移） | `5f49b24` |
| 2026-08-10 | 用量監控頁移除所有金額與 token（權限本來就沒漏，是金額散在兩頁的問題）；成本一律只在超管「成本總覽」 | `4ae3a0c` |
| 2026-08-10 | AI 記帳稽核：修正成本頁用錯分母（該用 invocations 不是 answered）、小幫手與生成腳本兩處歸錯桶、「找真人」轉真人事件漏記；三桶算式抽成單一來源 `ai-cost-buckets.ts` ＋ 7 項測試 | `66a6af7` |
| 2026-08-10 | 超管成本頁改版：拖拉估算器整組刪掉，改成 AI（實測）／資料庫（Firebase 實測用量）／主機（AWS 真實帳單）三筆；含 Cloud Monitoring 分桶時區 bug（`taipeiMidnightAfter`）與 AWS 設定教學 | `a936ca3` |
| 2026-08-09 | 自動回應合一：自動回覆功能整組下架、腳本簡單模式、整頁捲軸收成一條 | `023b36d` |
| 2026-08-09 | 腳本編輯器 UX 大改 1~10 項（含合併成兩分頁、健康狀態三重檢查） | `023b36d` |
| 2026-08-08 | 對話完整時間軸 ＋ 分段讀（兩端點合一、一段 40 則） | `a9e41c7` `9194b89` `6528595` `e6b3108` |
| 2026-08-08 | 未讀紅點規則（只有客人來訊才紅）＋ 全部已讀 | `fbfc055` |
| 2026-08-08 | 自動回覆複讀防呆（並行去重＋時效＋轉真人後清紀錄） | `1dce231` `6716c90` |
| 2026-08-08 | 續扣單誤作廢造成重複扣款 | `4c1e1cc` |
| 2026-08-08 | 會話事件補 workspaceId ＋ 舊資料回填（兩專案已跑） | `7792461` `62c65bb` |
| 2026-08-08 | LIFF 設定端點改前綴查詢、租戶判斷規則收斂 | `6dec35a` `35e4839` |
| 2026-08-08 | 小幫手收編腳本異常兩項（scriptDeadEnd 紅／scriptUnreachable 黃） | `cb83e8f` |
| 2026-08-07 | PAYUNi 自動扣款端到端實測（首刷→Notify→開通→發票→期末續扣→折抵→降級→取消解約），含兩個會動到錢的真 bug 修復 | `4c1e1cc` |
| 2026-08-06 | PAYUNi `UPP02087` 結案：是後台「限定 API 設定」開關，**四份申請書都不用送**；沙盒首刷真刷拿到 `CreditHash`、續扣／查詢／解約全鏈路通 | `c5d61cf` |
| 2026-08-07 | LIFF 換網域災情三件套 ＋ Endpoint 填錯系統內偵測 | `1cbe1b1` |
| 2026-08-07 | 活動連結「無法完成綁定」修復 | `1cbe1b1` |
| 2026-08-07 | 昨日摘要卡改版 ＋ 日期窗時區 bug（`taipei-day.ts`） | `9ee9544` |
| 2026-08-07 | 對話日期分隔線、誰回的標籤、右鍵標記（釘選／待跟進） | `1cbe1b1` |
| 2026-08-07 | 開通引導對話 agent Phase 1 | `c1ce12e` |
| 2026-08-06 | 客服 LINE 推播改版（二選一模式／每日摘要合併／digestHour） | `3f71f75` |
| 2026-08-06 | AI 脈絡卡每回合一張（`aiTurns`）＋ 接手前要知道的事 | `3f71f75` |
| 2026-08-06 | 轉真人拓樸統一 ＋ 勿擾時段 | `9261f55` |
| 2026-08-06 | 對話列表五分頁統一、客人動作軌跡 | `9261f55` |
| 2026-08-05 | 跨產品張冠李戴（對話級產品鎖）、重新同步報假變更 | `8f8de5d` |
| 2026-08-05 | 圖片辨識 Phase 1+2、客人照片存檔 | `c7e87ba` |
| 2026-08-04 | 知識庫 UX 收斂＋P1 七項、建議收件匣、待處理佇列說謊六修 | `4736127` `240c4cb` |
| 2026-07-31 | 知識卡啟用開關＋有效期限、併發子保溫 | `a47d40d` `fc591f4` |
| 2026-07-30 | PAYUNi 特店送件補件（品牌統一 MiniMe、改價）、幕後 Token 續扣 P1 | `c510965` `c5d61cf` |

更早的完成項目請看 `git log`（438 個 commit，2026-08-13 實測）。

---

## 怎麼維護這份

1. **每次工作結束前**，把動到的東西寫進對應區塊：新開工 → `進行中`；commit 完 → 從 `進行中` 搬到 `完成`（補 commit hash）；發現新事情 → `待辦`。
2. **狀態要對得上 repo 實況**：`DOING` 必須真的在 `git status` 看得到；`DONE` 必須真的在 `git log` 查得到。不確定就寫 `DEPLOY?`，不要猜「已上線」。
3. **一句話寫清楚「為什麼」**，不要只寫做了什麼——三個月後只有 why 有用。
4. 細節規格不要塞進這裡，放 `docs/` 專門文件，這份只放指標。
5. 同步到 Notion 時，三個區塊各對一個 view；`狀態` 欄直接對 Notion 的 select 屬性。
6. **編號是同步的對齊鍵**（`A-1`、`B-3`…＝區塊字母＋流水號）。Notion 開發表的「編號」欄存同一組值，同步時照編號比對，**不要靠語意猜**（兩邊命名刻意不同，例如 md 寫「PAYUNi P3 每期續扣排程」、Notion 寫「每期自動續扣排程」）。
   - **編號一旦給就不重用**：項目做完搬去「完成」時，編號跟著走不要回收，完成列補上編號；新項目一律取該區塊的下一號。
   - 2026-08-10 以前的完成紀錄留空不補——那批項目當時根本沒有編號，事後回填只是造假。
7. 同步節奏一週一次。同步前先跑一次事實校對：`wc -l server/utils/handler.ts`、`git rev-list --count HEAD`，把會漂的數字更新掉再往 Notion 推。
