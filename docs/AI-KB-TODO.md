# AI 知識庫待辦總表(2026-07-31 定稿)

## 2026-08-01 再評估+執行紀錄(動工前逐項覆核:優缺點/必要性/業界對照)

評估方法:三路並查——匯入管線與檢索答題管線逐檔探勘(確認每項的真實插入點與工程量)+業界 9 家查證(Chatbase/Intercom Fin/Dify/Zendesk/CustomGPT/Tidio/Crisp/Gorgias/Forethought,官方文件為據)。

### 業界對照結論(9 家)
- **整站爬取=業界標配**(7/9 家有,我們落後最大的一項);**網頁定期自動重爬=主流每週全自動**(我們刻意人審有理由:保護手編卡+重切卡品質不穩,不必跟)。
- **知識缺口報表=頭部玩家全都產品化**(Intercom 聚類失敗對話+比對真人回覆+自動生成草稿最強;Forethought/Gorgias/Chatbase/Tidio 皆有)——是我們對齊業界最該補的一項。
- **三塊業界空白**(9 家全沒有,做了是差異化):匯入自動認產品名並讓使用者確認、匯入當下的內容品質守門、常駐知識庫健康彙總(Zendesk 甚至把 Content Cues 下架了)。我們的檢索架構硬依賴 productName(反問分組/防混答/指名作答),別人不需要、我們必須做。
- **比較題/PDF 表格沒人端到端做**(最多通用 reranker);492 卡規模不需要 rerank 模型,關鍵字重排即可。

### 判定與執行結果(✅=2026-08-01 已完成;vitest 549 綠/typecheck 0,未 commit)
1. ✅ **開發序6 一句兩問**(判定:必須改——是程式 bug 不是調參):根因=`dedupeBySource` 同來源只留一張,第二問的卡(常與第一問同來源:價格卡+保固卡同一產品頁)被刪且無豁免。修法=子問題「錨點卡」(各子問題 top-1 過地板分者,top-k 裁切/識別碼 tag 合併/同來源去重三處都不得丟)+answer prompt 逐條點名子問題。
2. ✅ **開發序5 比較題檢索**(必須改):逐品項檢索本已存在,敗因=取卡無面向偏好(query 只有產品名,top-3 常全是保固/故障卡)。修法=每品項 topK 3→6+`preferSpecCards`(規格/特色優先、售後卡靠後的穩定重排,title+tags 比對)。**⚠️ 部署後用 7/31 的 4 道比較測題重驗**(無主卡已修、基準已變,可能部分本來就會好)。G2「PDF 表格走 Gemini 文件理解」未動,先看輕量修法夠不夠。
3. ✅ **開發序3 的資料地基:轉真人事件流**(必須先上——現況 aiMeta 只存每客人最後一筆,報表沒資料可聚類;資料從部署日才開始累積,晚一天少一天):新 collection `aiHandoffEvents`(query/reason/confidence/intent/top3卡/followupOf,expireAt=240天),answerWithAi 全部 handoff 出口 fire-and-forget 寫入、isTest 不記;複合索引已加 firestore.indexes.json。**報表本體(聚類排行+補一張卡按鈕)=下一批**。
4. ✅ **開發序1 匯入自動認產品名**(必須改——productName 原是幽靈欄位:型別未定義/後台改不了/程式零寫入路徑,7/31 靠工程師手改 Firestore,違反自助化):預覽 finalize 時 LLM 偵測(多產品/平台頁回空)→ 匯入視窗「所屬產品」欄預填可改 → bulk-create 寫入 source → 來源詳情頁新「所屬產品」卡可編輯,儲存自動重建該來源索引(新端點 `POST /api/ai/sources/:id/reindex`,只重跑該來源,不再全庫重跑)→ 產品索引隨來源**只增不刪**自動維護(刻意不刪:清單含手動種入的別名、無 provenance,全量重建會洗掉)。
5. ✅ **開發序4 匯入品質守門**(必須改——warnings 通道本已貫通前後端只缺生產者,工程比預估小):擷取層=太薄(<300字,url 給動態頁專屬提示)/連結密度過高(只抓到選單頁尾);finalize 四檢查=時效字眼(募資/折扣/檔期→建議設有效期限)、與現有卡標題重複≥3張(GPLUS 教訓)、總覽卡對照產品索引涵蓋率<30%(「主要產品為X」矛盾)、多卡檔案沒認出產品名(說明書漏設提醒);文案=URL 分頁+總覽卡勾選改「不建議用首頁」。全部非阻斷。
6. ✅ **批次3 反問按鈕短 label**(順手修,小):quick reply 送出文字改短 label(撞名退回完整標題),followup 比對 label/title 都認(部署前發出的舊反問相容),playground 按鈕同步。
7. ✅ **開發序7 的防輪播部分**(小):`pendingHash`——新 hash 連兩輪相同才確認變動,跳回原值/又變別的值都重置;代價=真變動晚一個檢查週期通知。**小改自動套用=下一批**。
8. ⏭ **開發序2.5 整站匯入**(該做:業界標配落後最大,但工程中大→下一批之首;地基已確認:同域連結解析器 `resolveInternalUrl` 現成,差 sitemap 探測+頁面清單 UI+批次 job)。
9. ⏭ **開發序2 健康檢查列**(該做,下一批:orphan banner 是現成 UI pattern,缺一支彙總端點;業界沒有完整版=非跟風必需,但自家稽核痛點真實)。
10. ⏸ **A4 回覆字數**(查證後**無需工程**):程式預設本來就是 300,MYFEEL 的 220 是 Firestore 直接寫入值(後台滑桿 step=50 產不出 220)——後台滑桿拉到 300 即可。另查明:截斷會**無聲丟掉列表尾端項目、不留省略提示**,要不要加提示留給老闆拍板。
11. ⏸ **開發序8 設定頁白話分層**(不動:資訊架構重排,先跟老闆對齊再做)。

### 第二批執行紀錄(2026-08-01 深夜,vitest 555 綠/typecheck 0,未 commit)
1. ✅ **開發序2.5 整站匯入**:`POST /api/ai/knowledge/discover-pages` — robots.txt(尊重 Disallow,前綴+萬用字元)→ sitemap 優先(含 sitemap index 走一層)→ 同域連結備援(錨文字當標題);上限 100 頁、去 fragment 去重、濾二進位副檔名。UI=匯入視窗網址分頁新增「探索整站頁面」→ 頁面清單勾選(預設不勾,成本考量)→ 批次匯入(併發 2,每頁走既有 preview-job+bulk-create 各自成來源,自動認產品名一起生效)+逐頁狀態(切卡中/N 張卡/失敗原因)。不渲染 JS 紅線不變——整站匯入正是靠「商品內頁多半靜態」繞開動態首頁。純函式(parseRobots/isDisallowed)有單測。
2. ✅ **開發序2 健康檢查列**:`GET /api/ai/knowledge/health` 六類彙總(來源層=同步失敗/偵測到變動/多卡檔案未設產品名;卡片層=索引失敗/內容過短<30字/已過期停用),掃描上限 1500 卡+每類 30 筆樣本。UI=來源頁側欄頂部體檢 banner(沿用 orphan banner 視覺)+分類 chip → 清單 modal(每類附白話說明「這是什麼、怎麼修」)→ 點項目直達來源(selectSource)或卡片(openChunkById)。與 loadSources 平行載入,失敗不擋頁面。「無主卡」刻意收斂為「file 型多卡來源未設產品名」——FAQ/公告本來就沒有產品名,全算會淹沒訊號。
3. ⏭ **開發序7 小改自動套用**(評估後移下一批):要把 resync-apply 套用邏輯重構成共用 util、cron 內跑 LLM 重切卡(10 分鐘輪的時間預算風險)、且屬「無人審自動改知識卡」高風險區——單獨一批做+完整測試比較穩。防輪播 pendingHash 已在第一批上。
4. ⏭ **開發序3 缺口報表本體**:等 aiHandoffEvents 部署後累積 1–2 週資料再做,做了才有東西可聚類。

### 第三批執行紀錄(2026-08-01 午後,vitest 563 綠/typecheck 0,未 commit)
1. ✅ **開發序7 小改自動套用**(`server/utils/ai-knowledge-autoapply.ts`):變動偵測確認真變(pendingHash 過關)後先試自動——**只有最安全形態放行**:標題精確配對的內容修改、無新增無刪除、不涉手編卡、變動比例 ≤30%、新內容長度未暴跌(<50% 視為抓取不完整)、非型錄來源(總覽卡要重生的交人工);全過 → `updateKnowledgeChunk`(不標 manualEdit)逐卡更新+重建索引+摘要通知「已自動更新 N 張卡」;任一不過 → 照舊標記+通知人工審,**通知帶原因**(店家知道為什麼要自己看)。重切後無差異(排版級變動)= noop 不吵人。每輪排程限額 2 個來源(LLM 重切卡貴且慢,保住 10 分鐘輪的時間預算)。手編卡雙保險(分級判定整源退人工+computeDiff 第二輪本來就排除)。分級判定純函式 8 個單測;來源頁偵測設定卡文案同步改(原承諾「不會自動覆蓋」→ 分級說法)。
2. ✅ **開發序8 設定頁白話分層——盤點後結案,不需工程**:8/1 對照現有 ai-settings.vue 逐項確認,ai_ux_backlog 的 P0/P1 早在 2026-06-12 就全部做完(基本 5 卡+「進階調校」收合、模型欄位只給超管、通知對象=暱稱遠端搜尋選人器、回答風格三選一 preset、頂部狀態卡+啟用前 checklist);7/31 排路線圖時誤把舊帳當待辦。殘留僅 P2 小項(sticky 儲存列、0=關閉改開關等),不值得成批。

### 程式碼審查修復(2026-08-01,15 項發現全數處理,vitest 566 綠/typecheck 0)
對第二、三批(整站匯入/健康列/小改自動套用)跑完整 code review,15 項發現逐一核對原始碼後**全部屬實**,已修:

**P0(會直接傷客人 / 靜默資料遺失)**
1. **停用卡被自動套用復活**:`loadOldChunksForDiff` 只濾 isOverview,過期停用卡仍在 diff 中;自動套用會把 status 寫回 indexed,**過期募資/折扣卡無聲對客人復活**,且到期日已搬到 expiredAt、排程不會再停用第二次。修法=autoapply 改用 `listChunksBySource` 一次取回含 status,disabled 卡進 diff 但命中即整源退人工(reindex/reindex-all 早有同樣護欄,自動路徑漏了)。
2. **contentHash 先寫再跑 LLM → 逾時永久遺失變更**:自動套用是整條 cron 唯一會撞閘道逾時的工作(LLM 重切+逐卡 embedding),原本 hash 已先提交,逾時會留下「卡改一半+hash 是新值」→ 下輪比對 unchanged,永遠沒人發現。修法=抽 `commitChange()`,**一律在套用之後才提交**;逾時只是下輪重跑,已套用的卡在 diff 中是 unchanged,天然冪等。

**P1**
3. 自動套用繞過 `normalizeChunkInput`/`validateChunkInput`(其他寫入路徑都有):超長/空白卡會直接寫進知識庫 → 改在 classify 內正規化+驗證,不合規整源退人工。
4. 30% 比例規則讓 ≤3 張卡的來源**數學上永不可能自動**(1/1=100%),卻已付一次重切卡費用 → 小來源(<4 張)改用絕對張數:一次只改 1 張才自動。
5. auto_applied 未清 `outdatedAt` → 舊的未審旗標永遠掛著、店家點進去只看到空 diff;已在提交時一併清除。
6. 自動套用限額在「決定是否呼叫 LLM 之前」就扣 → 型錄來源零成本退回也吃掉名額,真正該自動的來源被擠掉;改為 cron 端先判 `generateOverview`/`urlAutoApply` 再扣。
7. 整站探索 `origin` 取自輸入網址而非**跟完轉址**的網址 → apex→www 這種常見 301 會讓 sitemap 全部被判跨域丟掉(500 頁的站只列出 1 頁);改用 `res.url` 為基準,順便把首頁 HTML 留給連結備援重用。
8. 連結備援未剝 nav/header/footer/aside → 100 頁配額被導覽與頁尾吃光,使用者只看到選單頁;比照 stripHtml 先剝區塊。
9. 整站匯入未檢查既有來源 → 同一個站跑第二次會靜默生出整組重複來源與重複卡(正是 7/31 稽核的 P0 形態);discover-pages 改回傳 `imported` 旗標,已匯入的頁標「已匯入過」且全選不會勾到。
10. 批次匯入無上限無確認(一鍵 100 頁 LLM)→ 上限 50 頁 + ≥10 頁跳確認(附預估時間與用量提醒)。
11. 中途關窗不取消 worker、result 在 resetAll 後才寫入 → 加 `siteAborted` 旗標;關窗由 runSiteImport 收尾(emit imported 讓列表帶出已建立的來源再 reset)。
12. 批次匯入只留 warnings **數量**、原文丟掉 → 改存原文並逐條顯示在頁面列(整站匯入沒有逐頁預覽,守門提醒不能只給數字)。

**P2**
13. url 自動套用沒有 per-source 開關,與 gsheetAutoApply 不對稱、且型別註解仍寫「不提供自動覆蓋」→ 新增 `urlAutoApply`(預設 true)貫穿型別/summary/PUT/UI(偵測設定卡新增「小幅文字變動」二選一),並改寫 onChangeBehavior 契約註解。
14. `loadHealth` 掛在 `loadSources` 內 → 十幾個呼叫點每次都掃上千張卡;改 60 秒節流(失敗不佔窗)。
15. `chunkScanTruncated` 算了沒人用 → 超過掃描上限時 banner 標「(部分掃描)」、清單附說明,避免店家以為清完就沒事。

### 錯誤溝通/長作業 UX 批次(2026-08-01 晚,vitest 566 綠/typecheck 0/sass 0,未 commit)
起因=老闆實測「重新同步」失敗只看到「取得差異失敗」。業界查證(Zendesk/Intercom/Chatbase/CustomGPT/Dify/Tidio/Crisp/Gorgias)五模式:錯誤帶修法、逐筆狀態+逐筆重試、長作業一律背景化、空內容當一等公民錯誤(Intercom 直接點名 JS 渲染+給解法)、抓不到給替代路徑(Crisp:改檔案上傳/手動 Q&A)。已實作:
1. **重新同步背景化(治本)**:新端點 `POST /api/ai/sources/:id/resync-jobs` 走匯入同一套 preview-jobs 狀態機(抽取在建立時做掉會當場報錯;LLM 重切+diff 留給輪詢推進,永不撞閘道逾時)——補上當年修 preview-chunks 504 時漏掉的那條路。前端:按鈕顯示階段進度(重新抓取網頁→切卡 n/m→比對新舊差異)+**取消鈕**;舊同步端點 resync-preview 改用共用 `getResyncExtracted` helper、標記 legacy 保留。
2. **錯誤三要素文案**:任何失敗訊息=發生什麼+「你的知識卡沒有被改動」(比對唯讀,可誠實保證)+下一步(再試一次/改用貼上文字/聯絡我們)。
3. **內容縮水偵測**:resync 抓回的新內容 < 舊卡總字數一半 → diff 視窗頂部紅色警告「這次只抓到 N 字(上次 M 字),大量『移除』很可能是抓不到不是真下架,建議先不要套用;網頁真改版請改用貼上文字」。防「動態頁改版→盲目套用→整源卡片被刪」。
4. **整站清單路徑分組導覽**:頁面清單頂部自動依網址第一段分組成可點 chips(「/media 1511 頁 / /projects 225 頁」),點了縮小範圍;全選改作用在「目前顯示的頁」(點分組再全選=大站挑商品頁主要動線),已勾但被篩掉的不會被取消。
- myfeel 個案結論(2026-08-01 實測):官網首頁**現在抓得到內容了**(0.4s/3752 字,與 7 月只抓到頁尾不同=網站改版過);sitemap 1768 頁(media 1511/projects 225);/products 商品清單仍動態載入抓不到 → **總覽卡維持人工維護**、整站匯入只適合「篩 /projects 手挑在售品」,勿全選。

### 錯誤溝通批次的程式碼審查修復(2026-08-01,15 項全數處理,vitest 566 綠/typecheck 0/sass 0)
**P0(資料損毀級,前兩項是這批新引入的)**
1. **applyDiff 打錯來源**:套用時用「當下選中的來源」而非「算這份 diff 的來源」。重新同步改背景作業後可跑數分鐘,期間使用者能點別的來源 → A 的新增卡會建到 B 底下,還用 A 的網頁指紋覆蓋 B 的變動偵測基準。改成一律用 `diffData.sourceId`,不一致時跳確認框。
2. **縮水警告擋不住誤刪**:紅字寫「建議先取消」,但主按鈕仍可按、所有移除項預設仍是「刪除」——動態頁改版時一鍵清空整個來源。改成縮水時移除項一律預設「保留」,並要求勾「我確認網頁真的改版了」才放行。
3. **手動重新同步吃到舊快取**:套用過一次後 contentHash 就等於快取 hash,使用者改完網頁馬上按重新同步會短路讀舊文字、回報「全部未變」。加 `forceRefetch`(手動路徑一律重抓)+快取 10 分鐘鮮度檢查。

**P1**
4. 縮水判定原本比「LLM 重寫後的卡片字數」→ LLM 這輪寫精簡就誤報整頁抓不到。改用實際抓到的網頁字數 `resyncFetchedChars`,並加「真的有卡消失」才示警。
5. 取消旗標只在 await 前檢查 → 在最慢的 finalize 按取消仍會被帶進套用畫面。改成前後都檢查。
6. `someSiteChecked` 仍用全域已勾數 → 在 A 分組勾了頁再切到 B,B 顯示半勾、點一下反而全勾上千頁。改成與「全選」同範圍。
7. 分組全選會勾到遠超上限(/media 1511 頁)再用錯誤訊息擋 → 使用者卡在「1511 個勾要自己取消」。改成勾到 50 就停並說明剩幾頁可下批。
8. 用量只在 finalize 記帳 → 取消/逾時的 job 已花的 token 完全沒入帳(取消鈕上線後會變常態)。新增 `flushJobUsage` 每步增量結清。
9. 匯入的擷取守門(太薄/只有選單頁尾)沒套用到重新同步 → 同一個網址兩條路診斷不一致。`extractionQualityWarnings` 搬到 ai-source-extractors 共用,diff 視窗也顯示。

**P2**
10. 一輪只推一段 → 100k 字文件要 13 次來回、比改寫前的並行切卡還慢。加 18 秒時間預算,單次請求內連續推進多段。
11. `computeDiff` 第二輪每對重建 bigram 集合(150×150 張 5000 字 = 上億次字串切片,卡住同機的 LINE webhook)→ 預先算好、改迭代較小集合。
12. `pageGroupKey` 每次篩選/勾選重新解析上千網址 → 探索時算一次存進列。
13. 兩份重複的輪詢實作(匯入/重新同步,已開始分歧)→ 抽成 `usePreviewJobPoll` composable。
14. `[jobId].get` 的 finalize 收尾整段複製兩份 → 重構成「分支只做各自的事 + 共用收尾」。
15. 已無呼叫者的 `resync-preview` 仍是活的第二套實作(且沒有縮水防護)→ 改回 410 並指引重新整理頁面。

### 產品別名歸一 + 切卡並行化(2026-08-02,vitest 581 綠/typecheck 0/sass 0)
**① 別名歸一(原「刻意不做」項,老闆 8/02 拍板要做)**
- 災情:「上好ㄟ抽取式除濕機」＝「NWT 威技 16L」同一台,但答題端判同產品只有三招(產品名字串相同/同來源/標題共用英數型號),中文別名三招全不中 → 當成兩台:反問要客人二選一、問出貨時間並列成兩台且日期互相矛盾。
- 新檔 `server/utils/ai-product-alias.ts`:偵測候選(純函式+9 單測)、正規化、對照表讀寫與 60s 快取。存在 `knowledgeProductIndex/{ws}` 同一份文件(`aliases`/`aliasLabels`/`dismissedPairs`)。
- **偵測出候選、由人確認,不自動合併**:「W1 REGEN」vs「W1 REGEN ULTRA」字面上也是一個包含另一個,但那是兩台不同機器,自動併會讓問 ULTRA 的客人拿到普通版答案(比原問題更嚴重)。多出來的片段含 ultra/pro/plus/型號數字 → 標「可能是不同型號」。
- 偵測訊號:①來源檔名用「｜」並列兩個叫法(最可靠,使用者上傳時就寫了)②一個名稱包含另一個。只差標點/空白的(《筋牌特務》vs 筋牌特務)**不列候選**——答題端改用正規化比對後自動視為同一台,不浪費使用者一次確認。
- 套用點:`groupSameProduct`(反問分組)、答題 context 的「產品：X」標示、`runIndexOnChunk`(新卡直接存正式名)。`productNamedInQuery` 不必改——兩個叫法併成一組後,組內成員的標題同時貢獻兩邊的識別詞,指名辨識自動生效。
- 確認後**不強制重建索引**:舊卡的別名前綴留著反而有助於用別名搜尋,答題端即時歸一即可。
- UI:來源頁工具列「產品名稱」按鈕(帶待確認數字徽章)→ 確認視窗,每組列證據+三顆按鈕(是同一台/不是/以後再說),下方列已確認對照可解除。
- 正式庫實測偵測結果:2 組高信心(上好ㄟ=威技、SHARP頂級A咖=iBarista)、3 組待判斷(其中 W1 REGEN vs ULTRA 應按「不是」)。**內容本身矛盾(5/15 已出貨 vs 8月底出貨)程式解不了,仍要人改那張卡**。

**② 切卡並行化(還技術債)**
- 改成背景作業時退化為「一輪一段」:100k 字文件 13 段 = 13 次來回、5 分鐘以上,**比改寫前的並行切卡還慢**。
- `advanceChunk` 改成一輪並行 4 段(`CHUNK_PARALLEL`),長文件來回次數降到 1/4。保留截斷對半切重試:某段截斷時先收下它之前的成果、把該段換成兩小段、cursor 停在那裡,之後已完成的結果丟棄重跑但 token 照實入帳。補 2 個測試(一輪跑完 / 超過並行上限分批)。

### f31650f 的程式碼審查修復(2026-08-02,15 項全處理,vitest 585 綠/typecheck 0/sass 0)
**P0(資料損毀 / 成果全丟)**
1. **時間預算只在步驟開始前檢查**:17.9 秒時開一個 25 秒的步驟 → 43 秒 → 撞閘道逾時,而 `saveWork` 在迴圈之後才跑 → 這次做完的每一段全部白費(token 已付),下輪從頭再跑再付一次。修法=**每步做完就存檔**+續跑前用「上一步實際耗時」預估下一步+只有「進來時就是 chunk 階段」才續跑(OCR 最後一批會翻成 chunk,順勢接下去會做成 OCR+切卡一整包超過租約)。
2. **切出 0 張卡時縮水守門完全沒攔到**:抓到的網頁字數正常、但 LLM 回空陣列(不會 throw)→ 舊卡全被標「移除」且預設刪除,而 shrink=null 沒有紅字也不用勾確認 → 一鍵刪光整個來源。修法=字數暴跌**或卡數暴跌**任一成立就示警。

**P1 — 別名功能的四個錯(不修等於做白工)**
3. 訊號1 把任何含「｜」的來源名當高信心別名,但**網址來源的 name 是網頁標題**,商城標題慣用「｜」分隔品名與站名(「…除濕機｜MiniMe 官方購物網」)→ 會把站名併成產品、汙染整庫歸屬,且 300 個 url 來源會塞爆對話框。修法=訊號1 只吃 `type='file'`。
4. 訊號1 的正式名直接用檔名片段,**沒有對齊既有產品名**:檔名多帶括號型號(WDH31B16E),卡片上存的是不含型號的寫法 → 合併後對照表查不到卡片的舊寫法,分組照樣分兩邊,**使用者按了合併卻毫無效果也毫無提示**。修法=`snapToExistingName` 對齊產品名池。
5. `confirmAlias` 同時寫進 dismissedPairs、`removeAlias` 不清 → 「解除」是單向門,按錯後永遠找不回這組候選。修法=確認時不寫 dismissedPairs(alreadyLinked 本來就會濾掉)。
6. `canonicalProductName` 只查一層 → A→B、B→C 的連鎖合併會讓 A 停在 B、卡片再度分兩組。修法=逐層解析(上限 5 跳、防成環)。

**P1 — 用量帳**
7. 並行批次撞卡片上限或非截斷錯誤 throw 時,**已完成呼叫的 token 被丟棄**(截斷分支反而有補撈,自相矛盾)。修法=allSettled 之後先把所有 fulfilled 的 token 一次入帳。
8. `flushJobUsage` 先推進水位才 await 記帳且吞掉例外 → 記帳失敗那一段永遠沒人記(正是這機制要防的事)。修法=記帳成功才推進水位。

**P2**
9. `getResyncExtracted` 的快取分支是死碼(唯一呼叫端永遠 forceRefetch)→ 刪掉死碼與參數,並在註解說明取捨:手動按鈕的語意就是「現在去看一次」,代價是可能看到比通知更新的版本(可接受,回寫 hash 仍一致)。
10. `CHUNK_PARALLEL=4` 與 chunker 既有的 `CHUNK_CONCURRENCY=5` 是同一份速率預算的兩個旋鈕、註解還宣稱一致 → 改成直接 re-export。
11. **工具列別名徽章永遠不會顯示**(數字只在開過視窗後才有)→ 使用者沒有理由去點,整個功能等於不存在。修法=候選數改由體檢端點回(它本來就撈了全部來源),開過視窗後改用即時清單。
12. 輪詢逾時訊息在抽成 composable 時掉了可行動指引 → 補回「可以再試一次;內容很長建議改用貼上文字分批」。
13. 未使用的 `recordAiUsage` import;14. resync 回應把 warnings 送兩份(頂層+巢狀)會漂移 → 只留一份;15. 確認別名後沒讓「產品名清單」快取失效(正式名 arrayUnion 進同一份文件)→ 端點一併失效。

### 部署狀態(2026-08-01)
- ✅ firestore 索引已 deploy 兩租戶(aiHandoffEvents workspaceId+createdAt);✅ TTL policy 兩專案已設(aiHandoffEvents.expireAt),順手補齊 myfeel 懸置的 webhookEventLocks TTL。
- ⏳ **程式碼:commit 7b79d43 在本機 main,待老闆 push**(此機器帳號無 repo 寫入權)→ push 即觸發 Amplify 部署。
- 部署後重驗:比較題+一句兩問用 7/31 測題;新匯入流程實抓一份說明書+一個網址走一遍(產品名預填/警示/重建索引)。

---

## 總排序速查(2026-07-31 深夜定版,老闆核可路線圖後的最終順序)

**等人動作(分鐘級,不用開發)**
- [ ] A1 老闆 push 本機兩個 commit(a47d40d 功能+a8d7dc5 文件)→ 觸發部署,啟用開關/有效期限/通知才上線
- [ ] A2 兩份 Google 試算表分享給 `firebase-adminsdk-fbsvc@linebot-e8dda.iam.gserviceaccount.com`(老闆方)
- [ ] A3 部署後:過期行銷卡逐張「停用」或設有效期限(後台操作,老闆圈選哪些)
- [ ] A4 回覆字數上限 220→300 與否(後台一格設定,老闆拍板)

**開發順序**(✅=2026-08-01 已完成,見上方三批執行紀錄)
1. ✅ 匯入自動認產品名+來源頁可編輯+產品索引自動維護(中)
2. ✅ 知識庫健康檢查警示列(小中)
2.5 ✅ 整站匯入:貼網址→列頁面清單勾選(中大)
3. 知識缺口報表+一鍵補卡(中大)← 事件流資料地基 ✅ 已上,**報表本體=等部署後資料累積 1–2 週再做(唯一剩餘工程項)**
4. ✅ 匯入守門:太薄/重複/矛盾提示+總覽卡對照+時效字眼建議設期限+動態頁文案(中)
5. ✅ 比較題檢索偏好規格卡(中);PDF 表格走 Gemini 文件理解=看重驗結果再定
6. ✅ 一句兩問漏答第二問(中)
7. ✅ 網頁來源半自動更新(中):防輪播 pendingHash+小改自動套用(分級判定,詳第三批紀錄)
8. ✅ AI 設定頁白話分層——盤點結案:2026-06-12 早已做完,7/31 誤列(詳第三批紀錄)

**內容面(要老闆方決定,系統之後會自動浮出)**:過期卡處置(A3)、補缺內容(價格/規格/清淨機/貨到付款/店面/國際運送 — 缺口報表上線後照榜補)

**刻意不做**:別名統一(已知殘留:同機兩名出貨矛盾)、動態頁 JS 渲染(紅線,整站匯入繞開)、反問選項重造(產品名補齊後待實測殘留)

---

## 優化路線圖(2026-07-31 深夜全面評估,老闆原則:自助化、不動資料庫)

評估方法:整條流水線(匯入→切卡→歸屬→索引→維護→檢索→反問→生成→轉真人→回饋)逐站問三題——文件裡有的資訊系統自己拿了嗎?出問題店家看得到嗎?店家自己修得動嗎?

### P1 匯入自助化(原則落實,最優先)
1. **匯入自動認產品名**(中):檔名+文件前段 LLM 抽產品名 → 匯入視窗預填、使用者確認可改;URL 用頁面標題。**來源頁顯示產品名+可編輯,改了自動重建該來源卡片索引**(=把 7/31 的工程師腳本變成按鈕)。順帶:產品索引隨來源增刪自動維護(原批次2小項併入)。
2. **匯入品質守門**(中):切卡預覽頁警示——擷取內容太薄/疑似只有頁尾(動態頁)、沒認出產品名、與現有卡高度重複(GPLUS 兩本說明書重複卡的教訓)、總覽卡與產品索引涵蓋率對照(「主要產品為X」矛盾偵測);匯入文案提醒動態首頁改用列表頁。(原批次2「匯入防護」擴充)

### P2 可見性與維運自動化(讓店家看得到、修得動)
3. **知識庫健康檢查列**(小中):來源頁頂部一條彙總警示——N 張無主卡/N 張內容過短/N 張已過期停用/N 個來源同步失敗,點了直接篩出來修。(把 7/31 稽核手動發現的東西變成常駐體檢)
4. **知識缺口報表**(中大,高槓桿):把「轉真人/答不出」的客人真實問題自動聚類排行(本週 12 人問價格、5 人問水箱容量…),每條旁邊「補一張卡」按鈕帶入問題直接寫卡。內容該補什麼從此不靠工程師稽核,系統自己告訴店家。
5. **網頁來源半自動更新**(中):小改自動套用+摘要通知;刪卡/大改/手編卡留人工;hash 連兩次相同新值才算真變。(原批次2)

### P3 AI 回覆工程債(客人高頻場景)
6. **比較題檢索**(中):「6L 跟 12L 差在哪」逐品項檢索仍偏向保固/故障卡、撈不到規格卡 → 比較意圖偏好規格/特色類卡。(7/31 稽核 4/4 全滅)
7. **一句兩問**(中):subQuestions 機制存在但常漏答第二問(「訂閱多少錢?不訂閱能用嗎?」)。
8. **回覆字數/列表題**(小):列表型答案被 220 字截尾;調 300 或列表題放寬,老闆拍板。
9. **AI 設定頁白話分層**(中):基本/進階分層、術語翻譯(既有 ai_ux_backlog P0)——店家看得懂才調得動。

原批次 2 的「啟用開關+有效期限」已完成;其餘批次結構見下。

## 匯入方式體檢(2026-07-31 對照業界評估)

現況:檔案=unpdf 抽字+掃描檔 Gemini OCR(30頁上限)+100k字上限;網址=**單頁**抓取、不渲染 JS(刻意紅線)、保留同域連結;gsheet=一列一卡不走 LLM(穩定)+LLM 只補問法;手寫=單卡+AI整理。切卡=Gemini 語意切卡(8k字分段/≤150卡)+「重點:」行+tags+2-3客人問法+識別碼進tags+索引時產品名前綴。

**跟業界比的結論:切卡核心不輸人**——多數同類產品(Chatbase/Dify/一般 RAG)用固定字數硬切+重疊,我們的「LLM 語意切卡+問法生成+產品名前綴」相當於業界最佳實踐(semantic chunking+query augmentation+contextual retrieval)的組合,gsheet 不走 LLM 保穩定、手編保護、變動偵測+人審 diff 也是講究的做法。**差距在入口廣度與結構化**:

- **G1 網址只吃單頁,不會爬站**(業界 Intercom/Chatbase 都是給首頁就爬整站):使用者要一頁一頁貼網址,而且首頁常是動態頁抓不到內容(7/31 總覽卡事故根源)。→ **整站匯入**:輸入網址 → 探 sitemap/同域連結 → 列出頁面清單勾選匯入(每頁一來源),尊重 robots、設頁數上限。這同時把「動態首頁」問題繞開(商品內頁多半是靜態可抓的)。工程量:中大。
- **G2 PDF 表格被打散**:unpdf 純文字抽取會把規格表拆爛(規格題/比較題答不好的上游原因之一)。既有 OCR 路徑已用 Gemini 視覺,把「含表格/版面複雜」的文件也改走 Gemini 原生文件理解(保留表格結構再切卡)。工程量:中。
- **G3 動態頁不渲染 JS**:刻意紅線,維持;由 G1(爬內頁)+匯入文案+守門警示補位。
- **G4 混合檢索/重排(reranker)**:業界標配之一;我們有向量+識別碼精確比對(務實版混合)。492 卡規模暫不需要;若 P3「比較題偏好規格卡」的輕量修法不夠,再評估 rerank。
- Sheet/手寫路徑健康,不動。

→ G1 排進 P2 之首(編號 2.5)、G2 併 P3-6(比較題)一起做評估。

## 問題↔解法 對號總表(2026-07-31,老闆問「這樣做是否就解決我們遇到的問題」)

| 遇到的問題 | 靠什麼解 | 狀態 |
|---|---|---|
| 「有賣什麼」答成廚餘處理機(總覽卡垃圾) | 已手修總覽卡;制度解=整站匯入(2.5)+守門對照(4) | ✅已止血,路線圖治本 |
| 反問出現咖啡機怪按鈕(無主卡) | 已腳本補 6 來源;制度解=自動認產品名(1) | ✅已止血,路線圖治本 |
| 點按鈕答非所問 | followupOf 程式修復 | ✅已修待部署 |
| 過期募資/折扣照答 | 日期注入+啟用開關/有效期限功能 | ✅已修;舊卡設期限=批次1人工一次 |
| 轉真人黑洞(通知關) | 收件人已設(即刻生效) | ✅已解 |
| 越界問題塞爆真人/誤殺拒答/「還是一樣」鬼打牆 | offtopic+systemPrompt 根因+unresolved 程式修復 | ✅已修待部署 |
| gsheet 403 壞半月沒人知 | 失敗通知已上;**分享權限仍待老闆方** | ✅程式已解,差一個人工動作 |
| 價格題全滅 | shopUrl 已設(引導官網);真價格卡=缺口報表(3)浮出+**內容仍要店家補** | 🟡部分解 |
| 比較題 4/4 全滅 | 比較檢索偏好規格卡(5)+PDF 表格 Gemini(5) | 🟡路線圖內,**做完要用測題重驗** |
| 一句兩問漏答 | P3-7 | 🟡路線圖內 |
| 知識缺什麼靠工程師稽核 | 健康檢查(2)+缺口報表(3) | 🟡路線圖內(系統指出洞,**填洞仍是人**) |
| 產品索引死清單 | 併入(1)自動維護 | 🟡路線圖內 |
| 別名分裂(上好ㄟ=威技答矛盾出貨時間) | **老闆拍板不做**;守門(4)的「同主題內容衝突提示」可部分預警 | 🔴殘留,已知風險 |
| 反問選項 label 品質 | 產品名補齊後大幅緩解 | 🟡待實測殘留 |
| 動態頁抓不到 | 刻意不渲染(紅線);整站匯入(2.5)繞開 | ✅接受+繞道 |

評估補充(對號時新發現,已併入守門 4):①匯入時偵測「募資/折扣/檔期」字眼 → 建議使用者順手設有效期限;②「同主題但內容矛盾」的新舊卡提示(部分補位別名不做的風險)。

背景與完整稽核見 `docs/AI-KB-AUDIT-20260731.md`。已完成項不列(見稽核文件修復紀錄)。

## 批次 0:門檻(分鐘級,先清)

- [x] **commit + 部署本輪程式修改**:AI 答題修正已由老闆收進 5145234/0289093 並推上(部署管線觸發);「來源通知+啟用開關/有效期限」在後續 commit,**待老闆 push**(此機器 git 帳號無推送權限)。
- [x] **套用說明書產品名對照表 + 重建索引**(2026-07-31 完成):六份來源設定 productName、226 張卡重建向量全數成功;驗收=咖啡機卡不再偽裝怪按鈕、指名產品無資料時誠實轉真人(另補 prompt 規則:不准拿別的產品「供參考」充數)。
- [ ] **分享兩份 Google 試算表給服務帳號**(老闆方動手):客服(商品資訊)、客服(FAQ) → `firebase-adminsdk-fbsvc@linebot-e8dda.iam.gserviceaccount.com`(檢視權限);分享完同步自動恢復。

## 批次 1:知識庫內容(要老闆方決定)

- [ ] **過期行銷內容處置**:募資期間/倒數、折扣碼 NEXT300、感恩回饋價、5 月出貨進度——圈選要下架的(見批次 2 的「啟用開關+有效期限」功能,做完後可停用而非刪除)。
- [ ] **補缺的內容**:價格查詢方式、除濕機水箱容量/噪音分貝等規格數字、森呼吸 NEXT(清淨機)產品卡、貨到付款、實體店面、國際運送。

## 批次 2:程式工程(排開發)

- [x] **知識卡「啟用開關+有效期限」**(老闆 7/31 提案,同日完成+端對端驗收):
  - 停用開關:status 新增 `disabled`(向量/tag 檢索天然排除,embedding 保留免重算);編輯視窗「供 AI 使用」開關;列表壓淡+「已停用」標示;reindex-all 跳過停用卡(不偷偷重新啟用)。
  - 有效期限 `activeUntil`(選填=永久,存到當日台灣時間 23:59:59):排程 `ai:expire-knowledge-cards`(10 分鐘一輪)到期自動停用、期限搬到 `expiredAt`、推播通知管理員;searchSimilarChunks 另有當場過濾兜底(最壞 10 分鐘空窗也不會被引用)。
  - 端點 `POST /api/ai/knowledge/:chunkId/settings`;到期卡把日期改到未來=自動重新上架;手動停用的卡尊重開關。
  - 驗收:停用→檢索即排除→恢復→撈得到;過期測試卡被排程正確停用+搬欄位。
  - 與「答題 prompt 帶今天日期」互補:資料層治本+答題層保險。
- [ ] **匯入防護**(中工程):擷取內容太薄→匯入預覽警示「可能不完整」;總覽卡合成對照產品清單,矛盾不准寫「主要產品為X」;匯入頁文案提醒「動態網站首頁抓不到商品,請用商品列表頁網址」。
- [ ] **網頁來源半自動更新**(中工程,與匯入防護同批):標題精確配對的小改自動套用+摘要通知;刪卡/變動>30%/手編卡/內容長度暴跌→通知人工審;hash 連續兩次相同新值才算真變(防輪播假變動)。
- [ ] **產品清單自動維護**(小工程):knowledgeProductIndex 目前是 7/10 一次性種入的死清單,改成隨來源 productName 增刪自動同步。

## 批次 3:順手小調整(選配)

- [ ] 反問按鈕點擊後客人氣泡顯示短 label(現在送整串卡片標題,機器味重)。
- [ ] 回覆字數上限(現 220)是否調高:「有賣什麼」清單尾端會被截,老闆決定。

## 刻意不做(有結論的)

- 產品別名統一(上好ㄟ=威技):老闆 7/31 拍板先不做。
- 反問選項踢無主卡/硬聚合到產品層:風險大於收益(無主卡常是正解),治本=補產品名。
- 「有賣什麼」直接注入產品索引當答案:索引無人維護+含別名重複,只當體檢不當答案;總覽卡優先。
